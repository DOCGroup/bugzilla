? test_patch
? Simple_Two_Stage/output
Index: Simple_Two_Stage/README
===================================================================
RCS file: /project/cvs-repository/ACE_wrappers-repository/TAO/orbsvcs/tests/AVStreams/Simple_Two_Stage/README,v
retrieving revision 1.1
retrieving revision 1.1.86.1
diff -u -r1.1 -r1.1.86.1
--- Simple_Two_Stage/README	27 Mar 2001 11:41:00 -0000	1.1
+++ Simple_Two_Stage/README	17 Apr 2003 20:43:03 -0000	1.1.86.1
@@ -1,4 +1,4 @@
-// $Id: README,v 1.1 2001/03/27 11:41:00 irfan Exp $
+// $Id: README,v 1.1.86.1 2003/04/17 20:43:03 yamuna Exp $
 
 Description
 -----------
@@ -24,7 +24,7 @@
 sender
 ------
 
-sender [-f <filename>] [-p <protocol>] [-r <frame rate>] [-d] 
+sender [-f <filename>] [-p <protocol>] [-r <frame rate>] [-v] [-s <localhost:port>] [-d <remote_host:port>]
 
 
 -f filename --> The file to be streamed to the receiver (defaults to
@@ -36,7 +36,11 @@
 -r framerate--> The rate at which tha data frames need to be sent
                 (defaults to 30 frames per second).
 
--d          --> Increament the TAO_debug_level for debug messages.
+-s localhost:port --> The address to be used on the local host.
+
+-d remote_host:port --> The address to be used on the remote host.
+
+-v          --> Increament the TAO_debug_level for debug messages.
 
 
 The test must be run with the naming service.  Check the run_test.pl
Index: Simple_Two_Stage/sender.cpp
===================================================================
RCS file: /project/cvs-repository/ACE_wrappers-repository/TAO/orbsvcs/tests/AVStreams/Simple_Two_Stage/sender.cpp,v
retrieving revision 1.8
retrieving revision 1.8.52.1
diff -u -r1.8 -r1.8.52.1
--- Simple_Two_Stage/sender.cpp	30 Apr 2002 21:55:47 -0000	1.8
+++ Simple_Two_Stage/sender.cpp	17 Apr 2003 17:55:11 -0000	1.8.52.1
@@ -1,4 +1,5 @@
-// $Id: sender.cpp,v 1.8 2002/04/30 21:55:47 crodrigu Exp $
+
+// $Id: sender.cpp,v 1.8.52.1 2003/04/17 17:55:11 yamuna Exp $
 
 #include "sender.h"
 #include "tao/debug.h"
@@ -34,9 +35,11 @@
     frame_count_ (0),
     filename_ ("input"),
     input_file_ (0),
-    protocol_ ("UDP"),
+    protocol_ ("QoS_UDP"),
     frame_rate_ (30.0),
-    mb_ (BUFSIZ)
+    mb_ (BUFSIZ),
+    src_addr_ (0),
+    dest_addr_ (0)
 {
 }
 
@@ -53,7 +56,7 @@
                     char **argv)
 {
   // Parse command line arguments
-  ACE_Get_Opt opts (argc, argv, "f:p:r:d");
+  ACE_Get_Opt opts (argc, argv, "f:p:r:s:d:v");
 
   int c;
   while ((c= opts ()) != -1)
@@ -69,7 +72,17 @@
         case 'r':
           this->frame_rate_ = (double)ACE_OS::atoi (opts.opt_arg ());
           break;
-        case 'd':
+	case 's':
+	  ACE_NEW_RETURN (this->src_addr_,
+			  ACE_INET_Addr (opts.opt_arg ()),
+			  0);
+	  break;
+	case 'd':
+	  ACE_NEW_RETURN (this->dest_addr_,
+			  ACE_INET_Addr (opts.opt_arg ()),
+			  0);
+	  break;
+        case 'v':
           TAO_debug_level++;
           break;
         default:
@@ -160,13 +173,17 @@
   // Initialize the  QoS
   AVStreams::streamQoS_var the_qos (new AVStreams::streamQoS);
 
+
   // Create the forward flow specification to describe the flow.
   TAO_Forward_FlowSpec_Entry entry ("Data_Receiver",
                                     "IN",
                                     "USER_DEFINED",
                                     "",
                                     this->protocol_.c_str (),
-                                    0);
+                                    this->src_addr_);
+  
+  if (this->dest_addr_ != 0)
+    entry.set_peer_addr (this->dest_addr_);
 
   AVStreams::flowSpec flow_spec (1);
   flow_spec.length (1);
@@ -399,6 +416,12 @@
   ACE_ENDTRY;
   ACE_CHECK_RETURN (-1);
   return 0;
+}
+
+Sender::~Sender (void)
+{
+  delete this->src_addr_;
+  delete this->dest_addr_;
 }
 
 #if defined (ACE_HAS_EXPLICIT_TEMPLATE_INSTANTIATION)
Index: Simple_Two_Stage/sender.h
===================================================================
RCS file: /project/cvs-repository/ACE_wrappers-repository/TAO/orbsvcs/tests/AVStreams/Simple_Two_Stage/sender.h,v
retrieving revision 1.4
retrieving revision 1.4.52.1
diff -u -r1.4 -r1.4.52.1
--- Simple_Two_Stage/sender.h	30 Apr 2002 21:55:47 -0000	1.4
+++ Simple_Two_Stage/sender.h	17 Apr 2003 20:04:59 -0000	1.4.52.1
@@ -1,5 +1,5 @@
 /* -*- C++ -*- */
-// $Id: sender.h,v 1.4 2002/04/30 21:55:47 crodrigu Exp $
+// $Id: sender.h,v 1.4.52.1 2003/04/17 20:04:59 yamuna Exp $
 
 // ============================================================================
 //
@@ -61,6 +61,9 @@
   Sender (void);
   // Constructor
 
+  ~Sender (void);
+  // Constructor
+
   int init (int argc,
             char **argv
             ACE_ENV_ARG_DECL_NOT_USED);
@@ -114,4 +117,7 @@
 
   TAO_AV_Protocol_Object *protocol_object_;
   // Protocol object corresponding to the transport protocol selected.
+
+  ACE_INET_Addr *src_addr_;
+  ACE_INET_Addr *dest_addr_;
 };
Index: Simple_Two_Stage_With_QoS/README
===================================================================
RCS file: /project/cvs-repository/ACE_wrappers-repository/TAO/orbsvcs/tests/AVStreams/Simple_Two_Stage_With_QoS/README,v
retrieving revision 1.1
retrieving revision 1.1.80.1
diff -u -r1.1 -r1.1.80.1
--- Simple_Two_Stage_With_QoS/README	20 Apr 2001 21:41:17 -0000	1.1
+++ Simple_Two_Stage_With_QoS/README	17 Apr 2003 20:46:43 -0000	1.1.80.1
@@ -1,4 +1,4 @@
-// $Id: README,v 1.1 2001/04/20 21:41:17 yamuna Exp $
+// $Id: README,v 1.1.80.1 2003/04/17 20:46:43 yamuna Exp $
 
 Description
 -----------
@@ -26,7 +26,7 @@
 sender
 ------
 
-sender [-f <filename>] [-p <protocol>] [-r <frame rate>] [-a <address>] [-d]  
+sender [-f <filename>] [-p <protocol>] [-r <frame rate>]  [-v]  [-s <localhost:port>] [-d <remote_host:port>]
 
 
 -f filename --> The file to be streamed to the receiver (defaults to
@@ -38,9 +38,12 @@
 -r framerate--> The rate at which tha data frames need to be sent
                 (defaults to 30 frames per second).
 
--a address  --> Destination address.
+-s localhost:port --> The address to be used on the local host.
 
--d          --> Increament the TAO_debug_level for debug messages.
+-d remote_host:port --> The address to be used on the remote host.
+
+
+-v          --> Increament the TAO_debug_level for debug messages.
 
 
 The test must be run with the naming service.  Check the run_test.pl
Index: Simple_Two_Stage_With_QoS/sender.cpp
===================================================================
RCS file: /project/cvs-repository/ACE_wrappers-repository/TAO/orbsvcs/tests/AVStreams/Simple_Two_Stage_With_QoS/sender.cpp,v
retrieving revision 1.10
retrieving revision 1.10.52.2
diff -u -r1.10 -r1.10.52.2
--- Simple_Two_Stage_With_QoS/sender.cpp	30 Apr 2002 21:55:47 -0000	1.10
+++ Simple_Two_Stage_With_QoS/sender.cpp	17 Apr 2003 19:31:03 -0000	1.10.52.2
@@ -1,4 +1,4 @@
-// $Id: sender.cpp,v 1.10 2002/04/30 21:55:47 crodrigu Exp $
+// $Id: sender.cpp,v 1.10.52.2 2003/04/17 19:31:03 yamuna Exp $
 
 #include "sender.h"
 #include "tao/debug.h"
@@ -74,7 +74,9 @@
     protocol_ ("QoS_UDP"),
     frame_rate_ (1.0),
     mb_ (BUFSIZ),
-    address_ ("localhost:8000")
+    address_ ("localhost:8000"),
+    src_addr_ (0),
+    dest_addr_  (0)
 {
 }
 
@@ -91,7 +93,7 @@
                     char **argv)
 {
   // Parse command line arguments
-  ACE_Get_Opt opts (argc, argv, "f:p:r:a:d");
+  ACE_Get_Opt opts (argc, argv, "f:p:r:vs:d:");
 
   int c;
   while ((c= opts ()) != -1)
@@ -107,11 +109,19 @@
         case 'r':
           this->frame_rate_ = (double)ACE_OS::atoi (opts.opt_arg ());
           break;
-        case 'd':
+        case 'v':
           TAO_debug_level++;
           break;
-        case 'a':
+	case 's':
+	  ACE_NEW_RETURN (this->src_addr_,
+			  ACE_INET_Addr (opts.opt_arg ()),
+			  0);
+	  break;
+        case 'd':
           this->address_ = opts.opt_arg ();
+	  ACE_NEW_RETURN (this->dest_addr_,
+			  ACE_INET_Addr (this->address_.c_str ()),
+			  0);
           break;
         default:
           ACE_DEBUG ((LM_DEBUG, "Unknown Option\n"));
@@ -243,9 +253,7 @@
     ACE_ERROR_RETURN ((LM_ERROR,
                        "(%P|%t) Error binding to the naming service\n"),
                       -1);
-
-  ACE_INET_Addr addr (this->address_.c_str ());
-
+  
   this->flowname_ = "Data_Receiver";
   // Create the forward flow specification to describe the flow.
   TAO_Forward_FlowSpec_Entry entry (this->flowname_.c_str (),
@@ -253,7 +261,10 @@
                                     "USER_DEFINED",
                                     "",
                                     this->protocol_.c_str (),
-                                    0);
+                                    this->src_addr_);
+
+  if (this->dest_addr_ != 0)
+    entry.set_peer_addr (this->dest_addr_);
 
   AVStreams::flowSpec flow_spec (1);
   flow_spec.length (1);
@@ -404,7 +415,7 @@
           // Reset the message block.
           this->mb_.reset ();
 
-          if (this->frame_count_ == 25)
+          if (this->frame_count_ == 3)
             {
               TAO_Forward_FlowSpec_Entry entry (this->flowname_.c_str (),
                                                 "IN",
@@ -502,6 +513,13 @@
   ACE_CHECK_RETURN (-1);
   return 0;
 }
+
+Sender::~Sender (void)
+{
+  delete this->src_addr_;
+  delete this->dest_addr_;
+}
+
 
 #if defined (ACE_HAS_EXPLICIT_TEMPLATE_INSTANTIATION)
 template class ACE_Singleton <Sender,ACE_Null_Mutex>;
Index: Simple_Two_Stage_With_QoS/sender.h
===================================================================
RCS file: /project/cvs-repository/ACE_wrappers-repository/TAO/orbsvcs/tests/AVStreams/Simple_Two_Stage_With_QoS/sender.h,v
retrieving revision 1.6
retrieving revision 1.6.52.1
diff -u -r1.6 -r1.6.52.1
--- Simple_Two_Stage_With_QoS/sender.h	30 Apr 2002 21:55:47 -0000	1.6
+++ Simple_Two_Stage_With_QoS/sender.h	17 Apr 2003 18:28:52 -0000	1.6.52.1
@@ -1,5 +1,5 @@
 /* -*- C++ -*- */
-// $Id: sender.h,v 1.6 2002/04/30 21:55:47 crodrigu Exp $
+// $Id: sender.h,v 1.6.52.1 2003/04/17 18:28:52 yamuna Exp $
 
 // ============================================================================
 //
@@ -67,6 +67,9 @@
   Sender (void);
   // Constructor
 
+  ~Sender (void);
+  // Destructor
+
   int init (int argc,
             char **argv
             ACE_ENV_ARG_DECL_NOT_USED);
@@ -128,4 +131,8 @@
   // Destination Address.
 
   ACE_CString flowname_;
+
+  ACE_INET_Addr *src_addr_;
+  ACE_INET_Addr *dest_addr_;
+
 };
