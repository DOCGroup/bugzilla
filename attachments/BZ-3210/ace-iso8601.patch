--- ACE_wrappers.orig/ace/ACE.cpp	1972-02-01 11:43:32.000000000 -0800
+++ ACE_wrappers/ace/ACE.cpp	2006-02-23 15:25:11.000000000 -0800
@@ -2523,10 +2523,26 @@
                    (int) (local.wMilliseconds * 1000));
   return &date_and_time[15 + (return_pointer_to_first_digit != 0)];
 #else  /* UNIX */
-  ACE_TCHAR timebuf[26]; // This magic number is based on the ctime(3c) man page.
   ACE_Time_Value cur_time = ACE_OS::gettimeofday ();
   time_t secs = cur_time.sec ();
 
+#ifdef ACE_USE_ISO8601_TIME
+  struct tm tms;
+  ACE_OS::localtime_r (&secs,
+                       &tms);
+  ACE_OS::sprintf (date_and_time,
+                   ACE_LIB_TEXT ("%4.4d-%2.2d-%2.2d %2.2d:%2.2d:%2.2d.%06ld"),
+                   (tms.tm_year+1900),
+                   (tms.tm_mon+1),
+                   (tms.tm_mday),
+                   (tms.tm_hour),
+                   (tms.tm_min),
+                   (tms.tm_sec),
+                   (cur_time.usec()));
+  date_and_time[26] = '\0';
+  return &date_and_time[11 + (return_pointer_to_first_digit != 0)];
+#else
+  ACE_TCHAR timebuf[26]; // This magic number is based on the ctime(3c) man page.
   ACE_OS::ctime_r (&secs,
                    timebuf,
                    sizeof timebuf);
@@ -2553,6 +2569,7 @@
                    cur_time.usec ());
   date_and_time[33] = '\0';
   return &date_and_time[15 + (return_pointer_to_first_digit != 0)];
+#endif
 #endif /* WIN32 */
 }
 
