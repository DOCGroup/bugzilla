Index: ACE_wrappers/ace/OS.h
===================================================================
RCS file: /home/src/cvs/ACE_wrappers/ace/OS.h,v
retrieving revision 1.3
retrieving revision 1.4
diff -u -u -r1.3 -r1.4
--- ACE_wrappers/ace/OS.h	2002/07/24 22:23:51	1.3
+++ ACE_wrappers/ace/OS.h	2002/11/06 20:07:39	1.4
@@ -4422,7 +4422,7 @@
     && !defined (CHORUS)
 # if defined (ACE_WIN32)
   return _timezone;  // For Win32.
-# elif defined (__Lynx__) || defined (__FreeBSD__) || defined (ACE_HAS_SUNOS4_GETTIMEOFDAY)
+# elif ( defined (__Lynx__) || defined (__FreeBSD__) || defined (ACE_HAS_SUNOS4_GETTIMEOFDAY) ) && ( !defined (__linux__) )
   long result = 0;
   struct timeval time;
   struct timezone zone;
@@ -4430,6 +4430,18 @@
   ACE_OSCALL (::gettimeofday (&time, &zone), int, -1, result);
   return zone.tz_minuteswest * 60;
 # else  /* __Lynx__ || __FreeBSD__ ... */
+# if defined (__linux__)
+  // Under Linux, gettimeofday() does not correctly set the timezone
+  // struct, so we should use the global variable <timezone>.
+  // However, it is initialized by localtime().  I assume other
+  // systems are the same (i.e., localtime() needs to be called
+  // to set <timezone>), but since no one is complaining, I will
+  // only make the change for Linux.  Note that localtime() does
+  // not care what time we actually send in, <timezone> will still
+  // be set correctly.
+  time_t now = 0;
+  ::localtime(&now);
+# endif
   return timezone;
 # endif /* __Lynx__ || __FreeBSD__ ... */
 #else
