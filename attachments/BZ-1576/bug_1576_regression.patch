Index: ACE_wrappers/tests/.cvsignore
===================================================================
RCS file: /home/src/ACE_wrappers/tests/.cvsignore,v
retrieving revision 1.3
diff -u -r1.3 .cvsignore
--- ACE_wrappers/tests/.cvsignore	4 Jun 2003 20:14:14 -0000	1.3
+++ ACE_wrappers/tests/.cvsignore	22 Aug 2003 18:01:04 -0000
@@ -122,3 +122,6 @@
 Vector_Test
 XtReactor_Test
 Bug_1268_Regression_Test
+test.reg
+testConfig.ini
+Bug_1576_Regression_Test
Index: ACE_wrappers/tests/Bug_1576_Regression_Test.cpp
===================================================================
RCS file: ACE_wrappers/tests/Bug_1576_Regression_Test.cpp
diff -N ACE_wrappers/tests/Bug_1576_Regression_Test.cpp
--- /dev/null	1 Jan 1970 00:00:00 -0000
+++ ACE_wrappers/tests/Bug_1576_Regression_Test.cpp	22 Aug 2003 18:01:04 -0000
@@ -0,0 +1,57 @@
+/**
+ * @file
+ *
+ * $Id$
+ *
+ * Reproduces the problems reported in bug 1576:
+ *   http://deuce.doc.wustl.edu/bugzilla/show_bug.cgi?id=1576
+ *
+ * @author Carlos O'Ryan <coryan@atdesk.com>
+ */
+
+#include "test_config.h"
+#include "ace/DLL.h"
+
+ACE_RCSID(tests, Bug_1576_Regression_Test, "$Id$")
+
+int
+ACE_TMAIN (int, ACE_TCHAR *[])
+{
+  ACE_START_TEST (ACE_TEXT ("Bug_1576_Regression_Test"));
+
+  ACE_DLL dll;
+
+  char const dll_name[]="NOT_A_DLL.so";
+
+  // Normally applications should check the return value, but if they
+  // ignore it...
+  int result = dll.open(dll_name);
+
+  if(result == -1)
+  {
+    ACE_DEBUG((LM_DEBUG, "Load failed, as expected (%s)\n",
+                  dll.error()));
+  }
+  else
+  {
+    ACE_ERROR((LM_ERROR, "Success loading %s ? It should have failed!\n",
+                  dll_name));
+  }
+
+  // ... and then use the DLL library, the program crashes (instead of
+  // just getting an error ...
+  void * symbol = dll.symbol("SHOULD_CRASH");
+
+  if(symbol == 0)
+  {
+    ACE_DEBUG((LM_DEBUG, "Symbol lookup failed, as expected (%s)\n",
+                  dll.error()));
+  }
+  else
+  {
+    ACE_ERROR((LM_ERROR, "Found symbol ? It should have failed!\n"));
+  }
+
+  ACE_END_TEST;
+  return 0;
+}
Index: ACE_wrappers/tests/Makefile
===================================================================
RCS file: /home/src/ACE_wrappers/tests/Makefile,v
retrieving revision 1.2
diff -u -r1.2 Makefile
--- ACE_wrappers/tests/Makefile	4 Jun 2003 20:14:14 -0000	1.2
+++ ACE_wrappers/tests/Makefile	22 Aug 2003 18:01:04 -0000
@@ -19,6 +19,7 @@
         Basic_Types_Test \
 	Bound_Ptr_Test \
 	Bug_1268_Regression_Test \
+	Bug_1576_Regression_Test \
         Buffer_Stream_Test \
         Cache_Map_Manager_Test \
         Cached_Accept_Conn_Test \
