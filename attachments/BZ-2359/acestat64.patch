--- G:\Dev\C\org\ACE_wrappers\ace\os_ns_sys_stat.h	Fri Oct 28 17:14:54 2005
+++ G:\Dev\C\ACE_wrappers\ace\os_ns_sys_stat.h	Sun Feb 19 17:37:31 2006
@@ -37,7 +37,7 @@
 ACE_BEGIN_VERSIONED_NAMESPACE_DECL
 
 # if defined (ACE_WIN32) && !defined (ACE_HAS_WINCE) && !defined (__BORLANDC__) && !defined(__IBMCPP__)
-      typedef struct _stat ACE_stat;
+      typedef struct __stat64 ACE_stat;
 # else
       typedef struct stat ACE_stat;
 # endif /* ACE_WIN32 */
@@ -53,10 +53,10 @@
 
   // non-standard
   ACE_NAMESPACE_INLINE_FUNCTION
-  long filesize (ACE_HANDLE handle);
+  ACE_LOFF_T filesize (ACE_HANDLE handle);
 
   ACE_NAMESPACE_INLINE_FUNCTION
-  long filesize (const ACE_TCHAR *handle);
+  ACE_LOFF_T filesize (const ACE_TCHAR *handle);
 
 
   //@}
--- G:\Dev\C\org\ACE_wrappers\ace\os_ns_sys_stat.inl	Mon Nov 14 09:22:48 2005
+++ G:\Dev\C\ACE_wrappers\ace\os_ns_sys_stat.inl	Sun Feb 19 17:45:47 2006
@@ -79,14 +79,9 @@
         ACE_OS::set_errno_to_last_error ();
         return -1;
       }
-    else if (fdata.nFileSizeHigh != 0)
+	else  
       {
-        errno = EINVAL;
-        return -1;
-      }
-    else
-      {
-        stp->st_size = fdata.nFileSizeLow;
+		stp->st_size = fdata.nFileSizeLow+(fdata.nFileSizeHigh<<32);
         stp->st_atime = ACE_Time_Value (fdata.ftLastAccessTime).sec ();
         stp->st_mtime = ACE_Time_Value (fdata.ftLastWriteTime).sec ();
         stp->st_ctime = ACE_Time_Value (fdata.ftCreationTime).sec ();
@@ -115,19 +110,25 @@
   // This function returns the number of bytes in the file referenced by
   // FD.
 
-  ACE_INLINE long
+  ACE_INLINE ACE_LOFF_T
   filesize (ACE_HANDLE handle)
   {
     ACE_OS_TRACE ("ACE_OS::filesize");
 #if defined (ACE_WIN32)
-    ACE_WIN32CALL_RETURN (::GetFileSize (handle, 0), long, -1);
+	DWORD  sl,sh;
+	sl=::GetFileSize (handle, &sh);
+	
+	ACE_LOFF_T s=sh;
+	s<<=32;
+	s+=sl;	
+    ACE_WIN32CALL_RETURN (s, long, -1);
 #else /* !ACE_WIN32 */
     ACE_stat sb;
-    return ACE_OS::fstat (handle, &sb) == -1 ? -1 : (long) sb.st_size;
+    return ACE_OS::fstat (handle, &sb) == -1 ? -1 : (ACE_LOFF_T) sb.st_size;
 #endif /* ACE_WIN32 */
   }
 
-  ACE_INLINE long
+  ACE_INLINE ACE_LOFF_T
   filesize (const ACE_TCHAR *filename)
   {
     ACE_OS_TRACE ("ACE_OS::filesize");
@@ -135,7 +136,7 @@
     ACE_HANDLE h = ACE_OS::open (filename, O_RDONLY);
     if (h != ACE_INVALID_HANDLE)
       {
-        long size = ACE_OS::filesize (h);
+        ACE_LOFF_T size = ACE_OS::filesize (h);
         ACE_OS::close (h);
         return size;
       }
@@ -306,7 +307,7 @@
 # if defined(__IBMCPP__)
     ACE_OSCALL_RETURN (::_stat (file,  stp), int, -1);
 #else
-    ACE_OSCALL_RETURN (::_stat (file, (struct _stat *) stp), int, -1);
+    ACE_OSCALL_RETURN (::_stat64 (file, (struct __stat64 *) stp), int, -1);
 #endif /* __IBMCPP__ */
 #else /* ACE_HAS_NONCONST_STAT */
     ACE_OSCALL_RETURN (::stat (file, stp), int, -1);
@@ -345,7 +346,7 @@
 #elif defined (__BORLANDC__)  && (__BORLANDC__ <= 0x540)
     ACE_OSCALL_RETURN (::_wstat (file, stp), int, -1);
 #elif defined (ACE_WIN32)
-    ACE_OSCALL_RETURN (::_wstat (file, (struct _stat *) stp), int, -1);
+    ACE_OSCALL_RETURN (::_wstat64 (file, (struct __stat64 *) stp), int, -1);
 #else /* ACE_HAS_WINCE */
     ACE_Wide_To_Ascii nfile (file);
     return ACE_OS::stat (nfile.char_rep (), stp);
