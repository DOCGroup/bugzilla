Index: TAO/orbsvcs/orbsvcs/HTIOP/HTIOP_Endpoint.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/HTIOP/HTIOP_Endpoint.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/HTIOP/HTIOP_Endpoint.inl	(working copy)
@@ -21,10 +21,10 @@
       TAO::HTIOP::Endpoint *endpoint =
         const_cast<TAO::HTIOP::Endpoint *> (this);
 
-      ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                         guard,
                         endpoint->addr_lookup_lock_,
-                        this->object_addr_ );
+                          ACE_UNEXPECTED(this->object_addr_) );
 
       if (!this->object_addr_set_)
         {
Index: TAO/orbsvcs/orbsvcs/Notify/Object.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/Object.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/Object.cpp	(working copy)
@@ -106,7 +106,10 @@
 TAO_Notify_Object::shutdown (void)
 {
   {
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->lock_, 1);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
+                        ace_mon,
+                        this->lock_,
+                        ACE_UNEXPECTED(1));
 
     if (this->shutdown_ == 1)
       return 1; // Another thread has already run shutdown.
Index: TAO/orbsvcs/orbsvcs/Notify/Refcountable.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/Refcountable.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/Refcountable.cpp	(working copy)
@@ -156,7 +156,7 @@
 void
 TAO_Notify_Tracker::dump( const char* title )
 {
-  ACE_Guard<TAO_SYNCH_MUTEX> grd(this->lock_);
+  ACE_GUARD_REACTION(TAO_SYNCH_MUTEX, grd, this->lock_, ACE_UNEXPECTED(;));
 
   ACE_DEBUG ((LM_DEBUG,"\nTAO_Notify_Tracker: %s\n", (title ? title : "dump")));
  
@@ -191,7 +191,8 @@
 {
   if ( p == 0 ) return;
 
-  ACE_Guard<TAO_SYNCH_MUTEX> grd(this->lock_);
+  ACE_GUARD_REACTION(TAO_SYNCH_MUTEX, grd, this->lock_, ACE_UNEXPECTED(;));
+
   int id = ++id_counter_;
  
   Entry e = { p, typeid(*p).name() };
@@ -215,7 +216,8 @@
 {
   if ( p == 0 ) return;
 
-  ACE_Guard<TAO_SYNCH_MUTEX> grd(this->lock_);
+  ACE_GUARD_REACTION(TAO_SYNCH_MUTEX, grd, this->lock_, ACE_UNEXPECTED(;));
+
   int ref_id = p->ref_id_;
   EntityMap::iterator iter( map_.find( ref_id ) );
   if ( iter == map_.end() )
@@ -237,7 +239,8 @@
   Entry e = { 0, "" };
   if ( p == 0 ) return e;
 
-  ACE_Guard<TAO_SYNCH_MUTEX> grd(this->lock_);
+  ACE_GUARD_REACTION(TAO_SYNCH_MUTEX, grd, this->lock_, ACE_UNEXPECTED(e));
+
   int ref_id = p->ref_id_;
   EntityMap::const_iterator iter( map_.find( ref_id ) );
   if ( iter != map_.end() )
Index: TAO/orbsvcs/orbsvcs/Notify/Routing_Slip_Persistence_Manager.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/Routing_Slip_Persistence_Manager.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/Routing_Slip_Persistence_Manager.cpp	(working copy)
@@ -43,7 +43,7 @@
 void
 Routing_Slip_Persistence_Manager::set_callback(Persistent_Callback* callback)
 {
-  ACE_GUARD(TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION(TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   this->callback_ = callback;
 }
 
Index: TAO/orbsvcs/orbsvcs/Notify/Routing_Slip.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/Routing_Slip.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/Routing_Slip.cpp	(working copy)
@@ -175,6 +175,11 @@
   , rspm_ (0)
 {
   Routing_Slip_Guard guard (sequence_lock_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   this->sequence_ = ++routing_slip_sequence_;
   if (DEBUG_LEVEL > 1) ACE_DEBUG ((LM_DEBUG,
       ACE_TEXT ("(%P|%t) Routing Slip #%d: constructor\n"),
@@ -219,6 +224,11 @@
 Routing_Slip::wait_persist ()
 {
   Routing_Slip_Guard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   while (!this->is_safe_)
   {
     this->until_safe_.wait ();
@@ -233,6 +243,10 @@
   TAO_Notify_ProxyConsumer::Ptr pcgrd(pc);
 
   Routing_Slip_Guard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   size_t request_id = delivery_requests_.size ();
 
@@ -289,6 +303,10 @@
 
   TAO_Notify_ProxySupplier::Ptr psgrd(ps);
   Routing_Slip_Guard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   enter_state_transient (guard);
   guard.acquire();
@@ -342,6 +360,10 @@
 
   TAO_Notify_ProxySupplier::Ptr psgrd(ps);
   Routing_Slip_Guard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   size_t request_id = delivery_requests_.size ();
 
@@ -388,6 +410,11 @@
 Routing_Slip::delivery_request_complete (size_t request_id)
 {
   Routing_Slip_Guard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   ACE_ASSERT (request_id < this->delivery_requests_.size ());
   // reset the pointer to allow the delivery_request to be deleted.
   this->delivery_requests_[request_id].reset ();
@@ -453,6 +480,11 @@
 Routing_Slip::at_front_of_persist_queue ()
 {
   Routing_Slip_Guard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   State state = this->state_;
   switch (state)
   {
@@ -512,6 +544,11 @@
   // keep this object around til this method returns.
   Routing_Slip_Ptr me(this->this_ptr_);
   Routing_Slip_Guard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   ACE_ASSERT (guard.locked ());
 
   // allow the ConsumerProxy to return from the CORBA push call.
@@ -908,6 +945,11 @@
 Routing_Slip::reconnect (void)
 {
   Routing_Slip_Guard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   enter_state_saved (guard);
 
   //@@todo is there a worker_task available to do this?
Index: TAO/orbsvcs/orbsvcs/Notify/Buffering_Strategy.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/Buffering_Strategy.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/Buffering_Strategy.cpp	(working copy)
@@ -56,7 +56,7 @@
 void
 TAO_Notify_Buffering_Strategy::shutdown (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->global_queue_lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->global_queue_lock_, ACE_UNEXPECTED(;));
 
   if (this->shutdown_)
   {
Index: TAO/orbsvcs/orbsvcs/Notify/Consumer.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/Consumer.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/Consumer.cpp	(working copy)
@@ -110,14 +110,14 @@
     request->sequence (),
     request
     ));
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, *this->proxy_lock ());
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, *this->proxy_lock (), ACE_UNEXPECTED(;));
   this->pending_events().enqueue_tail (queue_entry);
 }
 
 bool
 TAO_Notify_Consumer::enqueue_if_necessary (TAO_Notify_Method_Request_Event * request)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, *this->proxy_lock (), false);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, *this->proxy_lock (), ACE_UNEXPECTED(false));
   if (! this->pending_events().is_empty ())
     {
       if (DEBUG_LEVEL > 3)
@@ -485,7 +485,7 @@
   TAO_Notify_Consumer::Ptr self_grd (this);
 
   // dispatch events until: 1) the queue is empty; 2) the proxy shuts down, or 3) the dispatch fails
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, *this->proxy_lock ());
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, *this->proxy_lock (), ACE_UNEXPECTED(;));
   bool ok = true;
   while (ok
          && !this->proxy_supplier ()->has_shutdown ()
Index: TAO/orbsvcs/orbsvcs/Notify/Random_File.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/Random_File.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/Random_File.cpp	(working copy)
@@ -35,7 +35,7 @@
 Random_File::size() const
 {
   Random_File * const mutable_this = const_cast<Random_File *> (this);
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->lock_, 0);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, mutable_this->lock_, ACE_UNEXPECTED(0));
   ACE_OFF_T original_pos = mutable_this->tell ();
   mutable_this->ACE_FILE::seek(0, SEEK_END);
   ACE_OFF_T cursize = mutable_this->tell();
Index: TAO/orbsvcs/orbsvcs/Notify/Routing_Slip_Queue.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/Routing_Slip_Queue.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/Routing_Slip_Queue.cpp	(working copy)
@@ -93,6 +93,10 @@
   Routing_Slip_Queue::set_allowed (size_t allowed)
   {
     Guard guard (internals_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
     size_t allowed_was = this->allowed_;
     this->allowed_ = allowed;
     if (allowed == 0 && allowed_was != 0)
Index: TAO/orbsvcs/orbsvcs/Notify/Persistent_File_Allocator.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/Persistent_File_Allocator.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/Persistent_File_Allocator.cpp	(working copy)
@@ -207,7 +207,7 @@
 void
 Persistent_File_Allocator::used(size_t block_number)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->free_blocks_lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->free_blocks_lock_, ACE_UNEXPECTED(;));
   if (DEBUG_LEVEL > 0) ACE_DEBUG ((LM_DEBUG,
     ACE_TEXT ("(%P|%t) Persistent_File_Allocator::used: %d\n"),
     static_cast<int> (block_number)
@@ -298,7 +298,7 @@
 void
 Persistent_File_Allocator::free_block(const size_t block_number)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->free_blocks_lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->free_blocks_lock_, ACE_UNEXPECTED(;));
   ACE_ASSERT (this->free_blocks_.is_set (block_number));
   this->free_blocks_.set_bit(block_number, false);
 }
@@ -331,7 +331,7 @@
   if (this->thread_active_)
   {
     {
-      ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->queue_lock_);
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->queue_lock_, ACE_UNEXPECTED(;));
       this->terminate_thread_ = true;
       this->wake_up_thread_.signal();
     }
@@ -352,7 +352,7 @@
     do_more_work = false;
     Persistent_Storage_Block * blk = 0;
     {
-      ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->queue_lock_);
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->queue_lock_, ACE_UNEXPECTED(;));
       while (this->block_queue_.is_empty() && !terminate_thread_)
       {
         this->wake_up_thread_.wait();
@@ -374,7 +374,7 @@
       }
       {
         Persistent_Storage_Block * blk2 = 0;
-        ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->queue_lock_);
+        ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->queue_lock_, ACE_UNEXPECTED(;));
         this->block_queue_.dequeue_head (blk2);
         // if this triggers, someone pushed onto the head of the queue
         // or removed the head from the queue without telling ME.
Index: TAO/orbsvcs/orbsvcs/Notify/AdminProperties.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/AdminProperties.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/AdminProperties.cpp	(working copy)
@@ -80,7 +80,7 @@
 CORBA::Boolean
 TAO_Notify_AdminProperties::queue_full (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->global_queue_lock_, 1);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->global_queue_lock_, ACE_UNEXPECTED(1));
 
   if (this->max_global_queue_length () == 0)
     return 0;
Index: TAO/orbsvcs/orbsvcs/Notify/ID_Factory.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/Notify/ID_Factory.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Notify/ID_Factory.inl	(working copy)
@@ -7,14 +7,14 @@
 ACE_INLINE TAO_Notify_Object::ID
 TAO_Notify_ID_Factory::id (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mtx_, 0);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mtx_, ACE_UNEXPECTED(0));
   return ++seed_;
 }
 
 ACE_INLINE void
 TAO_Notify_ID_Factory::set_last_used (const TAO_Notify_Object::ID id)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mtx_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mtx_, ACE_UNEXPECTED(;));
   if (this->seed_ < id)
   {
     this->seed_ = id;
Index: TAO/orbsvcs/orbsvcs/PortableGroup/PG_Object_Group.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/PortableGroup/PG_Object_Group.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/PortableGroup/PG_Object_Group.cpp	(working copy)
@@ -119,7 +119,7 @@
 TAO::PG_Object_Group::get_group_specific_factories (
   PortableGroup::FactoryInfos & result) const
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
 
   // copy is needed to have some semblance of thread safeness.
   // if performance is an issue avoid this method.
@@ -129,10 +129,10 @@
 const PortableGroup::Location &
 TAO::PG_Object_Group::get_primary_location (void) const
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                     guard,
                     this->internals_,
-                    this->primary_location_);
+                      ACE_UNEXPECTED(this->primary_location_));
   return this->primary_location_;
 }
 
@@ -183,7 +183,7 @@
                                   CORBA::Object_ptr member)
 
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
 
   /////////////////////////////////////////
   // Convert the new member to a string IOR
@@ -310,7 +310,7 @@
 TAO::PG_Object_Group::remove_member (
     const PortableGroup::Location & the_location)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
   MemberInfo * info = 0;
   if (this->members_.unbind (the_location, info) == 0)
     {
@@ -366,7 +366,7 @@
 TAO::PG_Object_Group::set_properties_dynamically (
     const PortableGroup::Properties & overrides)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
 
   this->properties_.decode (overrides);
 
@@ -377,7 +377,7 @@
 TAO::PG_Object_Group::get_properties (
     PortableGroup::Properties_var & result) const
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
   this->properties_.export_properties(*result);
 }
 
@@ -583,7 +583,7 @@
     const char * type_id,
     const PortableGroup::Criteria & the_criteria)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
 
 
   if (0 != this->members_.find (the_location))
@@ -759,7 +759,7 @@
 void
 TAO::PG_Object_Group::initial_populate (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
 
   if (this->get_membership_style () == PortableGroup::MEMB_INF_CTRL)
     {
@@ -776,7 +776,7 @@
 void
 TAO::PG_Object_Group::minimum_populate (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
 
   if ( this->get_membership_style () == PortableGroup::MEMB_INF_CTRL )
     {
Index: TAO/orbsvcs/orbsvcs/PortableGroup/Portable_Group_Map.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/PortableGroup/Portable_Group_Map.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/PortableGroup/Portable_Group_Map.cpp	(working copy)
@@ -47,9 +47,10 @@
     const TAO::ObjectKey &key
   )
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              guard,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
 
   Map_Entry *new_entry;
 
@@ -111,9 +112,10 @@
                                   TAO_ServerRequest &request,
                                   CORBA::Object_out forward_to)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              guard,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
 
   // Look up the GroupId.
   Map_Entry *entry = 0;
Index: TAO/orbsvcs/orbsvcs/PortableGroup/PG_Object_Group_Manipulator.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/PortableGroup/PG_Object_Group_Manipulator.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/PortableGroup/PG_Object_Group_Manipulator.cpp	(working copy)
@@ -38,7 +38,7 @@
 
 void TAO::PG_Object_Group_Manipulator::allocate_ogid (PortableGroup::ObjectGroupId & ogid)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_ogid_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_ogid_, ACE_UNEXPECTED(;));
 
   // The numerical value used for the ObjectId increases
   // monotonically.
Index: TAO/orbsvcs/orbsvcs/PortableGroup/PG_GenericFactory.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/PortableGroup/PG_GenericFactory.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/PortableGroup/PG_GenericFactory.cpp	(working copy)
@@ -192,7 +192,7 @@
       // Successfully extracted the FactoryCreationId.  Now find the
       // TAO_PG_Factory_Set corresponding to it.
 
-      ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
       // If no entry exists in the factory map, infrastructure
       // controlled membership was not used.
@@ -274,7 +274,7 @@
   CORBA::ULong group_id,
   const PortableGroup::Location & location)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
   // If no entry exists in the factory map, infrastructure
   // controlled membership was not used.
Index: TAO/orbsvcs/orbsvcs/PortableGroup/PG_ObjectGroupManager.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/PortableGroup/PG_ObjectGroupManager.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/PortableGroup/PG_ObjectGroupManager.cpp	(working copy)
@@ -514,7 +514,7 @@
 TAO_PG_ObjectGroupManager::destroy_object_group (
   const PortableServer::ObjectId & oid)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
   TAO_PG_ObjectGroup_Map_Entry * group_entry = 0;
   if (this->object_group_map_.unbind (oid, group_entry) != 0)
@@ -558,10 +558,10 @@
 TAO_PG_ObjectGroupManager::member_count (
     PortableGroup::ObjectGroup_ptr group)
 {
-//   ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+//   ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
 //                     guard,
 //                     this->lock_,
-//                     0);
+//                       ACE_UNEXPECTED(0));
 
   TAO_PG_ObjectGroup_Map_Entry * group_entry =
     this->get_group_entry (group);
Index: TAO/orbsvcs/orbsvcs/PortableGroup/UIPMC_Endpoint.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/PortableGroup/UIPMC_Endpoint.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/PortableGroup/UIPMC_Endpoint.cpp	(working copy)
@@ -142,10 +142,10 @@
     return this->hash_val_;
 
   {
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                       guard,
                       this->addr_lookup_lock_,
-                      this->hash_val_);
+                        ACE_UNEXPECTED(this->hash_val_));
     // .. DCL
     if (this->hash_val_ != 0)
       return this->hash_val_;
Index: TAO/orbsvcs/orbsvcs/PortableGroup/PG_PropertyManager.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/PortableGroup/PG_PropertyManager.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/PortableGroup/PG_PropertyManager.cpp	(working copy)
@@ -47,7 +47,7 @@
 
   this->property_validator_.validate_property (props);
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
   this->default_properties_ = props;
 }
@@ -78,7 +78,7 @@
   if (props.length () == 0)
     return;  // @@ Throw CORBA::BAD_PARAM instead?
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
   this->remove_properties (props,
                            this->default_properties_);
@@ -97,7 +97,7 @@
   if (num_overrides == 0)
     return;  // Throw CORBA::BAD_PARAM exception instead?
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
   Type_Prop_Table::ENTRY * entry = 0;
   if (this->type_properties_.find (type_id, entry) != 0)
@@ -161,7 +161,7 @@
   if (props.length () == 0)
     return;  // @@ Throw CORBA::BAD_PARAM instead?
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
   Type_Prop_Table::ENTRY * entry = 0;
   if (this->type_properties_.find (type_id, entry) != 0)
Index: TAO/orbsvcs/orbsvcs/PortableGroup/PG_Properties_Support.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/PortableGroup/PG_Properties_Support.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/PortableGroup/PG_Properties_Support.cpp	(working copy)
@@ -56,7 +56,7 @@
     const char *type_id,
     const PortableGroup::Properties & overrides)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
 
   TAO::PG_Property_Set * typeid_properties;
   if ( 0 != this->properties_map_.find (type_id, typeid_properties))
@@ -98,7 +98,7 @@
   // Reference counted pointers could be used to allow property sets
   // for unused typeids to be deleted.
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
 
   TAO::PG_Property_Set * typeid_properties = 0;
   if ( 0 != this->properties_map_.find (type_id, typeid_properties))
Index: TAO/orbsvcs/orbsvcs/PortableGroup/PG_Property_Set.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/PortableGroup/PG_Property_Set.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/PortableGroup/PG_Property_Set.cpp	(working copy)
@@ -56,7 +56,7 @@
 void
 TAO::PG_Property_Set::decode (const PortableGroup::Properties & property_set)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
 
   size_t count = property_set.length ();
   for (size_t nItem = 0; nItem < count; ++nItem)
@@ -103,7 +103,7 @@
 
 void TAO::PG_Property_Set::clear ()
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
   for (ValueMapIterator it = this->values_.begin ();
        it != this->values_.end ();
        ++it)
@@ -115,7 +115,7 @@
 
 void TAO::PG_Property_Set::remove (const PortableGroup::Properties & property_set)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
   size_t count = property_set.length ();
   for (size_t nItem = 0; nItem < count; ++nItem)
   {
@@ -201,7 +201,7 @@
 
 void TAO::PG_Property_Set::merge_properties (ValueMap & merged_values) const
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->internals_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(;));
   if (0 != this->defaults_)
   {
     this->defaults_->merge_properties (merged_values);
@@ -222,7 +222,7 @@
   const ACE_CString & key,
   const PortableGroup::Value *& pValue) const
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, guard, this->internals_, 0);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->internals_, ACE_UNEXPECTED(0));
   int found = (0 == this->values_.find (key, pValue));
   if (! found)
   {
Index: TAO/orbsvcs/orbsvcs/ESF/ESF_Copy_On_Read.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/ESF/ESF_Copy_On_Read.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/ESF/ESF_Copy_On_Read.cpp	(working copy)
@@ -31,7 +31,7 @@
   try
     {
       {
-        ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+        ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
         size = this->collection_.size ();
         ACE_NEW (proxies, PROXY*[size]);
@@ -75,7 +75,7 @@
 TAO_ESF_Copy_On_Read<PROXY,COLLECTION,ITERATOR,ACE_LOCK>::
     connected (PROXY *proxy)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   proxy->_incr_refcnt ();
   this->collection_.connected (proxy);
@@ -85,7 +85,7 @@
 TAO_ESF_Copy_On_Read<PROXY,COLLECTION,ITERATOR,ACE_LOCK>::
     reconnected (PROXY *proxy)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   proxy->_incr_refcnt ();
   this->collection_.reconnected (proxy);
@@ -95,7 +95,7 @@
 TAO_ESF_Copy_On_Read<PROXY,COLLECTION,ITERATOR,ACE_LOCK>::
     disconnected (PROXY *proxy)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->collection_.disconnected (proxy);
 }
@@ -103,7 +103,7 @@
 template<class PROXY, class COLLECTION, class ITERATOR, class ACE_LOCK> void
 TAO_ESF_Copy_On_Read<PROXY,COLLECTION,ITERATOR,ACE_LOCK>::shutdown (void)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->collection_.shutdown ();
 }
Index: TAO/orbsvcs/orbsvcs/ESF/ESF_Copy_On_Write.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/ESF/ESF_Copy_On_Write.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/ESF/ESF_Copy_On_Write.cpp	(working copy)
@@ -54,7 +54,7 @@
 TAO_ESF_Copy_On_Write<PROXY,COLLECTION,ITERATOR,ACE_SYNCH_USE>::
     ~TAO_ESF_Copy_On_Write (void)
 {
-  ACE_GUARD (ACE_SYNCH_MUTEX_T, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX_T, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
 
   while (this->pending_writes_ != 0)
     this->cond_.wait ();
@@ -147,7 +147,7 @@
          collection (cr)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX_T, ace_mon, this->mutex);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX_T, ace_mon, this->mutex, ACE_UNEXPECTED(;));
 
     this->pending_writes++;
 
@@ -180,7 +180,7 @@
 {
   Collection *tmp = 0;
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX_T, ace_mon, this->mutex);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX_T, ace_mon, this->mutex, ACE_UNEXPECTED(;));
 
     tmp = this->collection;
     this->collection = this->copy;
Index: TAO/orbsvcs/orbsvcs/ESF/ESF_Copy_On_Write.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/ESF/ESF_Copy_On_Write.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/ESF/ESF_Copy_On_Write.inl	(working copy)
@@ -20,7 +20,7 @@
       :  collection (0),
          mutex (m)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->mutex);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex, ACE_UNEXPECTED(;));
   this->collection = collection_ref;
   this->collection->_incr_refcnt ();
 }
@@ -31,7 +31,7 @@
 {
   if (this->collection != 0)
     {
-      ACE_GUARD (ACE_LOCK, ace_mon, this->mutex);
+      ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex, ACE_UNEXPECTED(;));
       this->collection->_decr_refcnt ();
     }
 }
Index: TAO/orbsvcs/orbsvcs/ESF/ESF_Delayed_Changes.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/ESF/ESF_Delayed_Changes.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/ESF/ESF_Delayed_Changes.cpp	(working copy)
@@ -48,7 +48,7 @@
 TAO_ESF_Delayed_Changes<PROXY,COLLECTION,ITERATOR,ACE_SYNCH_USE>::
     for_each (TAO_ESF_Worker<PROXY> *worker)
 {
-  ACE_GUARD (Busy_Lock, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (Busy_Lock, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   worker->set_size(this->collection_.size());
   ITERATOR end = this->collection_.end ();
Index: TAO/orbsvcs/orbsvcs/ESF/ESF_Immediate_Changes.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/ESF/ESF_Immediate_Changes.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/ESF/ESF_Immediate_Changes.inl	(working copy)
@@ -8,7 +8,7 @@
 TAO_ESF_Immediate_Changes<PROXY,COLLECTION,ITERATOR,ACE_LOCK>::
     connected (PROXY *proxy)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   proxy->_incr_refcnt ();
   this->collection_.connected (proxy);
@@ -18,7 +18,7 @@
 TAO_ESF_Immediate_Changes<PROXY,COLLECTION,ITERATOR,ACE_LOCK>::
     reconnected (PROXY *proxy)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   proxy->_incr_refcnt ();
   this->collection_.reconnected (proxy);
@@ -28,7 +28,7 @@
 TAO_ESF_Immediate_Changes<PROXY,COLLECTION,ITERATOR,ACE_LOCK>::
     disconnected (PROXY *proxy)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->collection_.disconnected (proxy);
 }
@@ -36,7 +36,7 @@
 template<class PROXY, class COLLECTION, class ITERATOR, class ACE_LOCK> void
 TAO_ESF_Immediate_Changes<PROXY,COLLECTION,ITERATOR,ACE_LOCK>::shutdown (void)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->collection_.shutdown ();
 }
Index: TAO/orbsvcs/orbsvcs/ESF/ESF_Immediate_Changes.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/ESF/ESF_Immediate_Changes.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/ESF/ESF_Immediate_Changes.cpp	(working copy)
@@ -32,7 +32,7 @@
 TAO_ESF_Immediate_Changes<PROXY,C,ITERATOR,ACE_LOCK>::
     for_each (TAO_ESF_Worker<PROXY> *worker)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   worker->set_size(this->collection_.size());
   ITERATOR end = this->collection_.end ();
Index: TAO/orbsvcs/orbsvcs/Concurrency/CC_LockSet.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Concurrency/CC_LockSet.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Concurrency/CC_LockSet.cpp	(working copy)
@@ -153,7 +153,7 @@
 
   CC_LockModeEnum lm = lmconvert (mode);
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mlock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mlock_, ACE_UNEXPECTED(;));
 
   if (lock_[lm] == 0) // This lock is not held.
     throw CosConcurrencyControl::LockNotHeld();
@@ -210,7 +210,7 @@
 int
 CC_LockSet::lock_i (CC_LockModeEnum lm)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mlock_, 1);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mlock_, ACE_UNEXPECTED(1));
   // If the lock is not compatible with the locks we hold allready or
   // there is lock requests in the queue we cannot grant the lock and
   // thus we queue the request. Otherwise update the lock count.
@@ -231,7 +231,7 @@
 int
 CC_LockSet::try_lock_i (CC_LockModeEnum lm)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mlock_, 1);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mlock_, ACE_UNEXPECTED(1));
   // If the lock we try is compatible with the locks we hold we just
   // opdates the count. Otherwise we return false.
   if (compatible (lm) == 0)
@@ -250,7 +250,7 @@
 CC_LockSet::change_mode_i (CC_LockModeEnum lm_held,
                           CC_LockModeEnum lm_new)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mlock_, 1);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mlock_, ACE_UNEXPECTED(1));
   // If the new mode is compatible with the modes we hold we change
   // the counts for the two locks. If not we must queue the new
   // request. We can decrement the count for the old mode without
@@ -277,7 +277,7 @@
 int
 CC_LockSet::lock_held (CC_LockModeEnum lm)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mlock_, 1);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mlock_, ACE_UNEXPECTED(1));
   if (lock_[lm] > 0)
     return 1;
   else
Index: TAO/orbsvcs/orbsvcs/Security/SL3_CredentialsCurator.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Security/SL3_CredentialsCurator.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Security/SL3_CredentialsCurator.cpp	(working copy)
@@ -207,7 +207,7 @@
 {
   Credentials_Table::ENTRY * entry;
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->credentials_table_.find (credentials_id, entry) == 0)
     {
@@ -228,7 +228,7 @@
 
   CORBA::String_var method = CORBA::string_dup (acquisition_method);
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
   const int result =
     this->acquirer_factories_.bind (method.in (), factory);
Index: TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullConsumer.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullConsumer.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullConsumer.inl	(working copy)
@@ -13,7 +13,7 @@
 ACE_INLINE CORBA::Boolean
 TAO_CEC_ProxyPullConsumer::is_connected (void) const
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
 
   return this->is_connected_i ();
 }
@@ -34,7 +34,7 @@
 ACE_INLINE void
 TAO_CEC_ProxyPullConsumer::supplier (CosEventComm::PullSupplier_ptr supplier)
 {
-  ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
   this->supplier_i (supplier);
 }
Index: TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushSupplier.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushSupplier.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushSupplier.cpp	(working copy)
@@ -210,7 +210,7 @@
                               this);
 
   {
-    ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
     if (this->is_connected_i () == 0)
       return;
@@ -220,7 +220,7 @@
     {
       TAO_CEC_Unlock reverse_lock (*this->lock_);
 
-      ACE_GUARD (TAO_CEC_Unlock, ace_mon, reverse_lock);
+      ACE_GUARD_REACTION (TAO_CEC_Unlock, ace_mon, reverse_lock, ACE_UNEXPECTED(;));
       this->event_channel_->dispatching ()->push (this,
                                                   event);
     }
@@ -238,7 +238,7 @@
                                     this);
   {
 
-    ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
     if (this->is_connected_i () == 0)
       return;
@@ -248,7 +248,7 @@
     {
       TAO_CEC_Unlock reverse_lock (*this->lock_);
 
-      ACE_GUARD (TAO_CEC_Unlock, ace_mon, reverse_lock);
+      ACE_GUARD_REACTION (TAO_CEC_Unlock, ace_mon, reverse_lock, ACE_UNEXPECTED(;));
       this->typed_event_channel_->dispatching ()->invoke (this,
                                                           typed_event);
     }
@@ -264,7 +264,7 @@
                               this);
 
   {
-    ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
     if (this->is_connected_i () == 0)
       return;
@@ -274,7 +274,7 @@
     {
       TAO_CEC_Unlock reverse_lock (*this->lock_);
 
-      ACE_GUARD (TAO_CEC_Unlock, ace_mon, reverse_lock);
+      ACE_GUARD_REACTION (TAO_CEC_Unlock, ace_mon, reverse_lock, ACE_UNEXPECTED(;));
       this->event_channel_->dispatching ()->push_nocopy (this,
                                                          event);
     }
@@ -295,7 +295,7 @@
 CORBA::ULong
 TAO_CEC_ProxyPushSupplier::_incr_refcnt (void)
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
   return this->refcount_++;
 }
 
@@ -303,7 +303,7 @@
 TAO_CEC_ProxyPushSupplier::_decr_refcnt (void)
 {
   {
-    ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
     --this->refcount_;
     if (this->refcount_ != 0)
       return this->refcount_;
@@ -641,7 +641,7 @@
 {
   CosEventComm::PushConsumer_var consumer;
   {
-    ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
     if (this->is_connected_i () == 0)
       return; // TAO_THROW (CosEventComm::Disconnected ());????
 
@@ -768,7 +768,7 @@
   CORBA::Request_var target_request;
 
   {
-    ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
     if (this->is_connected_i () == 0)
     {
       return; // TAO_THROW (CosEventComm::Disconnected ());????
Index: TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushConsumer.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushConsumer.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushConsumer.cpp	(working copy)
@@ -150,7 +150,7 @@
 CORBA::ULong
 TAO_CEC_ProxyPushConsumer::_incr_refcnt (void)
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
   return this->refcount_++;
 }
 
@@ -158,7 +158,7 @@
 TAO_CEC_ProxyPushConsumer::_decr_refcnt (void)
 {
   {
-    ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
     --this->refcount_;
     if (this->refcount_ != 0)
       return this->refcount_;
@@ -331,6 +331,10 @@
   // case.
   // @@ Returning something won't work either, the error should be
   // logged though!
+  if (! ace_mon.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   if (!proxy->is_connected_i ())
     return;
@@ -355,6 +359,10 @@
     // case.
     // @@ Returning something won't work either, the error should be
     // logged though!
+    if (! ace_mon.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     --this->refcount_;
     if (this->refcount_ != 0)
Index: TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullSupplier.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullSupplier.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullSupplier.cpp	(working copy)
@@ -145,7 +145,7 @@
   if (this->is_connected () == 0)
     return;
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->queue_lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->queue_lock_, ACE_UNEXPECTED(;));
 
   // Ignore errors
   (void) this->queue_.enqueue_tail (event);
@@ -206,7 +206,7 @@
 CORBA::ULong
 TAO_CEC_ProxyPullSupplier::_incr_refcnt (void)
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
   return this->refcount_++;
 }
 
@@ -214,7 +214,7 @@
 TAO_CEC_ProxyPullSupplier::_decr_refcnt (void)
 {
   {
-    ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
     this->refcount_--;
     if (this->refcount_ != 0)
       return this->refcount_;
Index: TAO/orbsvcs/orbsvcs/CosEvent/CEC_TypedProxyPushConsumer.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/CosEvent/CEC_TypedProxyPushConsumer.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/CosEvent/CEC_TypedProxyPushConsumer.cpp	(working copy)
@@ -180,7 +180,7 @@
 CORBA::ULong
 TAO_CEC_TypedProxyPushConsumer::_incr_refcnt (void)
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
   return this->refcount_++;
 }
 
@@ -188,7 +188,7 @@
 TAO_CEC_TypedProxyPushConsumer::_decr_refcnt (void)
 {
   {
-    ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
     this->refcount_--;
     if (this->refcount_ != 0)
       return this->refcount_;
@@ -375,6 +375,10 @@
   // case.
   // @@ Returning something won't work either, the error should be
   // logged though!
+  if (! ace_mon.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   if (proxy->is_connected_i () == 0)
     return;
@@ -399,6 +403,10 @@
     // case.
     // @@ Returning something won't work either, the error should be
     // logged though!
+    if (! ace_mon.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     this->refcount_--;
     if (this->refcount_ != 0)
Index: TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullConsumer.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullConsumer.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullConsumer.cpp	(working copy)
@@ -234,7 +234,7 @@
 CORBA::ULong
 TAO_CEC_ProxyPullConsumer::_incr_refcnt (void)
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
   return this->refcount_++;
 }
 
@@ -242,7 +242,7 @@
 TAO_CEC_ProxyPullConsumer::_decr_refcnt (void)
 {
   {
-    ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
     this->refcount_--;
     if (this->refcount_ != 0)
       return this->refcount_;
Index: TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushSupplier.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushSupplier.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushSupplier.inl	(working copy)
@@ -24,7 +24,7 @@
 ACE_INLINE CORBA::Boolean
 TAO_CEC_ProxyPushSupplier::is_connected (void) const
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
 
   return this->is_connected_i ();
 }
@@ -65,7 +65,7 @@
 ACE_INLINE void
 TAO_CEC_ProxyPushSupplier::consumer (CosEventComm::PushConsumer_ptr consumer)
 {
-  ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
   this->consumer_i (consumer);
 }
@@ -74,7 +74,7 @@
 ACE_INLINE void
 TAO_CEC_ProxyPushSupplier::consumer (CosTypedEventComm::TypedPushConsumer_ptr typed_consumer)
 {
-  ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
   this->consumer_i (typed_consumer);
 }
Index: TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushConsumer.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushConsumer.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPushConsumer.inl	(working copy)
@@ -13,7 +13,7 @@
 ACE_INLINE CORBA::Boolean
 TAO_CEC_ProxyPushConsumer::is_connected (void) const
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
 
   return this->is_connected_i ();
 }
@@ -21,7 +21,7 @@
 ACE_INLINE CosEventComm::PushSupplier_ptr
 TAO_CEC_ProxyPushConsumer::supplier (void) const
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
   return CosEventComm::PushSupplier::_duplicate (this->supplier_.in ());
 }
 
@@ -34,7 +34,7 @@
 ACE_INLINE void
 TAO_CEC_ProxyPushConsumer::supplier (CosEventComm::PushSupplier_ptr supplier)
 {
-  ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
   this->supplier_i (supplier);
 }
Index: TAO/orbsvcs/orbsvcs/CosEvent/CEC_MT_Dispatching.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/CosEvent/CEC_MT_Dispatching.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/CosEvent/CEC_MT_Dispatching.cpp	(working copy)
@@ -25,7 +25,7 @@
 void
 TAO_CEC_MT_Dispatching::activate (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->active_ != 0)
     return;
@@ -49,7 +49,7 @@
 void
 TAO_CEC_MT_Dispatching::shutdown (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->active_ == 0)
     return;
Index: TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullSupplier.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullSupplier.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/CosEvent/CEC_ProxyPullSupplier.inl	(working copy)
@@ -13,7 +13,7 @@
 ACE_INLINE CORBA::Boolean
 TAO_CEC_ProxyPullSupplier::is_connected (void) const
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
 
   return this->is_connected_i ();
 }
@@ -35,7 +35,7 @@
 ACE_INLINE void
 TAO_CEC_ProxyPullSupplier::consumer (CosEventComm::PullConsumer_ptr consumer)
 {
-  ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
   this->consumer_i (consumer);
 }
Index: TAO/orbsvcs/orbsvcs/FaultTolerance/FT_ClientRequest_Interceptor.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/FaultTolerance/FT_ClientRequest_Interceptor.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/FaultTolerance/FT_ClientRequest_Interceptor.cpp	(working copy)
@@ -316,9 +316,10 @@
         }
       else
         {
-          ACE_GUARD  (ACE_Lock,
+          ACE_GUARD_REACTION (ACE_Lock,
                       guard,
-                      *this->lock_);
+                              *this->lock_,
+                              ACE_UNEXPECTED(;));
 
           ftrsc.retention_id = ++this->retention_id_;
           ftrsc.expiration_time =
Index: TAO/orbsvcs/orbsvcs/Event/ECG_CDR_Message_Receiver.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/ECG_CDR_Message_Receiver.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/ECG_CDR_Message_Receiver.cpp	(working copy)
@@ -511,7 +511,7 @@
 void
 TAO_ECG_CDR_Message_Receiver::shutdown (void)
 {
-  // ACE_GUARD (ACE_Lock, guard, *this->lock_);
+  // ACE_GUARD_REACTION (ACE_Lock, guard, *this->lock_, ACE_UNEXPECTED(;));
 
   Request_Map::iterator end = this->request_map_.end ();
   for (Request_Map::iterator i =  this->request_map_.begin ();
Index: TAO/orbsvcs/orbsvcs/Event/EC_Gateway_IIOP.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_Gateway_IIOP.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_Gateway_IIOP.cpp	(working copy)
@@ -97,7 +97,7 @@
 void
 TAO_EC_Gateway_IIOP::close (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->close_i ();
 }
@@ -105,7 +105,7 @@
 void
 TAO_EC_Gateway_IIOP::cleanup_consumer_proxies (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   // In case we are still pushing, don't cleanup the proxies
   if (this->busy_count_ != 0)
@@ -176,7 +176,7 @@
 void
 TAO_EC_Gateway_IIOP::reconnect_consumer_ec(void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->busy_count_ != 0)
     {
@@ -194,7 +194,7 @@
   if (c_qos.dependencies.length () == 0)
     return;
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->c_qos_ = c_qos;
 
@@ -452,7 +452,7 @@
     }
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     this->busy_count_++;
   }
@@ -494,7 +494,7 @@
     }
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     this->busy_count_--;
 
Index: TAO/orbsvcs/orbsvcs/Event/EC_TPC_Dispatching.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_TPC_Dispatching.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_TPC_Dispatching.cpp	(working copy)
@@ -131,7 +131,7 @@
 void
 TAO_EC_TPC_Dispatching::shutdown (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   // The MT_Dispatching strategy sends a TAO_EC_Shutdown_Task_Command
   // to the dispatching task.  Is that what we should do here?
@@ -183,7 +183,7 @@
   if (TAO_EC_TPC_debug_level > 0)
     ACE_DEBUG ((LM_DEBUG, "EC (%P|%t) TPC_Dispatching::push_nocopy(supplier=%@,consumer=%@)\n", proxy, consumer));
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   TAO_EC_Dispatching_Task* dtask;
 
   if (this->consumer_task_map_.find (consumer, dtask) == -1)
Index: TAO/orbsvcs/orbsvcs/Event/EC_Event_Channel_Base.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_Event_Channel_Base.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_Event_Channel_Base.cpp	(working copy)
@@ -114,7 +114,7 @@
   {
     // First check if the EC is idle, if it is not then we need to
     // return right away...
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (this->status_ != EC_S_IDLE)
       return;
     this->status_ = EC_S_ACTIVATING;
@@ -126,7 +126,7 @@
   {
     // Only when all the operations complete successfully we can mark
     // the EC as active...
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     ACE_ASSERT (this->status_ == EC_S_ACTIVATING);
     this->status_ = EC_S_ACTIVE;
   }
@@ -138,7 +138,7 @@
   {
     // First check if the EC is already active, if it is not then we
     // need to return right away...
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (this->status_ != EC_S_ACTIVE)
       return;
     this->status_ = EC_S_DESTROYING;
@@ -158,7 +158,7 @@
   {
     // Wait until all the shutdown() operations return before marking
     // the EC as destroyed...
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     ACE_ASSERT (this->status_ == EC_S_DESTROYING);
     this->status_ = EC_S_DESTROYED;
   }
Index: TAO/orbsvcs/orbsvcs/Event/EC_Event_Channel_Base.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_Event_Channel_Base.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_Event_Channel_Base.inl	(working copy)
@@ -199,7 +199,7 @@
 ACE_INLINE int
 TAO_EC_Event_Channel_Base::destroyed (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, 0);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(0));
   return (this->status_ == EC_S_DESTROYED);
 }
 
Index: TAO/orbsvcs/orbsvcs/Event/EC_ProxySupplier.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_ProxySupplier.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_ProxySupplier.cpp	(working copy)
@@ -155,7 +155,7 @@
 CORBA::ULong
 TAO_EC_ProxyPushSupplier::_incr_refcnt (void)
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
   return this->refcount_++;
 }
 
@@ -170,7 +170,7 @@
 TAO_EC_ProxyPushSupplier::_decr_refcnt (void)
 {
   {
-    ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
     --this->refcount_;
     if (this->refcount_ != 0)
       return this->refcount_;
@@ -442,7 +442,7 @@
 void
 TAO_EC_ProxyPushSupplier::clear (void)
 {
-  ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
   this->child_->clear ();
 }
@@ -459,7 +459,7 @@
 TAO_EC_ProxyPushSupplier::can_match (
       const RtecEventComm::EventHeader &header) const
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
 
   if (!this->is_connected_i ())
     return 0;
Index: TAO/orbsvcs/orbsvcs/Event/EC_ProxyConsumer.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_ProxyConsumer.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_ProxyConsumer.cpp	(working copy)
@@ -206,7 +206,7 @@
 CORBA::ULong
 TAO_EC_ProxyPushConsumer::_incr_refcnt (void)
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
   return this->refcount_++;
 }
 
@@ -221,7 +221,7 @@
 TAO_EC_ProxyPushConsumer::_decr_refcnt (void)
 {
   {
-    ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
     this->refcount_--;
     if (this->refcount_ != 0)
       return this->refcount_;
@@ -252,6 +252,10 @@
   // case.
   // @@ Returning something won't work either, the error should be
   // logged though!
+  if (! ace_mon.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   if (proxy->is_connected_i ())
     {
@@ -279,6 +283,10 @@
     // case.
     // @@ Returning something won't work either, the error should be
     // logged though!
+    if (! ace_mon.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     this->filter->_decr_refcnt ();
 
Index: TAO/orbsvcs/orbsvcs/Event/EC_Gateway_Sched.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_Gateway_Sched.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_Gateway_Sched.cpp	(working copy)
@@ -26,7 +26,7 @@
                            const char* consumer_name,
                            const char* supplier_name)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->init_i (supplier_ec, consumer_ec);
 
Index: TAO/orbsvcs/orbsvcs/Event/EC_MT_Dispatching.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_MT_Dispatching.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_MT_Dispatching.cpp	(working copy)
@@ -26,7 +26,7 @@
 void
 TAO_EC_MT_Dispatching::activate (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->active_ != 0)
     return;
@@ -53,7 +53,7 @@
 void
 TAO_EC_MT_Dispatching::shutdown (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->active_ == 0)
     return;
Index: TAO/orbsvcs/orbsvcs/Event/EC_ProxySupplier.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_ProxySupplier.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_ProxySupplier.inl	(working copy)
@@ -47,7 +47,7 @@
 ACE_INLINE CORBA::Boolean
 TAO_EC_ProxyPushSupplier::is_connected (void) const
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
 
   return this->is_connected_i ();
 }
@@ -55,7 +55,7 @@
 ACE_INLINE CORBA::Boolean
 TAO_EC_ProxyPushSupplier::is_suspended (void) const
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
 
   return this->suspended_;
 }
@@ -76,7 +76,7 @@
 ACE_INLINE void
 TAO_EC_ProxyPushSupplier::consumer (RtecEventComm::PushConsumer_ptr consumer)
 {
-  ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
   this->consumer_i (consumer);
 }
@@ -85,7 +85,7 @@
 TAO_EC_ProxyPushSupplier::subscriptions (void) const
 {
   // @@ TODO There should be a better way to signal errors here.
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, this->qos_);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(this->qos_));
 
   return this->qos_;
 }
Index: TAO/orbsvcs/orbsvcs/Event/EC_Per_Supplier_Filter.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_Per_Supplier_Filter.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_Per_Supplier_Filter.cpp	(working copy)
@@ -37,7 +37,7 @@
 void
 TAO_EC_Per_Supplier_Filter::bind (TAO_EC_ProxyPushConsumer* consumer)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->consumer_ != 0)
     return;
@@ -48,7 +48,7 @@
 void
 TAO_EC_Per_Supplier_Filter::unbind (TAO_EC_ProxyPushConsumer* consumer)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->consumer_ == 0 || this->consumer_ != consumer)
     return;
@@ -68,7 +68,7 @@
 void
 TAO_EC_Per_Supplier_Filter::connected (TAO_EC_ProxyPushSupplier* supplier)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->consumer_ == 0)
     return;
@@ -104,7 +104,7 @@
 void
 TAO_EC_Per_Supplier_Filter::reconnected (TAO_EC_ProxyPushSupplier* supplier)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->consumer_ == 0)
     return;
@@ -164,7 +164,7 @@
 CORBA::ULong
 TAO_EC_Per_Supplier_Filter::_incr_refcnt (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->lock_, 0);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(0));
 
   this->refcnt_++;
   return this->refcnt_;
@@ -174,7 +174,7 @@
 TAO_EC_Per_Supplier_Filter::_decr_refcnt (void)
 {
   {
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->lock_, 0);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(0));
 
     this->refcnt_--;
     if (this->refcnt_ != 0)
Index: TAO/orbsvcs/orbsvcs/Event/EC_ProxyConsumer.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/Event/EC_ProxyConsumer.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Event/EC_ProxyConsumer.inl	(working copy)
@@ -13,7 +13,7 @@
 ACE_INLINE CORBA::Boolean
 TAO_EC_ProxyPushConsumer::is_connected (void) const
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, false);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(0));
 
   return this->is_connected_i ();
 }
@@ -34,7 +34,7 @@
 ACE_INLINE void
 TAO_EC_ProxyPushConsumer::supplier (RtecEventComm::PushSupplier_ptr supplier)
 {
-  ACE_GUARD (ACE_Lock, ace_mon, *this->lock_);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
   this->supplier_i (supplier);
 }
@@ -43,7 +43,7 @@
 TAO_EC_ProxyPushConsumer::publications (void) const
 {
   // @@ TODO There should some way to signal errors here.
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->lock_, this->qos_);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->lock_, ACE_UNEXPECTED(this->qos_));
 
   return this->qos_;
 }
Index: TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FTEC_ProxyConsumer.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FTEC_ProxyConsumer.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FTEC_ProxyConsumer.cpp	(working copy)
@@ -66,6 +66,10 @@
   try{
     FTRTEC::Replication_Service* svc = FTRTEC::Replication_Service::instance();
     ACE_Read_Guard<FTRTEC::Replication_Service> locker(*svc);
+    if (! locker.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     svc->replicate_request(update,
                            &FtRtecEventChannelAdmin::EventChannelFacade::disconnect_push_consumer);
@@ -88,6 +92,10 @@
 
   FTRTEC::Replication_Service* svc = FTRTEC::Replication_Service::instance();
   ACE_Read_Guard<FTRTEC::Replication_Service> locker(*svc);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   svc->replicate_request(update, 0);
 }
Index: TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/Update_Manager.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/Update_Manager.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/Update_Manager.cpp	(working copy)
@@ -31,6 +31,11 @@
 void Update_Manager::handle_reply(int id)
 {
   ACE_Guard<TAO_SYNCH_MUTEX> guard(mutex_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   replied_[id] = true;
 
   if ((replied_ & signal_condition_) == signal_condition_) {
@@ -44,6 +49,11 @@
 void Update_Manager::handle_exception(int id)
 {
   ACE_Guard<TAO_SYNCH_MUTEX> guard(mutex_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   replied_[id] = true;
   ++transaction_level_;
 
Index: TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FTEC_Group_Manager.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FTEC_Group_Manager.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FTEC_Group_Manager.cpp	(working copy)
@@ -111,6 +111,10 @@
   if (impl_->my_position == 0) {
     FTRTEC::Replication_Service* svc = FTRTEC::Replication_Service::instance();
     ACE_Write_Guard<FTRTEC::Replication_Service> lock(*svc);
+    if (! lock.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
     add_member(info, IOGR_Maker::instance()->get_ref_version()+1);
   }
 }
@@ -248,6 +252,10 @@
   TAO_FTRTEC::Log(1, ACE_TEXT("TAO_FTEC_Group_Manager::replica_crashed\n"));
   FTRTEC::Replication_Service* svc = FTRTEC::Replication_Service::instance();
     ACE_Write_Guard<FTRTEC::Replication_Service> lock(*svc);
+    if (! lock.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
     remove_member(location, IOGR_Maker::instance()->get_ref_version()+1);
 }
 
Index: TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FT_ProxyAdmin_T.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FT_ProxyAdmin_T.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FT_ProxyAdmin_T.cpp	(working copy)
@@ -29,6 +29,10 @@
   FTRTEC::Replication_Service* svc = FTRTEC::Replication_Service::instance();
   try{
     ACE_Read_Guard<FTRTEC::Replication_Service> locker(*svc);
+    if (! locker.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     svc->replicate_request(op,
       Proxy::rollback_obtain);
@@ -64,6 +68,10 @@
   try{
     FTRTEC::Replication_Service* svc = FTRTEC::Replication_Service::instance();
     ACE_Read_Guard<FTRTEC::Replication_Service> locker(*svc);
+    if (! locker.locked())
+      {
+        ACE_UNEXPECTED(0);
+      }
     obj = IOGR_Maker::instance()->forge_iogr(result.in());
 
     result = ProxyInterface::_narrow(obj.in());
Index: TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FTEC_ProxySupplier.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FTEC_ProxySupplier.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/FtRtEvent/EventChannel/FTEC_ProxySupplier.cpp	(working copy)
@@ -67,6 +67,10 @@
     FTRTEC::Replication_Service* svc = FTRTEC::Replication_Service::instance();
 
     ACE_Read_Guard<FTRTEC::Replication_Service> locker(*svc);
+    if (! locker.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     svc->replicate_request(update,
       &FtRtecEventChannelAdmin::EventChannelFacade::disconnect_push_supplier);
@@ -89,6 +93,10 @@
   FTRTEC::Replication_Service *svc = FTRTEC::Replication_Service::instance();
 
   ACE_Read_Guard<FTRTEC::Replication_Service> locker(*svc);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   svc->replicate_request(update, 0);
 }
@@ -109,6 +117,10 @@
   {
     FTRTEC::Replication_Service* svc = FTRTEC::Replication_Service::instance();
     ACE_Read_Guard<FTRTEC::Replication_Service> locker(*svc);
+    if (! locker.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     svc->replicate_request(update,
       &FtRtecEventChannelAdmin::EventChannelFacade::resume_push_supplier);
@@ -135,6 +147,10 @@
     FTRTEC::Replication_Service* svc = FTRTEC::Replication_Service::instance();
 
     ACE_Read_Guard<FTRTEC::Replication_Service> locker(*svc);
+    if (! locker.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     svc->replicate_request(update,
       &FtRtecEventChannelAdmin::EventChannelFacade::suspend_push_supplier);
Index: TAO/orbsvcs/orbsvcs/SSLIOP/SSLIOP_Endpoint.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/SSLIOP/SSLIOP_Endpoint.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/SSLIOP/SSLIOP_Endpoint.cpp	(working copy)
@@ -243,10 +243,10 @@
   const ACE_INET_Addr &oaddr = this->object_addr();
 
   { // nested scope for the lock
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                       guard,
                       this->addr_lookup_lock_,
-                      this->hash_val_);
+                        ACE_UNEXPECTED(this->hash_val_));
     // .. DCL
     if (this->hash_val_ != 0)
       return this->hash_val_;
@@ -287,10 +287,10 @@
     {
       const ACE_INET_Addr &iiop_addr = this->iiop_endpoint_->object_addr ();
 
-      ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                         guard,
                         this->addr_lookup_lock_,
-                        this->object_addr_);
+                          ACE_UNEXPECTED(this->object_addr_));
 
       if (this->object_addr_.get_type () != AF_INET
 #if defined (ACE_HAS_IPV6)
@@ -314,9 +314,10 @@
   if (this->credentials_set_)
     return;
 
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
              guard,
-             this->addr_lookup_lock_);
+                    this->addr_lookup_lock_,
+                    ACE_UNEXPECTED(;));
 
   // double-check
   if (this->credentials_set_)
Index: TAO/orbsvcs/orbsvcs/SSLIOP/SSLIOP_CredentialsAcquirer.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/SSLIOP/SSLIOP_CredentialsAcquirer.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/SSLIOP/SSLIOP_CredentialsAcquirer.cpp	(working copy)
@@ -183,9 +183,10 @@
 {
   this->check_validity ();
 
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              guard,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
 
   if (!this->destroyed_)
     {
@@ -199,9 +200,10 @@
 void
 TAO::SSLIOP::CredentialsAcquirer::check_validity (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              guard,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
 
   if (this->destroyed_)
     throw CORBA::BAD_INV_ORDER ();
Index: TAO/orbsvcs/orbsvcs/Trader/Trader_Interfaces.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/Trader/Trader_Interfaces.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/Trader/Trader_Interfaces.cpp	(working copy)
@@ -26,7 +26,7 @@
 template <class TRADER_LOCK_TYPE, class MAP_LOCK_TYPE>
 TAO_Lookup<TRADER_LOCK_TYPE,MAP_LOCK_TYPE>::~TAO_Lookup (void)
 {
-  ACE_GUARD (TRADER_LOCK_TYPE, trader_mon, this->lock_);
+  ACE_GUARD_REACTION (TRADER_LOCK_TYPE, trader_mon, this->lock_, ACE_UNEXPECTED(;));
   for (Request_Ids::ITERATOR riter (this->request_ids_);
        ! riter.done ();
        riter.advance ())
@@ -752,7 +752,7 @@
                         CORBA::NO_MEMORY ());
     }
 
-  ACE_GUARD_RETURN (TRADER_LOCK_TYPE, trader_mon, this->lock_, 1);
+  ACE_GUARD_REACTION (TRADER_LOCK_TYPE, trader_mon, this->lock_, ACE_UNEXPECTED(1));
 
   for (Request_Ids::ITERATOR riter (this->request_ids_);
        ! riter.done ();
@@ -1399,8 +1399,8 @@
 TAO_Admin<TRADER_LOCK_TYPE,MAP_LOCK_TYPE>::
 set_request_id_stem (const CosTrading::Admin::OctetSeq& stem)
 {
-  ACE_GUARD_RETURN (TRADER_LOCK_TYPE, trader_mon, this->lock_,
-                    &this->stem_id_);
+  ACE_GUARD_REACTION (TRADER_LOCK_TYPE, trader_mon, this->lock_,
+                      ACE_UNEXPECTED(&this->stem_id_));
   this->stem_id_ = stem;
   return &this->stem_id_;
 }
Index: TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LeastLoaded.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LeastLoaded.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LeastLoaded.cpp	(working copy)
@@ -103,7 +103,7 @@
 
   if (this->load_map_ != 0)
     {
-      ACE_GUARD (TAO_SYNCH_MUTEX, guard, *this->lock_);
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, *this->lock_, ACE_UNEXPECTED(;));
 
       TAO_LB_LoadMap::ENTRY * entry = 0;
       if (this->load_map_->find (the_location, entry) == 0)
Index: TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadAlert.inl
===================================================================
--- TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadAlert.inl	(revision 623)
+++ TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadAlert.inl	(working copy)
@@ -8,7 +8,7 @@
 ACE_INLINE CORBA::Boolean
 TAO_LB_LoadAlert::alerted (void) const
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, monitor, this->lock_, 0);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(0));
 
   return this->alerted_;
 }
Index: TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadAlert.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadAlert.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadAlert.cpp	(working copy)
@@ -24,7 +24,7 @@
 void
 TAO_LB_LoadAlert::enable_alert (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, monitor, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
   this->alerted_ = 1;
 }
@@ -32,7 +32,7 @@
 void
 TAO_LB_LoadAlert::disable_alert (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, monitor, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
   this->alerted_ = 0;
 }
Index: TAO/orbsvcs/orbsvcs/LoadBalancing/LB_IORInterceptor.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/LoadBalancing/LB_IORInterceptor.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/LoadBalancing/LB_IORInterceptor.cpp	(working copy)
@@ -108,7 +108,7 @@
 TAO_LB_IORInterceptor::register_load_alert (void)
 {
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
     if (!CORBA::is_nil (this->la_ref_.in ()))
       return;
@@ -122,7 +122,7 @@
         this->load_alert_._this ();
 
       {
-        ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+        ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
 
         this->la_ref_ = la;
       }
Index: TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadMinimum.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadMinimum.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadMinimum.cpp	(working copy)
@@ -102,7 +102,7 @@
 
   if (this->load_map_ != 0)
     {
-      ACE_GUARD (TAO_SYNCH_MUTEX, guard, *this->lock_);
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, *this->lock_, ACE_UNEXPECTED(;));
 
       TAO_LB_LoadMap::ENTRY * entry;
       if (this->load_map_->find (the_location, entry) == 0)
Index: TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadManager.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadManager.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadManager.cpp	(working copy)
@@ -78,9 +78,10 @@
     throw CORBA::BAD_PARAM ();
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX,
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                guard,
-               this->load_lock_);
+                        this->load_lock_,
+                        ACE_UNEXPECTED(;));
 
     if (this->load_map_.rebind (the_location, loads) == -1)
       throw CORBA::INTERNAL ();
@@ -156,7 +157,7 @@
 void
 TAO_LB_LoadManager::enable_alert (const PortableGroup::Location & the_location)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->load_alert_lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->load_alert_lock_, ACE_UNEXPECTED(;));
 
   TAO_LB_LoadAlertMap::ENTRY * entry;
   if (this->load_alert_map_.find (the_location, entry) == 0)
@@ -187,9 +188,10 @@
         // invocation.
         ACE_Reverse_Lock<TAO_SYNCH_MUTEX> reverse_lock (
           this->load_alert_lock_);
-        ACE_GUARD (ACE_Reverse_Lock<TAO_SYNCH_MUTEX>,
+        ACE_GUARD_REACTION (ACE_Reverse_Lock<TAO_SYNCH_MUTEX>,
                    reverse_guard,
-                   reverse_lock);
+                            reverse_lock,
+                            ACE_UNEXPECTED(;));
 
         // Use AMI to make the following operation "non-blocking,"
         // allowing the caller to continue without being forced to
@@ -210,7 +212,7 @@
 void
 TAO_LB_LoadManager::disable_alert (const PortableGroup::Location & the_location)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->load_alert_lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->load_alert_lock_, ACE_UNEXPECTED(;));
 
   TAO_LB_LoadAlertMap::ENTRY * entry;
   if (this->load_alert_map_.find (the_location, entry) == 0)
@@ -240,9 +242,10 @@
         // invocation.
         ACE_Reverse_Lock<TAO_SYNCH_MUTEX> reverse_lock (
           this->load_alert_lock_);
-        ACE_GUARD (ACE_Reverse_Lock<TAO_SYNCH_MUTEX>,
+        ACE_GUARD_REACTION (ACE_Reverse_Lock<TAO_SYNCH_MUTEX>,
                    reverse_guard,
-                   reverse_lock);
+                            reverse_lock,
+                            ACE_UNEXPECTED(;));
 
         // Use AMI to make the following operation "non-blocking,"
         // allowing the caller to continue without being forced to
@@ -268,7 +271,7 @@
   if (CORBA::is_nil (load_alert))
     throw CORBA::BAD_PARAM ();
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->load_alert_lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->load_alert_lock_, ACE_UNEXPECTED(;));
 
   TAO_LB_LoadAlertInfo info;
   info.load_alert = CosLoadBalancing::LoadAlert::_duplicate (load_alert);
@@ -320,7 +323,7 @@
   // object is no longer under the control of the LoadManager.
   this->disable_alert (the_location);
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->load_alert_lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->load_alert_lock_, ACE_UNEXPECTED(;));
 
   if (this->load_alert_map_.unbind (the_location) != 0)
     {
@@ -339,9 +342,10 @@
   const CosLoadBalancing::LoadMonitor_var the_monitor =
     CosLoadBalancing::LoadMonitor::_duplicate (load_monitor);
 
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              guard,
-             this->monitor_lock_);
+                      this->monitor_lock_,
+                      ACE_UNEXPECTED(;));
 
   int result = this->monitor_map_.bind (the_location, the_monitor);
 
@@ -409,9 +413,10 @@
 TAO_LB_LoadManager::remove_load_monitor (
     const PortableGroup::Location & the_location)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              guard,
-             this->monitor_lock_);
+                      this->monitor_lock_,
+                      ACE_UNEXPECTED(;));
 
   if (this->monitor_map_.unbind (the_location) != 0)
     throw CosLoadBalancing::LocationNotFound ();
@@ -715,9 +720,10 @@
   ACE_ASSERT (!CORBA::is_nil (orb));
   ACE_ASSERT (!CORBA::is_nil (root_poa));
 
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              guard,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
 
   if (CORBA::is_nil (this->poa_.in ()))
     {
Index: TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadAverage.cpp
===================================================================
--- TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadAverage.cpp	(revision 623)
+++ TAO/orbsvcs/orbsvcs/LoadBalancing/LB_LoadAverage.cpp	(working copy)
@@ -101,7 +101,7 @@
 
   if (this->load_map_ != 0)
     {
-      ACE_GUARD (TAO_SYNCH_MUTEX, guard, *this->lock_);
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, *this->lock_, ACE_UNEXPECTED(;));
 
       TAO_LB_LoadMap::ENTRY * entry;
       if (this->load_map_->find (the_location, entry) == 0)
Index: TAO/orbsvcs/performance-tests/RTEvent/lib/Supplier.cpp
===================================================================
--- TAO/orbsvcs/performance-tests/RTEvent/lib/Supplier.cpp	(revision 623)
+++ TAO/orbsvcs/performance-tests/RTEvent/lib/Supplier.cpp	(working copy)
@@ -31,7 +31,7 @@
     ec->for_suppliers ();
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (!CORBA::is_nil (this->proxy_consumer_.in ()))
       return;
 
@@ -62,7 +62,7 @@
 {
   RtecEventChannelAdmin::ProxyPushConsumer_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (CORBA::is_nil (this->proxy_consumer_.in ()))
       return;
     proxy = this->proxy_consumer_._retn ();
@@ -84,7 +84,7 @@
   // events[0].header.type, events[0].header.source));
   RtecEventChannelAdmin::ProxyPushConsumer_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (CORBA::is_nil (this->proxy_consumer_.in ()))
       return;
     proxy = this->proxy_consumer_;
@@ -96,7 +96,7 @@
 void
 Supplier::disconnect_push_supplier (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->proxy_consumer_ =
     RtecEventChannelAdmin::ProxyPushConsumer::_nil ();
 }
Index: TAO/orbsvcs/performance-tests/RTEvent/lib/Loopback_Consumer.cpp
===================================================================
--- TAO/orbsvcs/performance-tests/RTEvent/lib/Loopback_Consumer.cpp	(revision 623)
+++ TAO/orbsvcs/performance-tests/RTEvent/lib/Loopback_Consumer.cpp	(working copy)
@@ -33,7 +33,7 @@
     ec->for_consumers ();
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (!CORBA::is_nil (this->proxy_supplier_.in ()))
       return;
 
@@ -66,7 +66,7 @@
 {
   RtecEventChannelAdmin::ProxyPushSupplier_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (CORBA::is_nil (this->proxy_supplier_.in ()))
       return;
     proxy = this->proxy_supplier_._retn ();
@@ -91,7 +91,7 @@
 void
 Loopback_Consumer::disconnect_push_consumer (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->proxy_supplier_ =
     RtecEventChannelAdmin::ProxyPushSupplier::_nil ();
 }
Index: TAO/orbsvcs/performance-tests/RTEvent/lib/Consumer.cpp
===================================================================
--- TAO/orbsvcs/performance-tests/RTEvent/lib/Consumer.cpp	(revision 623)
+++ TAO/orbsvcs/performance-tests/RTEvent/lib/Consumer.cpp	(working copy)
@@ -37,7 +37,7 @@
     ec->for_consumers ();
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (!CORBA::is_nil (this->proxy_supplier_.in ()))
       return;
 
@@ -70,7 +70,7 @@
 {
   RtecEventChannelAdmin::ProxyPushSupplier_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (CORBA::is_nil (this->proxy_supplier_.in ()))
       return;
     proxy = this->proxy_supplier_._retn ();
@@ -107,7 +107,7 @@
       ACE_OS::sleep (0);
     }
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   if (sample_history_.max_samples () == sample_history_.sample_count ())
     return;
   this->sample_history_.sample (now - creation);
@@ -116,7 +116,7 @@
 void
 Consumer::disconnect_push_consumer (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->proxy_supplier_ =
     RtecEventChannelAdmin::ProxyPushSupplier::_nil ();
 }
Index: TAO/orbsvcs/performance-tests/RTEvent/lib/Control.cpp
===================================================================
--- TAO/orbsvcs/performance-tests/RTEvent/lib/Control.cpp	(revision 623)
+++ TAO/orbsvcs/performance-tests/RTEvent/lib/Control.cpp	(working copy)
@@ -46,7 +46,7 @@
 Control::join (Federated_Test::Peer_ptr peer)
 {
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (this->peers_count_ == this->peers_expected_)
       return;
 
Index: TAO/orbsvcs/performance-tests/RTEvent/lib/Send_Task.cpp
===================================================================
--- TAO/orbsvcs/performance-tests/RTEvent/lib/Send_Task.cpp	(revision 623)
+++ TAO/orbsvcs/performance-tests/RTEvent/lib/Send_Task.cpp	(working copy)
@@ -50,7 +50,7 @@
 void
 Send_Task::stop (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->stop_ = 1;
 }
 
Index: TAO/orbsvcs/performance-tests/RTEvent/lib/Loopback_Supplier.cpp
===================================================================
--- TAO/orbsvcs/performance-tests/RTEvent/lib/Loopback_Supplier.cpp	(revision 623)
+++ TAO/orbsvcs/performance-tests/RTEvent/lib/Loopback_Supplier.cpp	(working copy)
@@ -30,7 +30,7 @@
     ec->for_suppliers ();
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (!CORBA::is_nil (this->proxy_consumer_.in ()))
       return;
 
@@ -58,7 +58,7 @@
 {
   RtecEventChannelAdmin::ProxyPushConsumer_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (CORBA::is_nil (this->proxy_consumer_.in ()))
       return;
     proxy = this->proxy_consumer_._retn ();
@@ -78,7 +78,7 @@
   // ACE_DEBUG ((LM_DEBUG, "Loopback_Supplier pushing\n"));
   RtecEventChannelAdmin::ProxyPushConsumer_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (CORBA::is_nil (this->proxy_consumer_.in ()))
       return;
     proxy = this->proxy_consumer_;
@@ -109,7 +109,7 @@
 void
 Loopback_Supplier::disconnect_push_supplier (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->proxy_consumer_ =
     RtecEventChannelAdmin::ProxyPushConsumer::_nil ();
 }
Index: TAO/orbsvcs/performance-tests/RTEvent/RTCORBA_Callback/client.cpp
===================================================================
--- TAO/orbsvcs/performance-tests/RTEvent/RTCORBA_Callback/client.cpp	(revision 623)
+++ TAO/orbsvcs/performance-tests/RTEvent/RTCORBA_Callback/client.cpp	(working copy)
@@ -134,7 +134,7 @@
 
   void stop (void)
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     this->stopped_ = 1;
   }
 
@@ -156,7 +156,7 @@
         ACE_OS::sleep (period);
 
         {
-          ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+          ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
           if (this->stopped_)
             return;
         }
Index: TAO/orbsvcs/performance-tests/RTEvent/RTCORBA_Callback/Callback.cpp
===================================================================
--- TAO/orbsvcs/performance-tests/RTEvent/RTCORBA_Callback/Callback.cpp	(revision 623)
+++ TAO/orbsvcs/performance-tests/RTEvent/RTCORBA_Callback/Callback.cpp	(working copy)
@@ -27,7 +27,7 @@
 {
   ACE_hrtime_t elapsed = ACE_OS::gethrtime () - the_timestamp;
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   if (sample_history_.max_samples () == sample_history_.sample_count ())
     return;
   this->sample_history_.sample (elapsed);
Index: TAO/orbsvcs/performance-tests/RTEvent/TCP_Baseline/client.cpp
===================================================================
--- TAO/orbsvcs/performance-tests/RTEvent/TCP_Baseline/client.cpp	(revision 623)
+++ TAO/orbsvcs/performance-tests/RTEvent/TCP_Baseline/client.cpp	(working copy)
@@ -136,7 +136,7 @@
 void
 Scavenger_Task::stop(void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->stopped_ = 1;
 }
 
Index: TAO/orbsvcs/performance-tests/RTEvent/RTCORBA_Baseline/client.cpp
===================================================================
--- TAO/orbsvcs/performance-tests/RTEvent/RTCORBA_Baseline/client.cpp	(revision 623)
+++ TAO/orbsvcs/performance-tests/RTEvent/RTCORBA_Baseline/client.cpp	(working copy)
@@ -119,7 +119,7 @@
 
   void stop (void)
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     this->stopped_ = 1;
   }
 
@@ -131,7 +131,7 @@
         ACE_OS::sleep (period);
 
         {
-          ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+          ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
           if (this->stopped_)
             return;
         }
Index: TAO/orbsvcs/tests/EC_Custom_Marshal/ECM_Consumer.cpp
===================================================================
--- TAO/orbsvcs/tests/EC_Custom_Marshal/ECM_Consumer.cpp	(revision 623)
+++ TAO/orbsvcs/tests/EC_Custom_Marshal/ECM_Consumer.cpp	(working copy)
@@ -184,7 +184,7 @@
       return;
     }
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->recv_count_mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->recv_count_mutex_, ACE_UNEXPECTED(;));
 
   this->recv_count_ += events.length ();
 
Index: TAO/orbsvcs/tests/Security/MT_IIOP_SSL/test_i.cpp
===================================================================
--- TAO/orbsvcs/tests/Security/MT_IIOP_SSL/test_i.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Security/MT_IIOP_SSL/test_i.cpp	(working copy)
@@ -27,9 +27,10 @@
 {
   if (this->validated_ == NOT_VALIDATED)
     {
-      ACE_MT (ACE_GUARD (TAO_SYNCH_MUTEX,
+      ACE_MT (ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                          ace_mon,
-                         this->mutex_));
+                                  this->mutex_,
+                                  ACE_UNEXPECTED(;)));
 
       if (this->validated_ != NOT_VALIDATED)
         return;
Index: TAO/orbsvcs/tests/CosEvent/Basic/Random.cpp
===================================================================
--- TAO/orbsvcs/tests/CosEvent/Basic/Random.cpp	(revision 623)
+++ TAO/orbsvcs/tests/CosEvent/Basic/Random.cpp	(working copy)
@@ -317,7 +317,7 @@
 {
   CosEventChannelAdmin::ProxyPushSupplier_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     if (CORBA::is_nil (this->proxy_.in ()))
       {
@@ -334,7 +334,7 @@
 void
 RND_Consumer::disconnect (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (CORBA::is_nil (this->proxy_.in ()))
     return;
@@ -361,7 +361,7 @@
 {
   CosEventChannelAdmin::ProxyPushConsumer_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     if (CORBA::is_nil (this->proxy_.in ()))
       {
@@ -384,7 +384,7 @@
 void
 RND_Supplier::disconnect (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (CORBA::is_nil (this->proxy_.in ()))
     return;
@@ -408,7 +408,7 @@
 {
   CosEventChannelAdmin::ProxyPushConsumer_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     if (CORBA::is_nil (this->proxy_.in ()))
       return;
Index: TAO/orbsvcs/tests/CosEvent/Timeout/TestEventConsumer_i.cpp
===================================================================
--- TAO/orbsvcs/tests/CosEvent/Timeout/TestEventConsumer_i.cpp	(revision 623)
+++ TAO/orbsvcs/tests/CosEvent/Timeout/TestEventConsumer_i.cpp	(working copy)
@@ -55,7 +55,7 @@
           ACE_DEBUG ((LM_DEBUG, ACE_TEXT ("TestEventConsumer_i::push(): ")
                                 ACE_TEXT ("Simulating hung consumer\n")));
           {
-            ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->mtx_);
+            ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->mtx_, ACE_UNEXPECTED(;));
             while (!this->shutdown_)
               this->cond_.wait ();
           }
Index: TAO/orbsvcs/tests/CosEvent/lib/Counting_Consumer.cpp
===================================================================
--- TAO/orbsvcs/tests/CosEvent/lib/Counting_Consumer.cpp	(revision 623)
+++ TAO/orbsvcs/tests/CosEvent/lib/Counting_Consumer.cpp	(working copy)
@@ -220,7 +220,7 @@
 void
 CEC_Counting_Consumer_Task::stop (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   this->stop_flag_ = 1;
 }
 
@@ -255,7 +255,7 @@
 
     if (has_event)
       {
-        ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+        ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
         this->pull_count_++;
       }
 
Index: TAO/orbsvcs/tests/CosEvent/lib/Counting_Supplier.cpp
===================================================================
--- TAO/orbsvcs/tests/CosEvent/lib/Counting_Supplier.cpp	(revision 623)
+++ TAO/orbsvcs/tests/CosEvent/lib/Counting_Supplier.cpp	(working copy)
@@ -95,7 +95,7 @@
 void
 CEC_Counting_Supplier_Task::stop (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   this->stop_flag_ = 1;
 }
 
@@ -126,7 +126,7 @@
         ACE_OS::sleep (0);
       }
 
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     this->push_count_++;
 
Index: TAO/orbsvcs/tests/Notify/performance-tests/Throughput/Throughput.cpp
===================================================================
--- TAO/orbsvcs/tests/Notify/performance-tests/Throughput/Throughput.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Notify/performance-tests/Throughput/Throughput.cpp	(working copy)
@@ -61,7 +61,7 @@
   ORBSVCS_Time::TimeT_to_hrtime (Throughput_base,
                                  Throughput_base_recorded);
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->push_count_ == 0)
     {
@@ -480,7 +480,7 @@
   // Wait till we're signalled done.
   {
     ACE_DEBUG ((LM_DEBUG, "(%t)Waiting for shutdown signal in main..\n"));
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, lock_, ACE_UNEXPECTED(;));
 
     while (this->peer_done_count_ != 0)
       {
@@ -501,7 +501,7 @@
 void
 Notify_Throughput::peer_done (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, lock_, ACE_UNEXPECTED(;));
 
   if (--this->peer_done_count_ == 0)
     {
Index: TAO/orbsvcs/tests/Notify/performance-tests/RedGreen/RedGreen_Test.cpp
===================================================================
--- TAO/orbsvcs/tests/Notify/performance-tests/RedGreen/RedGreen_Test.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Notify/performance-tests/RedGreen/RedGreen_Test.cpp	(working copy)
@@ -459,9 +459,10 @@
     const CosNotification::StructuredEvent & notification
   )
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              ace_mon,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
   const char* domain_name =
     notification.header.fixed_header.event_type.domain_name;
 
Index: TAO/orbsvcs/tests/Notify/lib/Activation_Manager.cpp
===================================================================
--- TAO/orbsvcs/tests/Notify/lib/Activation_Manager.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Notify/lib/Activation_Manager.cpp	(working copy)
@@ -54,7 +54,7 @@
 void
 TAO_Notify_Tests_Activation_Manager::done (TAO_Notify_Tests_Periodic_Supplier* /*supplier*/)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   --active_suppliers_;
 
@@ -65,7 +65,7 @@
 void
 TAO_Notify_Tests_Activation_Manager::done (TAO_Notify_Tests_Periodic_Consumer* /*consumer*/)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   --active_consumers_;
 
@@ -76,7 +76,7 @@
 void
 TAO_Notify_Tests_Activation_Manager::wait_for_completion (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   while (active_suppliers_ != 0 || active_consumers_ != 0)
     this->active_done_.wait (); //@@ use timed wait.
@@ -97,7 +97,7 @@
 void
 TAO_Notify_Tests_Activation_Manager::_register (TAO_Notify_Tests_Periodic_Supplier* supplier, const char* obj_name)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   // Register in supplier map.
   ACE_CString name (obj_name);
@@ -114,7 +114,7 @@
 void
 TAO_Notify_Tests_Activation_Manager::_register (TAO_Notify_Tests_Periodic_Consumer* consumer, const char* obj_name)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   // Register in consumer map.
   ACE_CString name (obj_name);
@@ -131,7 +131,7 @@
 void
 TAO_Notify_Tests_Activation_Manager::resolve (TAO_Notify_Tests_Periodic_Supplier*& supplier, const char* obj_name)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   ACE_CString name (obj_name);
 
@@ -142,7 +142,7 @@
 void
 TAO_Notify_Tests_Activation_Manager::resolve (TAO_Notify_Tests_Periodic_Consumer*& consumer, const char* obj_name)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   ACE_CString name (obj_name);
 
@@ -251,7 +251,7 @@
 void
 TAO_Notify_Tests_Activation_Manager::wait_for_start_signal (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   while (this->started_ == 0)
     this->started_condition_.wait (); //@@ use timed wait.
@@ -260,7 +260,7 @@
 void
 TAO_Notify_Tests_Activation_Manager::start (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->started_ = 1;
 
Index: TAO/orbsvcs/tests/Notify/Basic/Updates.cpp
===================================================================
--- TAO/orbsvcs/tests/Notify/Basic/Updates.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Notify/Basic/Updates.cpp	(working copy)
@@ -123,7 +123,7 @@
       this->print_event_types (removed);
     }
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->added_count_ += added.length ();
   this->removed_count_ += removed.length ();
@@ -166,7 +166,7 @@
 void
 Updates::reset_counts (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
   this->added_count_ = 0;
   this->removed_count_ = 0;
 }
Index: TAO/orbsvcs/tests/FT_App/FT_ReplicaFactory_i.cpp
===================================================================
--- TAO/orbsvcs/tests/FT_App/FT_ReplicaFactory_i.cpp	(revision 623)
+++ TAO/orbsvcs/tests/FT_App/FT_ReplicaFactory_i.cpp	(working copy)
@@ -91,6 +91,10 @@
   //scope the guard
   {
     InternalGuard guard (this->internals_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     // be sure all replicas are gone
     // before this object disappears
@@ -484,6 +488,11 @@
   {
     // shouldn't be necessary, but create_replica assumes this
     InternalGuard guard (this->internals_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(-1);
+      }
+
     FT_TestReplica_i * replica = create_replica ("test");
 
     PortableServer::POA_var poa = replica->_default_POA ();
@@ -555,6 +564,11 @@
 void FT_ReplicaFactory_i::remove_replica(CORBA::ULong id, FT_TestReplica_i * replica)
 {
   InternalGuard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   if (id < this->replicas_.size())
   {
     if(this->replicas_[id] == replica)
@@ -594,6 +608,10 @@
   METHOD_ENTRY(FT_ReplicaFactory_i::create_object);
   ACE_UNUSED_ARG (type_id);
   InternalGuard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(0);
+    }
 
   ::TAO::PG_Property_Set decoder (the_criteria);
 
@@ -681,6 +699,10 @@
   METHOD_ENTRY(FT_ReplicaFactory_i::delete_object);
 
   InternalGuard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   CORBA::ULong factoryId;
   factory_creation_id >>= factoryId;
@@ -712,6 +734,11 @@
 {
   METHOD_ENTRY(FT_FaultDetectorFactory_i::shutdown);
   InternalGuard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   shutdown_i ();
   this->quit_requested_ = 1;
   METHOD_RETURN(FT_FaultDetectorFactory_i::shutdown);
Index: TAO/orbsvcs/tests/EC_Throughput/ECT_Consumer_Driver.cpp
===================================================================
--- TAO/orbsvcs/tests/EC_Throughput/ECT_Consumer_Driver.cpp	(revision 623)
+++ TAO/orbsvcs/tests/EC_Throughput/ECT_Consumer_Driver.cpp	(working copy)
@@ -207,7 +207,7 @@
   //
   // ACE_DEBUG ((LM_DEBUG, "(%t) events received by consumer %d\n", ID));
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   this->active_count_--;
 }
 
@@ -217,7 +217,7 @@
       RtecEventChannelAdmin::EventChannel_ptr channel)
 {
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
     this->active_count_ = this->n_consumers_;
   }
   for (int i = 0; i < this->n_consumers_; ++i)
Index: TAO/orbsvcs/tests/EC_Throughput/ECT_Throughput.cpp
===================================================================
--- TAO/orbsvcs/tests/EC_Throughput/ECT_Throughput.cpp	(revision 623)
+++ TAO/orbsvcs/tests/EC_Throughput/ECT_Throughput.cpp	(working copy)
@@ -306,7 +306,7 @@
   //
   // ACE_DEBUG ((LM_DEBUG, "(%t) events received by consumer %d\n", ID));
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   this->active_count_--;
   if (this->active_count_ <= 0)
     {
@@ -322,7 +322,7 @@
       RtecEventChannelAdmin::EventChannel_ptr channel)
 {
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
     this->active_count_ = this->n_consumers_;
   }
   for (int i = 0; i < this->n_consumers_; ++i)
Index: TAO/orbsvcs/tests/EC_Throughput/ECT_Consumer.cpp
===================================================================
--- TAO/orbsvcs/tests/EC_Throughput/ECT_Consumer.cpp	(revision 623)
+++ TAO/orbsvcs/tests/EC_Throughput/ECT_Consumer.cpp	(working copy)
@@ -118,7 +118,7 @@
       return;
     }
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   // We start the timer as soon as we receive the first event...
   if (this->recv_count_ == 0)
Index: TAO/orbsvcs/tests/Event/Basic/Control.cpp
===================================================================
--- TAO/orbsvcs/tests/Event/Basic/Control.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Event/Basic/Control.cpp	(working copy)
@@ -168,7 +168,7 @@
     }
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
     this->event_count++;
 
     if (this->event_count != this->shutdown_count_)
Index: TAO/orbsvcs/tests/Event/Basic/Observer.cpp
===================================================================
--- TAO/orbsvcs/tests/Event/Basic/Observer.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Event/Basic/Observer.cpp	(working copy)
@@ -365,7 +365,7 @@
 
       for (int i = 1; i < this->n_consumers_; ++i)
         {
-          ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+          ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
           if (this->consumers_[i]->connected ())
             {
Index: TAO/orbsvcs/tests/Event/Basic/Atomic_Reconnect.cpp
===================================================================
--- TAO/orbsvcs/tests/Event/Basic/Atomic_Reconnect.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Event/Basic/Atomic_Reconnect.cpp	(working copy)
@@ -267,7 +267,7 @@
       return;
     }
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   this->event_count++;
 
   // ACE_DEBUG ((LM_DEBUG,
Index: TAO/orbsvcs/tests/Event/Basic/Random.cpp
===================================================================
--- TAO/orbsvcs/tests/Event/Basic/Random.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Event/Basic/Random.cpp	(working copy)
@@ -376,7 +376,7 @@
 {
   RtecEventChannelAdmin::ProxyPushSupplier_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     if (CORBA::is_nil (this->proxy_.in ()))
       {
@@ -394,7 +394,7 @@
 void
 RND_Consumer::disconnect (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (CORBA::is_nil (this->proxy_.in ()))
     return;
@@ -422,7 +422,7 @@
 {
   RtecEventChannelAdmin::ProxyPushConsumer_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     if (CORBA::is_nil (this->proxy_.in ()))
       {
@@ -441,7 +441,7 @@
 void
 RND_Supplier::disconnect (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (CORBA::is_nil (this->proxy_.in ()))
     return;
@@ -466,7 +466,7 @@
 {
   RtecEventChannelAdmin::ProxyPushConsumer_var proxy;
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     if (CORBA::is_nil (this->proxy_.in ()))
       return;
Index: TAO/orbsvcs/tests/Event/Performance/Latency.cpp
===================================================================
--- TAO/orbsvcs/tests/Event/Performance/Latency.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Event/Performance/Latency.cpp	(working copy)
@@ -284,7 +284,7 @@
 int
 EC_Latency_Consumer::done (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, *this->mutex_, -1);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, *this->mutex_, ACE_UNEXPECTED(-1));
   return this->remaining_messages_ <= 0;
 }
 
@@ -296,7 +296,7 @@
                                  events[0].header.creation_time);
   ACE_hrtime_t now = ACE_OS::gethrtime ();
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, *this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, *this->mutex_, ACE_UNEXPECTED(;));
   this->history_->sample (now - creation);
   if (this->remaining_messages_ % 1000 == 0)
     {
@@ -335,7 +335,7 @@
 int
 Task::done (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, 1);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(1));
   return this->remaining_messages_ == 0;
 }
 
Index: TAO/orbsvcs/tests/Event/lib/Supplier.cpp
===================================================================
--- TAO/orbsvcs/tests/Event/lib/Supplier.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Event/lib/Supplier.cpp	(working copy)
@@ -53,7 +53,7 @@
 void
 EC_Supplier::send_event (const RtecEventComm::EventSet& event)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->push_count_ == 0)
     this->throughput_start_ = ACE_OS::gethrtime ();
Index: TAO/orbsvcs/tests/Event/lib/Counting_Supplier.cpp
===================================================================
--- TAO/orbsvcs/tests/Event/lib/Counting_Supplier.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Event/lib/Counting_Supplier.cpp	(working copy)
@@ -166,7 +166,7 @@
 void
 EC_Counting_Supplier_Task::stop (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   this->stop_flag_ = 1;
 }
 
@@ -188,7 +188,7 @@
     // Sleep for a short time to avoid spinning...
     ACE_OS::sleep (0);
 
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     this->push_count_++;
 
Index: TAO/orbsvcs/tests/Event/lib/Consumer.cpp
===================================================================
--- TAO/orbsvcs/tests/Event/lib/Consumer.cpp	(revision 623)
+++ TAO/orbsvcs/tests/Event/lib/Consumer.cpp	(working copy)
@@ -111,7 +111,7 @@
       return;
     }
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   if (this->push_count_ == 0)
     this->throughput_start_ = ACE_OS::gethrtime ();
 
Index: TAO/orbsvcs/tests/EC_Multiple/EC_Multiple.cpp
===================================================================
--- TAO/orbsvcs/tests/EC_Multiple/EC_Multiple.cpp	(revision 623)
+++ TAO/orbsvcs/tests/EC_Multiple/EC_Multiple.cpp	(working copy)
@@ -901,7 +901,7 @@
 void
 Test_ECG::wait_until_ready (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ready_mon, this->ready_mtx_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ready_mon, this->ready_mtx_, ACE_UNEXPECTED(;));
   while (!this->ready_)
     this->ready_cnd_.wait ();
 }
Index: TAO/orbsvcs/tests/LoadBalancing/GenericFactory/Infrastructure_Controlled/Factory.cpp
===================================================================
--- TAO/orbsvcs/tests/LoadBalancing/GenericFactory/Infrastructure_Controlled/Factory.cpp	(revision 623)
+++ TAO/orbsvcs/tests/LoadBalancing/GenericFactory/Infrastructure_Controlled/Factory.cpp	(working copy)
@@ -67,7 +67,7 @@
 
   if (fcid >>= my_fcid)
     {
-      ACE_GUARD (TAO_SYNCH_MUTEX, guard, this->lock_);
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, this->lock_, ACE_UNEXPECTED(;));
       Factory_Map::ENTRY *entry = 0;
       if (this->factory_map_.find (my_fcid, entry) == 0)
         {
Index: TAO/orbsvcs/Fault_Detector/FT_FaultDetectorFactory_i.cpp
===================================================================
--- TAO/orbsvcs/Fault_Detector/FT_FaultDetectorFactory_i.cpp	(revision 623)
+++ TAO/orbsvcs/Fault_Detector/FT_FaultDetectorFactory_i.cpp	(working copy)
@@ -78,6 +78,10 @@
   //scope the guard
   {
     InternalGuard guard (this->internals_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     // be sure all detectors are gone
     // before this object disappears
@@ -395,6 +399,11 @@
 void TAO::FT_FaultDetectorFactory_i::remove_detector(CORBA::ULong id, TAO::Fault_Detector_i * detector)
 {
   InternalGuard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   if (id < this->detectors_.size())
   {
     if(this->detectors_[id] == detector)
@@ -470,6 +479,11 @@
 {
   METHOD_ENTRY(TAO::FT_FaultDetectorFactory_i::shutdown);
   InternalGuard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   shutdown_i ();
   this->quit_requested_ = 1;
   METHOD_RETURN(TAO::FT_FaultDetectorFactory_i::shutdown);
@@ -485,6 +499,10 @@
 
   ACE_UNUSED_ARG (type_id); //@@ use it
   InternalGuard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(CORBA::Object::_nil ());
+    }
 
   ::TAO::PG_Property_Set decoder (the_criteria);
 
@@ -636,6 +654,10 @@
   METHOD_ENTRY(TAO::FT_FaultDetectorFactory_i::delete_object);
 
   InternalGuard guard (this->internals_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   CORBA::ULong detectorId;
   factory_creation_id >>= detectorId;
Index: TAO/orbsvcs/examples/Notify/Federation/Gate/Gate.cpp
===================================================================
--- TAO/orbsvcs/examples/Notify/Federation/Gate/Gate.cpp	(revision 623)
+++ TAO/orbsvcs/examples/Notify/Federation/Gate/Gate.cpp	(working copy)
@@ -24,6 +24,10 @@
   //
   {
     Lock l (mutex_);
+    if (! l.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
     stop_ = true;
   }
 
@@ -126,6 +130,10 @@
       //
       {
         Lock l (mutex_);
+        if (! l.locked())
+          {
+            ACE_UNEXPECTED(;);
+          }
 
         if (stop_)
           return;
Index: TAO/orbsvcs/examples/Notify/ThreadPool/Supplier.cpp
===================================================================
--- TAO/orbsvcs/examples/Notify/ThreadPool/Supplier.cpp	(revision 623)
+++ TAO/orbsvcs/examples/Notify/ThreadPool/Supplier.cpp	(working copy)
@@ -38,7 +38,7 @@
 TAO_Notify_ThreadPool_Supplier::run (void)
 {
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
     ACE_DEBUG ((LM_DEBUG, "(%P, %t) Waiting for %d consumers to connect...\n", this->expected_consumer_count_-1));
 
@@ -140,7 +140,7 @@
                                       const CosNotification::EventTypeSeq & /*removed */
                                       )
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   // Count the number of consumers connect and signal the supplier thread when the expected count have connected.
   // Only 1 consumer connects at a time.
Index: TAO/orbsvcs/examples/Notify/ThreadPool/Consumer.cpp
===================================================================
--- TAO/orbsvcs/examples/Notify/ThreadPool/Consumer.cpp	(revision 623)
+++ TAO/orbsvcs/examples/Notify/ThreadPool/Consumer.cpp	(working copy)
@@ -133,7 +133,7 @@
 TAO_Notify_ThreadPool_Consumer::push_structured_event (const CosNotification::StructuredEvent & /*notification*/
                                             )
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (TAO_debug_level > 0)
     ACE_DEBUG ((LM_DEBUG, "(%P, %t) Consumer received event %d\n",
Index: TAO/orbsvcs/examples/Notify/Lanes/Supplier.cpp
===================================================================
--- TAO/orbsvcs/examples/Notify/Lanes/Supplier.cpp	(revision 623)
+++ TAO/orbsvcs/examples/Notify/Lanes/Supplier.cpp	(working copy)
@@ -36,7 +36,7 @@
   RTCORBA::Priority priority = 1;
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
     ACE_DEBUG ((LM_DEBUG, "(%P, %t) Waiting for %d consumers to connect...\n", this->expected_consumer_count_));
 
@@ -140,7 +140,7 @@
                                       const CosNotification::EventTypeSeq & /*removed */
                                       )
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, mon, this->lock_, ACE_UNEXPECTED(;));
 
   // Count the number of consumers connect and signal the supplier thread when the expected count have connected.
   if (added.length () > 0)
Index: TAO/docs/tutorials/Quoter/RTCORBA/Distributor_i.cpp
===================================================================
--- TAO/docs/tutorials/Quoter/RTCORBA/Distributor_i.cpp	(revision 623)
+++ TAO/docs/tutorials/Quoter/RTCORBA/Distributor_i.cpp	(working copy)
@@ -235,7 +235,9 @@
                  StrSeq_Converter (corba_stocks));
 
 
-  ACE_READ_GUARD (ACE_RW_Thread_Mutex, g, lock_);
+  ACE_Read_Guard< ACE_RW_Thread_Mutex > g ( lock_ );
+  if ( g.locked() == 0 ) ACE_UNEXPECTED(;);
+
   std::for_each (this->subscribers_list_.begin (),
                  this->subscribers_list_.end (),
                  Stock_Publisher (this->orb_.in (),
Index: TAO/docs/tutorials/Quoter/RTCORBA/Stock_Database.tpp
===================================================================
--- TAO/docs/tutorials/Quoter/RTCORBA/Stock_Database.tpp	(revision 623)
+++ TAO/docs/tutorials/Quoter/RTCORBA/Stock_Database.tpp	(working copy)
@@ -120,9 +120,9 @@
 {
   if (!this->active_)
     { // Double checked locking
-      ACE_WRITE_GUARD (ACE_RW_Thread_Mutex,
-                       guard,
-                       this->lock_);
+      ACE_Write_Guard< ACE_RW_Thread_Mutex > guard ( this->lock_ );
+      if ( guard.locked() == 0 ) ACE_UNEXPECTED(;);
+
       if (!this->active_)
         {
           this->active_ = true;
@@ -135,9 +135,8 @@
 void 
 Stock_Database<CALLBACK>::stop (void)
 {
-  ACE_WRITE_GUARD (ACE_RW_Thread_Mutex,
-                          guard,
-                          this->lock_);
+  ACE_Write_Guard< ACE_RW_Thread_Mutex > guard ( this->lock_ );
+  if ( guard.locked() == 0 ) ACE_UNEXPECTED(;);
 
   this->active_ = false;
 }
Index: TAO/performance-tests/CSD_Strategy/TestInf/ClientTask.cpp
===================================================================
--- TAO/performance-tests/CSD_Strategy/TestInf/ClientTask.cpp	(revision 623)
+++ TAO/performance-tests/CSD_Strategy/TestInf/ClientTask.cpp	(working copy)
@@ -67,6 +67,11 @@
 
   {
     GuardType guard(this->lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(-1);
+      }
+
     this->engines_.get(engine, this->engines_.size() - 1);
     this->engines_.pop_back();
     num_loops = this->num_loops_;
@@ -77,6 +82,10 @@
     if (engine->execute(num_loops) == false)
       {
         GuardType guard(this->lock_);
+        if (! guard.locked())
+        {
+          ACE_UNEXPECTED(-1);
+        }
         this->failure_count_++;
       }
   }
@@ -86,6 +95,11 @@
       "ClientTask::svc Caught exception from execute():");
 
     GuardType guard(this->lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(-1);
+      }
+
     this->failure_count_ ++;
   }
   catch (...)
@@ -94,6 +108,11 @@
                "(%P|%t) ClientTask::svc caught unknown (...) exception "\
                "in execute() " ));
     GuardType guard(this->lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(-1);
+      }
+
     this->failure_count_++;
   }
 
Index: TAO/performance-tests/CSD_Strategy/TestInf/AppShutdown.cpp
===================================================================
--- TAO/performance-tests/CSD_Strategy/TestInf/AppShutdown.cpp	(revision 623)
+++ TAO/performance-tests/CSD_Strategy/TestInf/AppShutdown.cpp	(working copy)
@@ -56,6 +56,10 @@
 
   {
     GuardType guard(this->lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
     cur_shutdown = ++this->num_clients_shutdown_;
   }
 
Index: TAO/performance-tests/RTCorba/Thread_Pool/client.cpp
===================================================================
--- TAO/performance-tests/RTCorba/Thread_Pool/client.cpp	(revision 623)
+++ TAO/performance-tests/RTCorba/Thread_Pool/client.cpp	(working copy)
@@ -497,9 +497,10 @@
 void
 Paced_Worker::print_stats (ACE_hrtime_t test_end)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              mon,
-             this->synchronizers_.worker_lock_);
+                      this->synchronizers_.worker_lock_,
+                      ACE_UNEXPECTED(;));
 
   CORBA::ULong missed_total_deadlines =
     this->missed_start_deadlines_ + this->missed_end_deadlines_;
@@ -737,9 +738,10 @@
 Continuous_Worker::print_stats (ACE_Sample_History &history,
                                 ACE_hrtime_t test_end)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              mon,
-             this->synchronizers_.worker_lock_);
+                      this->synchronizers_.worker_lock_,
+                      ACE_UNEXPECTED(;));
 
   if (individual_continuous_worker_stats)
     {
Index: TAO/performance-tests/Callback/Callback_i.cpp
===================================================================
--- TAO/performance-tests/Callback/Callback_i.cpp	(revision 623)
+++ TAO/performance-tests/Callback/Callback_i.cpp	(working copy)
@@ -13,7 +13,7 @@
 int
 Callback_i::done (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, 0);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(0));
   return this->remaining_samples_ == 0;
 }
 
@@ -22,7 +22,7 @@
                       const Test::Payload &)
 {
   ACE_hrtime_t now = ACE_OS::gethrtime ();
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   if (this->remaining_samples_ == 0)
     return;
 
Index: TAO/performance-tests/Callback/client.cpp
===================================================================
--- TAO/performance-tests/Callback/client.cpp	(revision 623)
+++ TAO/performance-tests/Callback/client.cpp	(working copy)
@@ -237,7 +237,7 @@
 int
 Task::done (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, 1);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(1));
   return this->remaining_messages_ == 0;
 }
 
Index: TAO/performance-tests/Cubit/TAO/MT_Cubit/Task_Client.cpp
===================================================================
--- TAO/performance-tests/Cubit/TAO/MT_Cubit/Task_Client.cpp	(revision 623)
+++ TAO/performance-tests/Cubit/TAO/MT_Cubit/Task_Client.cpp	(working copy)
@@ -296,7 +296,7 @@
                      u_int thread_id,
                      u_int count)
 {
-  ACE_MT (ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->ts_->lock_));
+  ACE_MT (ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->ts_->lock_, ACE_UNEXPECTED(;)));
 
   this->ts_->latency_[thread_id] = latency;
   this->ts_->global_jitter_array_[thread_id] = jitter;
Index: TAO/tests/Stack_Recursion/Sender.cpp
===================================================================
--- TAO/tests/Stack_Recursion/Sender.cpp	(revision 623)
+++ TAO/tests/Stack_Recursion/Sender.cpp	(working copy)
@@ -18,7 +18,7 @@
 void
 Sender::dump_results (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   ACE_DEBUG ((LM_DEBUG,
               "Total messages = %d\n"
               "Total bytes = %d\n",
@@ -53,10 +53,10 @@
 CORBA::Long
 Sender::get_event_count (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                     ace_mon,
                     this->mutex_,
-                    0);
+                      ACE_UNEXPECTED(0));
   return this->message_count_;
 }
 
@@ -72,9 +72,10 @@
 {
   if (this->is_done_ == false)
     {
-      ACE_GUARD (TAO_SYNCH_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                  ace_mon,
-                 this->mutex_);
+                          this->mutex_,
+                          ACE_UNEXPECTED(;));
 
       if (this->is_done_ == false)
         this->is_done_ = true;
Index: TAO/tests/Big_Twoways/Session.cpp
===================================================================
--- TAO/tests/Big_Twoways/Session.cpp	(revision 623)
+++ TAO/tests/Big_Twoways/Session.cpp	(working copy)
@@ -95,7 +95,7 @@
     throw Test::No_Peers ();
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (this->running_)
       throw Test::Already_Running ();
 
@@ -156,8 +156,8 @@
   Test::Payload_var retval (new Test::Payload (the_payload));
 
   {
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mutex_,
-                      retval._retn ());
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_,
+                        ACE_UNEXPECTED(retval._retn ()));
     this->expected_messages_--;
 
     int verbose = 0;
Index: TAO/tests/Big_Twoways/Session_Control.cpp
===================================================================
--- TAO/tests/Big_Twoways/Session_Control.cpp	(revision 623)
+++ TAO/tests/Big_Twoways/Session_Control.cpp	(working copy)
@@ -43,7 +43,7 @@
 void
 Session_Control::session_finished (CORBA::Boolean success)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   if (this->session_count_ == 0)
     {
       ACE_ERROR ((LM_ERROR,
Index: TAO/tests/Muxing/Receiver.cpp
===================================================================
--- TAO/tests/Muxing/Receiver.cpp	(revision 623)
+++ TAO/tests/Muxing/Receiver.cpp	(working copy)
@@ -15,7 +15,7 @@
 void
 Receiver::dump_results ()
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   ACE_DEBUG ((LM_DEBUG,
               "Total messages = %d\n"
               "Total bytes = %d\n",
@@ -26,7 +26,7 @@
 void
 Receiver::receive_data (const Test::Payload &payload)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->message_count_++;
   this->byte_count_ += payload.length ();
 }
@@ -34,7 +34,7 @@
 CORBA::Long
 Receiver::get_event_count (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, 0);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(0));
   return this->message_count_;
 }
 
Index: TAO/tests/AMI_Buffering/Client_Task.cpp
===================================================================
--- TAO/tests/AMI_Buffering/Client_Task.cpp	(revision 623)
+++ TAO/tests/AMI_Buffering/Client_Task.cpp	(working copy)
@@ -15,7 +15,7 @@
 void
 Client_Task::terminate_loop (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->terminate_loop_ = 1;
 }
 
@@ -27,7 +27,7 @@
     {
       while (1)
         {
-          // run the even loop for 1 second...
+          // run the event loop for 1 second...
           ACE_Time_Value tv (1, 0);
           this->orb_->run (tv);
 
Index: TAO/tests/Bug_1020_Basic_Regression/Echo.cpp
===================================================================
--- TAO/tests/Bug_1020_Basic_Regression/Echo.cpp	(revision 623)
+++ TAO/tests/Bug_1020_Basic_Regression/Echo.cpp	(working copy)
@@ -14,9 +14,10 @@
 void
 Echo::echo_payload (Test::Payload &)
 {
-  ACE_MT (ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_MT (ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                      ace_mon,
-                     this->mutex_));
+                              this->mutex_,
+                              ACE_UNEXPECTED(;)));
 
   this->abort_counter_--;
 
Index: TAO/tests/LongUpcalls/AMI_Manager.cpp
===================================================================
--- TAO/tests/LongUpcalls/AMI_Manager.cpp	(revision 623)
+++ TAO/tests/LongUpcalls/AMI_Manager.cpp	(working copy)
@@ -143,7 +143,7 @@
 void
 Controller_Handler::worker_started (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, *this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, *this->mutex_, ACE_UNEXPECTED(;));
   (*this->pending_replies_)--;
 }
 
@@ -165,7 +165,7 @@
 void
 Controller_Handler::worker_finished (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, *this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, *this->mutex_, ACE_UNEXPECTED(;));
   (*this->pending_replies_)--;
 }
 
Index: TAO/tests/LongUpcalls/Controller.cpp
===================================================================
--- TAO/tests/LongUpcalls/Controller.cpp	(revision 623)
+++ TAO/tests/LongUpcalls/Controller.cpp	(working copy)
@@ -26,7 +26,7 @@
 void
 Controller::worker_started (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->start_count_++;
 
   // ACE_DEBUG ((LM_DEBUG, "Received worker start report\n"));
@@ -35,7 +35,7 @@
 void
 Controller::worker_finished (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->finish_count_++;
 
   // ACE_DEBUG ((LM_DEBUG, "Received worker completion report\n"));
Index: TAO/tests/Bug_1482_Regression/Hello.cpp
===================================================================
--- TAO/tests/Bug_1482_Regression/Hello.cpp	(revision 623)
+++ TAO/tests/Bug_1482_Regression/Hello.cpp	(working copy)
@@ -20,10 +20,10 @@
 CORBA::ULong
 Hello::next_prime (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                     ace_mon,
                     this->mutex_,
-                    this->latest_prime_);
+                      ACE_UNEXPECTED(this->latest_prime_));
 
   if (this->latest_prime_ > 10000)
     ACE_OS::abort ();
Index: TAO/tests/CSD_Strategy_Tests/TP_Common/ClientTask.cpp
===================================================================
--- TAO/tests/CSD_Strategy_Tests/TP_Common/ClientTask.cpp	(revision 623)
+++ TAO/tests/CSD_Strategy_Tests/TP_Common/ClientTask.cpp	(working copy)
@@ -59,6 +59,11 @@
 
   {
     GuardType guard(this->lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(-1);
+      }
+
     this->engines_.get(engine, this->engines_.size() - 1);
     this->engines_.pop_back();
   }
@@ -69,6 +74,11 @@
     if (exec_ret == false)
       {
         GuardType guard(this->lock_);
+        if (! guard.locked())
+          {
+            ACE_UNEXPECTED(-1);
+          }
+
         this->failure_count_ ++;
       }
   }
@@ -78,6 +88,11 @@
       "ClientTask::svc Caught exception from execute():");
 
     GuardType guard(this->lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(-1);
+      }
+
     this->failure_count_ ++;
   }
   catch (...)
@@ -86,6 +101,11 @@
                "(%P|%t) ClientTask::svc caught unknown (...) exception "\
                "in execute() " ));
     GuardType guard(this->lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(-1);
+      }
+
     this->failure_count_ ++;
   }
 
Index: TAO/tests/CSD_Strategy_Tests/TP_Common/AppShutdown.cpp
===================================================================
--- TAO/tests/CSD_Strategy_Tests/TP_Common/AppShutdown.cpp	(revision 623)
+++ TAO/tests/CSD_Strategy_Tests/TP_Common/AppShutdown.cpp	(working copy)
@@ -57,6 +57,11 @@
 
   {
     GuardType guard(this->lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
+
     cur_shutdown = ++this->num_clients_shutdown_;
   }
 
Index: TAO/tests/MT_BiDir/Receiver_i.cpp
===================================================================
--- TAO/tests/MT_BiDir/Receiver_i.cpp	(revision 623)
+++ TAO/tests/MT_BiDir/Receiver_i.cpp	(working copy)
@@ -17,7 +17,7 @@
 void
 Receiver_i::receive_payload (const Receiver::Payload &payload)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->message_count_++;
   this->byte_count_ += payload.length ();
 }
@@ -25,6 +25,6 @@
 CORBA::Long
 Receiver_i::get_event_count (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, 0);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(0));
   return this->message_count_;
 }
Index: TAO/tests/Client_Leaks/Startup_Callback.cpp
===================================================================
--- TAO/tests/Client_Leaks/Startup_Callback.cpp	(revision 623)
+++ TAO/tests/Client_Leaks/Startup_Callback.cpp	(working copy)
@@ -12,7 +12,7 @@
 int
 Startup_Callback::process_has_started (Test::Process_out the_process)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, 0);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(0));
   if (!CORBA::is_nil (this->process_.in ()))
     {
       the_process = Test::Process::_duplicate (this->process_.in ());
@@ -24,6 +24,6 @@
 void
 Startup_Callback::started (Test::Process_ptr process)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->process_ = Test::Process::_duplicate (process);
 }
Index: TAO/tests/Muxed_GIOP_Versions/server.cpp
===================================================================
--- TAO/tests/Muxed_GIOP_Versions/server.cpp	(revision 623)
+++ TAO/tests/Muxed_GIOP_Versions/server.cpp	(working copy)
@@ -246,7 +246,7 @@
   // Ping the object 100 times, ignoring all exceptions.
   // It would be better to use validate_connection() but the test must
   // run on minimum CORBA builds too!
-  ACE_GUARD (TAO_SYNCH_MUTEX, guard, mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, guard, mutex_, ACE_UNEXPECTED(;));
 
   for (int j = 0; j != 100; ++j)
     {
Index: TAO/tests/Big_Oneways/Session.cpp
===================================================================
--- TAO/tests/Big_Oneways/Session.cpp	(revision 623)
+++ TAO/tests/Big_Oneways/Session.cpp	(working copy)
@@ -119,7 +119,7 @@
     throw Test::No_Peers ();
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (this->running_)
       throw Test::Already_Running ();
 
@@ -183,7 +183,7 @@
     }
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     --this->expected_messages_;
 
 #if 0
Index: TAO/tests/Big_Oneways/Session_Control.cpp
===================================================================
--- TAO/tests/Big_Oneways/Session_Control.cpp	(revision 623)
+++ TAO/tests/Big_Oneways/Session_Control.cpp	(working copy)
@@ -43,7 +43,7 @@
 void
 Session_Control::session_finished (CORBA::Boolean success)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   if (this->session_count_ == 0)
     {
       ACE_ERROR ((LM_ERROR,
Index: TAO/tao/GUIResource_Factory.cpp
===================================================================
--- TAO/tao/GUIResource_Factory.cpp	(revision 623)
+++ TAO/tao/GUIResource_Factory.cpp	(working copy)
@@ -49,7 +49,7 @@
   void
   GUIResource_Factory::reclaim_reactor (ACE_Reactor *reactor)
   {
-    ACE_GUARD ( TAO_SYNCH_MUTEX, ace_mon, this->lock_ );
+    ACE_GUARD_REACTION ( TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
     if (this->dynamically_allocated_reactor_)
       delete reactor;
Index: TAO/tao/Thread_Lane_Resources.cpp
===================================================================
--- TAO/tao/Thread_Lane_Resources.cpp	(revision 623)
+++ TAO/tao/Thread_Lane_Resources.cpp	(working copy)
@@ -86,10 +86,10 @@
     {
       // @todo: Wouldnt this crash big time if you happen to
       // dereference a null-pointer? Needs fixing.
-      ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                         ace_mon,
                         this->lock_,
-                        *this->acceptor_registry_);
+                          ACE_UNEXPECTED(*this->acceptor_registry_));
 
       if (this->acceptor_registry_ == 0)
         {
@@ -159,10 +159,10 @@
   // Double check.
   if (this->leader_follower_ == 0)
     {
-      ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                         ace_mon,
                         this->lock_,
-                        *this->leader_follower_);
+                          ACE_UNEXPECTED(*this->leader_follower_));
 
       if (this->leader_follower_ == 0)
         {
@@ -488,9 +488,10 @@
 {
   TAO_Leader_Follower &leader_follower = this->leader_follower ();
 
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              ace_mon,
-             leader_follower.lock ());
+                      leader_follower.lock (),
+                      ACE_UNEXPECTED(;));
 
   ACE_Reactor *reactor = leader_follower.reactor ();
 
Index: TAO/tao/ORB_Core.cpp
===================================================================
--- TAO/tao/ORB_Core.cpp	(revision 623)
+++ TAO/tao/ORB_Core.cpp	(working copy)
@@ -2011,10 +2011,10 @@
   {
     // Lock the ORB_Table against concurrent modification while we
     // iterate through the ORBs.
-    ACE_MT (ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+    ACE_MT (ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                               guard,
                               TAO::ORB_Table::instance()->lock (),
-                              0));
+                                ACE_UNEXPECTED(0)));
 
     TAO::ORB_Table * const table = TAO::ORB_Table::instance ();
     TAO::ORB_Table::iterator const end = table->end ();
@@ -2218,7 +2218,7 @@
 TAO_ORB_Core::shutdown (CORBA::Boolean wait_for_completion)
 {
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     if (this->has_shutdown () == true)
       return;
Index: TAO/tao/AnyTypeCode/Recursive_Type_TypeCode.cpp
===================================================================
--- TAO/tao/AnyTypeCode/Recursive_Type_TypeCode.cpp	(revision 623)
+++ TAO/tao/AnyTypeCode/Recursive_Type_TypeCode.cpp	(working copy)
@@ -59,10 +59,10 @@
                               MemberArrayType>::equal_i (
   CORBA::TypeCode_ptr tc) const
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_RECURSIVE_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_RECURSIVE_MUTEX,
                     guard,
                     this->lock_,
-                    false);
+                      ACE_UNEXPECTED(false));
 
   // Top-level TypeCode case.
   if (!(this->in_recursion_))
@@ -89,10 +89,10 @@
                               MemberArrayType>::equivalent_i (
   CORBA::TypeCode_ptr tc) const
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_RECURSIVE_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_RECURSIVE_MUTEX,
                     guard,
                     this->lock_,
-                    false);
+                      ACE_UNEXPECTED(false));
 
   // Top-level TypeCode case.
   if (!(this->in_recursion_))
Index: TAO/tao/AnyTypeCode/NVList.cpp
===================================================================
--- TAO/tao/AnyTypeCode/NVList.cpp	(revision 623)
+++ TAO/tao/AnyTypeCode/NVList.cpp	(working copy)
@@ -276,7 +276,7 @@
       return;
     }
 
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->incoming_ != 0)
     {
@@ -291,9 +291,10 @@
 void
 CORBA::NVList::_tao_encode (TAO_OutputCDR &cdr, int flag)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              ace_mon,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
 
   if (this->incoming_ != 0)
     {
@@ -437,7 +438,7 @@
 void
 CORBA::NVList::evaluate (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (this->incoming_ == 0)
     {
Index: TAO/tao/Asynch_Reply_Dispatcher_Base.cpp
===================================================================
--- TAO/tao/Asynch_Reply_Dispatcher_Base.cpp	(revision 623)
+++ TAO/tao/Asynch_Reply_Dispatcher_Base.cpp	(working copy)
@@ -74,10 +74,10 @@
     }
   else
     {
-      ACE_GUARD_RETURN (ACE_Lock,
+      ACE_GUARD_REACTION (ACE_Lock,
                         mutex,
                         *this->lock_,
-                        false);
+                          ACE_UNEXPECTED(false));
 
       if (!this->is_reply_dispatched_)
         {
Index: TAO/tao/Muxed_TMS.cpp
===================================================================
--- TAO/tao/Muxed_TMS.cpp	(revision 623)
+++ TAO/tao/Muxed_TMS.cpp	(working copy)
@@ -35,10 +35,10 @@
 TAO_Muxed_TMS::request_id (void)
 {
   // @@ What is a good error return value?
-  ACE_GUARD_RETURN (ACE_Lock,
+  ACE_GUARD_REACTION (ACE_Lock,
                     ace_mon,
                     *this->lock_,
-                    0);
+                      ACE_UNEXPECTED(0));
 
   ++this->request_id_generator_;
 
@@ -244,9 +244,10 @@
 void
 TAO_Muxed_TMS::connection_closed (void)
 {
-  ACE_GUARD (ACE_Lock,
+  ACE_GUARD_REACTION (ACE_Lock,
              ace_mon,
-             *this->lock_);
+                      *this->lock_,
+                      ACE_UNEXPECTED(;));
 
   int retval = 0;
   do
Index: TAO/tao/PortableServer/Active_Object_Map.cpp
===================================================================
--- TAO/tao/PortableServer/Active_Object_Map.cpp	(revision 623)
+++ TAO/tao/PortableServer/Active_Object_Map.cpp	(working copy)
@@ -682,7 +682,6 @@
                 this->active_object_map_->user_id_map_->current_size ());
 #endif /* TAO_HAS_MONITOR_POINTS==1 */
     }
-
   return result;
 }
 
Index: TAO/tao/PortableServer/Object_Adapter.cpp
===================================================================
--- TAO/tao/PortableServer/Object_Adapter.cpp	(revision 623)
+++ TAO/tao/PortableServer/Object_Adapter.cpp	(working copy)
@@ -676,7 +676,7 @@
   TAO_POAManager_Factory* factory = 0;
 #endif
   {
-    ACE_GUARD (ACE_Lock, ace_mon, this->lock ());
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, this->lock (), ACE_UNEXPECTED(;));
     if (this->root_ == 0)
       return;
     root = this->root_;
Index: TAO/tao/Stub.cpp
===================================================================
--- TAO/tao/Stub.cpp	(revision 623)
+++ TAO/tao/Stub.cpp	(working copy)
@@ -117,9 +117,10 @@
 {
   // we assume that the profile_in_use_ is being
   // forwarded!  Grab the lock so things don't change.
-  ACE_MT (ACE_GUARD (ACE_Lock,
+  ACE_MT (ACE_GUARD_REACTION (ACE_Lock,
                      guard,
-                     *this->profile_lock_ptr_));
+                              *this->profile_lock_ptr_,
+                              ACE_UNEXPECTED(;)));
 
   if (permanent_forward)
     {
Index: TAO/tao/IORTable/IOR_Table_Impl.cpp
===================================================================
--- TAO/tao/IORTable/IOR_Table_Impl.cpp	(revision 623)
+++ TAO/tao/IORTable/IOR_Table_Impl.cpp	(working copy)
@@ -75,7 +75,7 @@
 void
 TAO_IOR_Table_Impl::bind (const char * object_key, const char * IOR)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   if (this->map_.bind (object_key, IOR) != 0)
     throw IORTable::AlreadyBound ();
 }
@@ -83,14 +83,14 @@
 void
 TAO_IOR_Table_Impl::rebind (const char * object_key, const char * IOR)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   this->map_.rebind (object_key, IOR);
 }
 
 void
 TAO_IOR_Table_Impl::unbind (const char * object_key)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   if (this->map_.unbind (object_key) != 0)
     throw IORTable::NotFound ();
 }
@@ -98,7 +98,7 @@
 void
 TAO_IOR_Table_Impl::set_locator (IORTable::Locator_ptr locator)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
   this->locator_ = IORTable::Locator::_duplicate (locator);
 }
 
Index: TAO/tao/DynamicInterface/Request.inl
===================================================================
--- TAO/tao/DynamicInterface/Request.inl	(revision 623)
+++ TAO/tao/DynamicInterface/Request.inl	(working copy)
@@ -185,10 +185,10 @@
 ACE_INLINE CORBA::Boolean
 CORBA::Request::response_received (void)
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                     ace_mon,
                     this->lock_,
-                    false);
+                      ACE_UNEXPECTED(false));
 
   return this->response_received_;
 }
Index: TAO/tao/DynamicInterface/Request.cpp
===================================================================
--- TAO/tao/DynamicInterface/Request.cpp	(revision 623)
+++ TAO/tao/DynamicInterface/Request.cpp	(working copy)
@@ -192,9 +192,10 @@
 CORBA::Request::send_deferred (void)
 {
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX,
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                ace_mon,
-               this->lock_);
+                        this->lock_,
+                        ACE_UNEXPECTED(;));
 
     this->response_received_ = false;
   }
@@ -310,10 +311,10 @@
   CORBA::Boolean response_received = false;
 
   {
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                       ace_mon,
                       this->lock_,
-                      false);
+                        ACE_UNEXPECTED(false));
     response_received = this->response_received_;
   }
 
@@ -326,10 +327,10 @@
       (void) this->orb_->perform_work (&tv);
 
       {
-        ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+        ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                           ace_mon,
                           this->lock_,
-                          false);
+                            ACE_UNEXPECTED(false));
         response_received = this->response_received_;
       }
     }
@@ -361,9 +362,10 @@
                                       this->lazy_evaluation_);
 
       {
-        ACE_GUARD (TAO_SYNCH_MUTEX,
+        ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                    ace_mon,
-                   this->lock_);
+                            this->lock_,
+                            ACE_UNEXPECTED(;));
 
         this->response_received_ = true;
       }
Index: TAO/tao/ORB_Table.cpp
===================================================================
--- TAO/tao/ORB_Table.cpp	(revision 623)
+++ TAO/tao/ORB_Table.cpp	(working copy)
@@ -129,9 +129,10 @@
 void
 TAO::ORB_Table::set_default (char const * orb_id)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              guard,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
 
   iterator const i = this->table_.find (key_type (orb_id));
 
@@ -146,9 +147,10 @@
   //     generalizing it. It works if the first ORB that is registered
   //     decides to not want be the default ORB. Should generalize it
   //     to handle all cases.
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              guard,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
 
   // Check if there is a default ORB already and if it is *not* the
   // same as the orb_id thats passed in.  We don't have to do
Index: TAO/tao/LF_Event.cpp
===================================================================
--- TAO/tao/LF_Event.cpp	(revision 623)
+++ TAO/tao/LF_Event.cpp	(working copy)
@@ -28,7 +28,7 @@
 void
 TAO_LF_Event::state_changed (int new_state, TAO_Leader_Follower &lf)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, lf.lock ());
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, lf.lock (), ACE_UNEXPECTED(;));
 
   if (this->is_state_final () == 0)
     {
Index: TAO/tao/Stub.inl
===================================================================
--- TAO/tao/Stub.inl	(revision 623)
+++ TAO/tao/Stub.inl	(working copy)
@@ -53,9 +53,10 @@
 ACE_INLINE void
 TAO_Stub::reset_profiles (void)
 {
-  ACE_MT (ACE_GUARD (ACE_Lock,
+  ACE_MT (ACE_GUARD_REACTION (ACE_Lock,
                      guard,
-                     *this->profile_lock_ptr_));
+                              *this->profile_lock_ptr_,
+                              ACE_UNEXPECTED(;)));
   this->reset_profiles_i ();
 }
 
@@ -204,10 +205,10 @@
 ACE_INLINE CORBA::Boolean
 TAO_Stub::next_profile_retry (void)
 {
-  ACE_MT (ACE_GUARD_RETURN (ACE_Lock,
+  ACE_MT (ACE_GUARD_REACTION (ACE_Lock,
                             guard,
                             *this->profile_lock_ptr_,
-                            0));
+                              ACE_UNEXPECTED(0)));
 
   if (this->profile_success_ && this->forward_profiles_)
     {
Index: TAO/tao/ObjectKey_Table.cpp
===================================================================
--- TAO/tao/ObjectKey_Table.cpp	(revision 623)
+++ TAO/tao/ObjectKey_Table.cpp	(working copy)
@@ -70,7 +70,7 @@
     ACE_GUARD_RETURN (ACE_Lock,
                       ace_mon,
                       *this->lock_,
-                      0);
+                      -1);
 
     // This is a tradeoff.. We could avoid this two stage process of
     // using a find () and then a bind () , which would make things
@@ -99,7 +99,7 @@
   ACE_GUARD_RETURN (ACE_Lock,
                     ace_mon,
                     *this->lock_,
-                    0);
+                    -1);
 
   // If the refcount has dropped to 1, just go ahead and unbind it
   // from the table.
@@ -119,7 +119,7 @@
       ACE_GUARD_RETURN (ACE_Lock,
                         ace_mon,
                         *this->lock_,
-                        0);
+                        -1);
 
       TABLE::ITERATOR end_iter = this->table_.end ();
       TABLE::ITERATOR start;
Index: TAO/tao/Messaging/AMH_Response_Handler.cpp
===================================================================
--- TAO/tao/Messaging/AMH_Response_Handler.cpp	(revision 623)
+++ TAO/tao/Messaging/AMH_Response_Handler.cpp	(working copy)
@@ -36,7 +36,7 @@
   // Since we are destroying the object we put a huge lock around the
   // whole destruction process (just paranoid).
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
 
     if (this->response_expected_ == 0) //oneway ?
       {
@@ -89,7 +89,7 @@
 TAO_AMH_Response_Handler::_tao_rh_init_reply (void)
 {
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (this->rh_reply_status_ != TAO_RS_UNINITIALIZED)
       {
         // Looks like someone is trying to call an AMH method
@@ -115,7 +115,7 @@
   reply_params.reply_status (this->reply_status_);
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
 
     this->mesg_base_->generate_reply_header (this->_tao_out, reply_params);
 
@@ -129,7 +129,7 @@
 TAO_AMH_Response_Handler::_tao_rh_send_reply (void)
 {
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
 
     // If the reply has not been initialised, raise an exception to the
     // server-app saying it is not doing something right.
@@ -164,7 +164,7 @@
     }
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     this->rh_reply_status_ = TAO_RS_SENT;
   }
 }
@@ -173,7 +173,7 @@
 TAO_AMH_Response_Handler::_tao_rh_send_exception (const CORBA::Exception &ex)
 {
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     if (this->rh_reply_status_ != TAO_RS_UNINITIALIZED)
       {
         throw ::CORBA::BAD_INV_ORDER (
@@ -222,7 +222,7 @@
     }
 
   {
-    ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
     this->rh_reply_status_ = TAO_RS_SENT;
   }
 }
Index: TAO/tao/Strategies/SCIOP_Endpoint.cpp
===================================================================
--- TAO/tao/Strategies/SCIOP_Endpoint.cpp	(revision 623)
+++ TAO/tao/Strategies/SCIOP_Endpoint.cpp	(working copy)
@@ -197,10 +197,10 @@
     return this->hash_val_;
 
   {
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                       guard,
                       this->addr_lookup_lock_,
-                      this->hash_val_);
+                        ACE_UNEXPECTED(this->hash_val_));
     // .. DCL
     if (this->hash_val_ != 0)
       return this->hash_val_;
@@ -233,10 +233,10 @@
   // Double checked locking optimization.
   if (!this->object_addr_set_)
     {
-      ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                         guard,
                         this->addr_lookup_lock_,
-                        this->object_addr_);
+                          ACE_UNEXPECTED(this->object_addr_));
 
       if (!this->object_addr_set_)
         {
Index: TAO/tao/Strategies/SHMIOP_Endpoint.cpp
===================================================================
--- TAO/tao/Strategies/SHMIOP_Endpoint.cpp	(revision 623)
+++ TAO/tao/Strategies/SHMIOP_Endpoint.cpp	(working copy)
@@ -199,10 +199,10 @@
     return this->hash_val_;
 
   {
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                       guard,
                       this->addr_lookup_lock_,
-                      this->hash_val_);
+                        ACE_UNEXPECTED(this->hash_val_));
     // .. DCL
     if (this->hash_val_ != 0)
       return this->hash_val_;
@@ -231,10 +231,10 @@
       TAO_SHMIOP_Endpoint *endpoint =
         const_cast <TAO_SHMIOP_Endpoint *>(this);
 
-      ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                         guard,
                         endpoint->addr_lookup_lock_,
-                        this->object_addr_ );
+                          ACE_UNEXPECTED(this->object_addr_) );
 
       if (!this->object_addr_set_)
         {
Index: TAO/tao/Strategies/DIOP_Endpoint.cpp
===================================================================
--- TAO/tao/Strategies/DIOP_Endpoint.cpp	(revision 623)
+++ TAO/tao/Strategies/DIOP_Endpoint.cpp	(working copy)
@@ -225,10 +225,10 @@
     return this->hash_val_;
 
   {
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                       guard,
                       this->addr_lookup_lock_,
-                      this->hash_val_);
+                        ACE_UNEXPECTED(this->hash_val_));
     // .. DCL
     if (this->hash_val_ != 0)
       return this->hash_val_;
@@ -252,10 +252,10 @@
   // Double checked locking optimization.
   if (!this->object_addr_set_)
     {
-      ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                         guard,
                         this->addr_lookup_lock_,
-                        this->object_addr_ );
+                          ACE_UNEXPECTED(this->object_addr_) );
 
       if (!this->object_addr_set_)
         {
Index: TAO/tao/Strategies/UIOP_Endpoint.cpp
===================================================================
--- TAO/tao/Strategies/UIOP_Endpoint.cpp	(revision 623)
+++ TAO/tao/Strategies/UIOP_Endpoint.cpp	(working copy)
@@ -89,10 +89,10 @@
     return this->hash_val_;
 
   {
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                       guard,
                       this->addr_lookup_lock_,
-                      this->hash_val_);
+                        ACE_UNEXPECTED(this->hash_val_));
     // .. DCL
     if (this->hash_val_ != 0)
       return this->hash_val_;
Index: TAO/tao/Transport.cpp
===================================================================
--- TAO/tao/Transport.cpp	(revision 623)
+++ TAO/tao/Transport.cpp	(working copy)
@@ -375,10 +375,10 @@
 
   // @@note: This should be okay since the register handler call will
   // not make a nested call into the transport.
-  ACE_GUARD_RETURN (ACE_Lock,
+  ACE_GUARD_REACTION (ACE_Lock,
                     ace_mon,
                     *this->handler_lock_,
-                    false);
+                      ACE_UNEXPECTED(false));
 
   if (r == this->event_handler_i ()->reactor ())
     {
@@ -1264,7 +1264,7 @@
 TAO_Transport::send_connection_closed_notifications (void)
 {
   {
-    ACE_MT (ACE_GUARD (ACE_Lock, guard, *this->handler_lock_));
+    ACE_MT (ACE_GUARD_REACTION (ACE_Lock, guard, *this->handler_lock_, ACE_UNEXPECTED(;)));
 
     this->send_connection_closed_notifications_i ();
   }
Index: TAO/tao/PI/ORBInitializer_Registry_Impl.cpp
===================================================================
--- TAO/tao/PI/ORBInitializer_Registry_Impl.cpp	(revision 623)
+++ TAO/tao/PI/ORBInitializer_Registry_Impl.cpp	(working copy)
@@ -101,9 +101,10 @@
 {
   if (!CORBA::is_nil (init))
     {
-      ACE_GUARD (TAO_SYNCH_RECURSIVE_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_RECURSIVE_MUTEX,
                  guard,
-                 this->lock_);
+                          this->lock_,
+                          ACE_UNEXPECTED(;));
 
       // Increase the length of the ORBInitializer array by one.
       size_t const cur_len = this->initializers_.size ();
@@ -187,9 +188,10 @@
 {
   if (pre_init_count > 0)
     {
-      ACE_GUARD (TAO_SYNCH_RECURSIVE_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_RECURSIVE_MUTEX,
                  guard,
-                 this->lock_);
+                          this->lock_,
+                          ACE_UNEXPECTED(;));
 
       TAO_ORBInitInfo * orb_init_info_temp = 0;
 
Index: TAO/tao/LF_Strategy_Complete.cpp
===================================================================
--- TAO/tao/LF_Strategy_Complete.cpp	(revision 623)
+++ TAO/tao/LF_Strategy_Complete.cpp	(working copy)
@@ -35,7 +35,7 @@
 TAO_LF_Strategy_Complete::reset_event_loop_thread (int call_reset,
                                                    TAO_Leader_Follower &lf)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, lf.lock ());
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, lf.lock (), ACE_UNEXPECTED(;));
 
   if (call_reset)
     lf.reset_event_loop_thread ();
Index: TAO/tao/Transport_Cache_Manager_T.cpp
===================================================================
--- TAO/tao/Transport_Cache_Manager_T.cpp	(revision 623)
+++ TAO/tao/Transport_Cache_Manager_T.cpp	(working copy)
@@ -599,7 +599,7 @@
     transport_set_type transports_to_be_closed;
 
     {
-      ACE_MT (ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->cache_lock_, 0));
+      ACE_MT (ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->cache_lock_, -1));
 
       DESCRIPTOR_SET sorted_set = 0;
       int const sorted_size = this->fill_set_i (sorted_set);
Index: TAO/tao/Object.cpp
===================================================================
--- TAO/tao/Object.cpp	(revision 623)
+++ TAO/tao/Object.cpp	(working copy)
@@ -97,7 +97,7 @@
 #define TAO_OBJECT_IOR_EVALUATE \
 if (!this->is_evaluated_) \
   { \
-    ACE_GUARD (ACE_Lock , mon, *this->object_init_lock_); \
+    ACE_GUARD_REACTION (ACE_Lock , mon, *this->object_init_lock_, ACE_UNEXPECTED(;)); \
       if (!this->is_evaluated_) \
         CORBA::Object::tao_object_initialize (this); \
   }
@@ -105,7 +105,7 @@
 #define TAO_OBJECT_IOR_EVALUATE_RETURN \
 if (!this->is_evaluated_) \
   { \
-    ACE_GUARD_RETURN (ACE_Lock , mon, *this->object_init_lock_, 0); \
+    ACE_GUARD_REACTION (ACE_Lock , mon, *this->object_init_lock_, ACE_UNEXPECTED(0)); \
     if (!this->is_evaluated_) \
       CORBA::Object::tao_object_initialize (this); \
   }
Index: TAO/tao/CSD_ThreadPool/CSD_TP_Task.cpp
===================================================================
--- TAO/tao/CSD_ThreadPool/CSD_TP_Task.cpp	(revision 623)
+++ TAO/tao/CSD_ThreadPool/CSD_TP_Task.cpp	(working copy)
@@ -24,6 +24,10 @@
 TAO::CSD::TP_Task::add_request(TP_Request* request)
 {
   GuardType guard(this->lock_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(false);
+    }
 
   if (!this->accepting_requests_)
     {
@@ -92,6 +96,10 @@
 
   // We need the lock acquired from here on out.
   GuardType guard(this->lock_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   // We can assume that we are in the proper state to handle this open()
   // call as long as we haven't been open()'ed before.
@@ -140,6 +148,11 @@
   // execution of this svc() method.
   {
     GuardType guard(this->lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(0);
+      }
+
     // Put the thread id into a collection which is used to check whether
     // the orb shutdown is called by one of the threads in the pool.
     ACE_thread_t thr_id = ACE_OS::thr_self ();
@@ -161,6 +174,10 @@
       {
         // Acquire the lock until just before we decide to "PerformWork".
         GuardType guard(this->lock_);
+        if (! guard.locked())
+          {
+            ACE_UNEXPECTED(0);
+          }
 
         // Start the "GetWork" loop.
         while (request.is_nil())
@@ -220,6 +237,10 @@
       // queue now for this not-busy servant.  We need the lock_ to do this.
       {
         GuardType guard(this->lock_);
+        if (! guard.locked())
+          {
+            ACE_UNEXPECTED(0);
+          }
         request->mark_as_ready();
         this->work_available_.signal();
       }
@@ -240,6 +261,10 @@
 TAO::CSD::TP_Task::close(u_long flag)
 {
   GuardType guard(this->lock_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   if (flag == 0)
     {
@@ -308,6 +333,10 @@
 TAO::CSD::TP_Task::cancel_servant (PortableServer::Servant servant)
 {
   GuardType guard(this->lock_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   // Cancel the requests targeted for the provided servant.
   TP_Cancel_Visitor cancel_visitor(servant);
Index: TAO/tao/CSD_ThreadPool/CSD_TP_Synch_Helper.inl
===================================================================
--- TAO/tao/CSD_ThreadPool/CSD_TP_Synch_Helper.inl	(revision 623)
+++ TAO/tao/CSD_ThreadPool/CSD_TP_Synch_Helper.inl	(working copy)
@@ -23,6 +23,10 @@
 TAO::CSD::TP_Synch_Helper::wait_while_pending()
 {
   GuardType guard(this->lock_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(false);
+    }
 
   while (this->state_ == PENDING)
     {
@@ -38,6 +42,11 @@
 TAO::CSD::TP_Synch_Helper::dispatched()
 {
   GuardType guard(this->lock_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   this->state_ = DISPATCHED;
   this->condition_.signal();
 }
@@ -48,6 +57,11 @@
 TAO::CSD::TP_Synch_Helper::cancelled()
 {
   GuardType guard(this->lock_);
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   this->state_ = CANCELLED;
   this->condition_.signal();
 }
Index: TAO/tao/ORBInitializer_Registry.cpp
===================================================================
--- TAO/tao/ORBInitializer_Registry.cpp	(revision 623)
+++ TAO/tao/ORBInitializer_Registry.cpp	(working copy)
@@ -30,9 +30,10 @@
       // Using ACE_Static_Object_Lock::instance() precludes
       // register_orb_initializer() from being called within a static
       // object CTOR.
-      ACE_MT (ACE_GUARD (TAO_SYNCH_RECURSIVE_MUTEX,
+      ACE_MT (ACE_GUARD_REACTION (TAO_SYNCH_RECURSIVE_MUTEX,
                          guard,
-                         *ACE_Static_Object_Lock::instance ()));
+                                  *ACE_Static_Object_Lock::instance (),
+                                  ACE_UNEXPECTED(;)));
 
       // Make sure TAO's singleton manager is initialized.
       if (TAO_Singleton_Manager::instance ()->init () == -1)
Index: TAO/tao/Leader_Follower.inl
===================================================================
--- TAO/tao/Leader_Follower.inl	(revision 623)
+++ TAO/tao/Leader_Follower.inl	(working copy)
@@ -134,7 +134,7 @@
 
   if (tss->event_loop_thread_ > 0)
     {
-      ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->lock ());
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->lock (), ACE_UNEXPECTED(;));
       this->reset_event_loop_thread_i (tss);
 
       this->elect_new_leader ();
Index: TAO/tao/IIOP_Endpoint.cpp
===================================================================
--- TAO/tao/IIOP_Endpoint.cpp	(revision 623)
+++ TAO/tao/IIOP_Endpoint.cpp	(working copy)
@@ -338,10 +338,10 @@
   // Double checked locking optimization.
   if (!this->object_addr_set_)
     {
-      ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+      ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                         guard,
                         this->addr_lookup_lock_,
-                        this->object_addr_);
+                          ACE_UNEXPECTED(this->object_addr_));
 
       if (!this->object_addr_set_)
         {
@@ -568,10 +568,10 @@
     return this->hash_val_;
 
   {
-    ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+    ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                       guard,
                       this->addr_lookup_lock_,
-                      this->hash_val_);
+                        ACE_UNEXPECTED(this->hash_val_));
     // .. DCL
     if (this->hash_val_ != 0)
       return this->hash_val_;
Index: TAO/tao/Transport.inl
===================================================================
--- TAO/tao/Transport.inl	(revision 623)
+++ TAO/tao/Transport.inl	(working copy)
@@ -105,10 +105,10 @@
 ACE_INLINE bool
 TAO_Transport::queue_is_empty (void)
 {
-  ACE_GUARD_RETURN (ACE_Lock,
+  ACE_GUARD_REACTION (ACE_Lock,
                     ace_mon,
                     *this->handler_lock_,
-                    false);
+                      ACE_UNEXPECTED(false));
   return this->queue_is_empty_i ();
 }
 
@@ -183,7 +183,11 @@
 ACE_INLINE bool
 TAO_Transport::is_connected (void) const
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->handler_lock_, false);
+  ACE_GUARD_REACTION (ACE_Lock,
+                      ace_mon,
+                      *this->handler_lock_,
+                      ACE_UNEXPECTED(false));
+
   return this->is_connected_;
 }
 
Index: TAO/tao/Object_Ref_Table.inl
===================================================================
--- TAO/tao/Object_Ref_Table.inl	(revision 623)
+++ TAO/tao/Object_Ref_Table.inl	(working copy)
@@ -28,9 +28,10 @@
 {
   Table tmp;
 
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              guard,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
 
   this->table_.swap (tmp);  // Force release of memory held by our table.
 }
Index: TAO/tao/Utils/Synch_Refcountable.inl
===================================================================
--- TAO/tao/Utils/Synch_Refcountable.inl	(revision 623)
+++ TAO/tao/Utils/Synch_Refcountable.inl	(working copy)
@@ -9,14 +9,14 @@
 ACE_INLINE int
 TAO_Synch_Refcountable::increment (void)
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->refcount_lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->refcount_lock_, ACE_UNEXPECTED(0));
   return ACE_Refcountable::increment ();
 }
 
 ACE_INLINE int
 TAO_Synch_Refcountable::decrement (void)
 {
-  ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->refcount_lock_, 0);
+  ACE_GUARD_REACTION (ACE_Lock, ace_mon, *this->refcount_lock_, ACE_UNEXPECTED(0));
   return ACE_Refcountable::decrement ();
 }
 
Index: TAO/tao/Policy_Manager.cpp
===================================================================
--- TAO/tao/Policy_Manager.cpp	(revision 623)
+++ TAO/tao/Policy_Manager.cpp	(working copy)
@@ -26,7 +26,7 @@
 TAO_Policy_Manager::set_policy_overrides (const CORBA::PolicyList & policies,
                                           CORBA::SetOverrideType set_add)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX, ace_mon, this->mutex_);
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;));
   this->impl_.set_policy_overrides (policies, set_add);
 }
 
Index: TAO/tao/RTCORBA/Thread_Pool.cpp
===================================================================
--- TAO/tao/RTCORBA/Thread_Pool.cpp	(revision 623)
+++ TAO/tao/RTCORBA/Thread_Pool.cpp	(working copy)
@@ -229,9 +229,10 @@
 void
 TAO_Thread_Lane::shutting_down (void)
 {
-  ACE_GUARD (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
              mon,
-             this->lock_);
+                      this->lock_,
+                      ACE_UNEXPECTED(;));
 
   // We are shutting down, this way we are not creating any more new dynamic
   // threads
@@ -390,10 +391,10 @@
 CORBA::ULong
 TAO_Thread_Lane::current_threads (void) const
 {
-  ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                     mon,
                     this->lock_,
-                    0);
+                      ACE_UNEXPECTED(0));
 
   return (this->static_threads_.thr_count () +
          this->dynamic_threads_.thr_count ());
@@ -406,7 +407,7 @@
   ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
                     mon,
                     this->lock_,
-                    0);
+                    -1);
 
   // Create static threads.
   return this->create_threads_i (this->static_threads_,
@@ -420,7 +421,7 @@
   ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
                     mon,
                     this->lock_,
-                    0);
+                    -1);
 
   return this->create_threads_i (this->dynamic_threads_,
                                  number_of_threads,
Index: TAO/examples/CSD_Strategy/ThreadPool2/FooServantList.cpp
===================================================================
--- TAO/examples/CSD_Strategy/ThreadPool2/FooServantList.cpp	(revision 623)
+++ TAO/examples/CSD_Strategy/ThreadPool2/FooServantList.cpp	(working copy)
@@ -78,6 +78,10 @@
 
   {
     GuardType guard(this->num_clients_lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
     num_left = --this->num_clients_;
   }
 
Index: TAO/examples/CSD_Strategy/ThreadPool5/FooServantList.cpp
===================================================================
--- TAO/examples/CSD_Strategy/ThreadPool5/FooServantList.cpp	(revision 623)
+++ TAO/examples/CSD_Strategy/ThreadPool5/FooServantList.cpp	(working copy)
@@ -138,6 +138,10 @@
 
   {
     GuardType guard(this->num_clients_lock_);
+    if (! guard.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
     num_left = --this->num_clients_;
   }
 
@@ -200,6 +204,11 @@
               else
                 {
                   GuardType guard(this->num_clients_lock_);
+                  if (! guard.locked())
+                    {
+                      ACE_UNEXPECTED(;);
+                    }
+
                   // The clients that requests this deactivated servant
                   // will catch exception due to the deactivated servant.
                   // We need descrease the num_clients so the alived
Index: TAO/examples/RTScheduling/DT_Creator.cpp
===================================================================
--- TAO/examples/RTScheduling/DT_Creator.cpp	(revision 623)
+++ TAO/examples/RTScheduling/DT_Creator.cpp	(working copy)
@@ -492,7 +492,7 @@
 DT_Creator::dt_ended (void)
 {
   {
-    ACE_GUARD (ACE_Lock, ace_mon, *state_lock_);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *state_lock_, ACE_UNEXPECTED(;));
     --active_dt_count_;
     if (TAO_debug_level > 0)
       ACE_DEBUG ((LM_DEBUG, "Active dt count = %d\n",active_dt_count_));
@@ -507,7 +507,7 @@
 DT_Creator::job_ended (void)
 {
   {
-    ACE_GUARD (ACE_Lock, ace_mon, *state_lock_);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *state_lock_, ACE_UNEXPECTED(;));
     --active_job_count_;
     char buf [BUFSIZ];
     ACE_OS::sprintf (buf,"Active job count = %d\n",active_job_count_);
@@ -529,7 +529,7 @@
   static int shutdown = 0;
 
   {
-    ACE_GUARD (ACE_Lock, ace_mon, *shutdown_lock_);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *shutdown_lock_, ACE_UNEXPECTED(;));
 
     if (!shutdown)
       {
Index: TAO/examples/RTCORBA/Activity/Activity.cpp
===================================================================
--- TAO/examples/RTCORBA/Activity/Activity.cpp	(revision 623)
+++ TAO/examples/RTCORBA/Activity/Activity.cpp	(working copy)
@@ -251,7 +251,7 @@
 {
   ACE_DEBUG ((LM_DEBUG, "Active task count = %d\n",active_task_count_));
   {
-    ACE_GUARD (ACE_Lock, ace_mon, *state_lock_);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *state_lock_, ACE_UNEXPECTED(;));
     --active_task_count_;
   }
 
@@ -263,7 +263,7 @@
 {
   ACE_DEBUG ((LM_DEBUG, "Active job count = %d\n",active_job_count_));
   {
-    ACE_GUARD (ACE_Lock, ace_mon, *state_lock_);
+    ACE_GUARD_REACTION (ACE_Lock, ace_mon, *state_lock_, ACE_UNEXPECTED(;));
     --active_job_count_;
   }
 
Index: TAO/examples/Content_Server/AMI_Observer/Callback_i.cpp
===================================================================
--- TAO/examples/Content_Server/AMI_Observer/Callback_i.cpp	(revision 623)
+++ TAO/examples/Content_Server/AMI_Observer/Callback_i.cpp	(working copy)
@@ -75,9 +75,10 @@
   else
     {
       {
-        ACE_MT (ACE_GUARD (TAO_SYNCH_MUTEX,
+        ACE_MT (ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                            guard,
-                           this->lock_));
+                                    this->lock_,
+                                    ACE_UNEXPECTED(;)));
 
         this->last_chunk_ = 1;  // Received entire content.
       }
@@ -109,9 +110,10 @@
   try
     {
       {
-        ACE_MT (ACE_GUARD (TAO_SYNCH_MUTEX,
+        ACE_MT (ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                            guard,
-                           this->lock_));
+                                    this->lock_,
+                                    ACE_UNEXPECTED(;)));
         this->metadata_ = metadata;
       }
 
@@ -143,10 +145,10 @@
 int
 Callback_i::metadata_received (void)
 {
-  ACE_MT (ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+  ACE_MT (ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                             guard,
                             this->lock_,
-                            0));
+                              ACE_UNEXPECTED(0)));
 
   return (this->metadata_.content_type.in () != 0);
 }
@@ -154,10 +156,10 @@
 int
 Callback_i::content_received (void)
 {
-  ACE_MT (ACE_GUARD_RETURN (TAO_SYNCH_MUTEX,
+  ACE_MT (ACE_GUARD_REACTION (TAO_SYNCH_MUTEX,
                             guard,
                             this->lock_,
-                            0));
+                              ACE_UNEXPECTED(0)));
 
   return this->last_chunk_;
 }
Index: TAO/examples/Advanced/ch_21/server.cpp
===================================================================
--- TAO/examples/Advanced/ch_21/server.cpp	(revision 623)
+++ TAO/examples/Advanced/ch_21/server.cpp	(working copy)
@@ -148,7 +148,7 @@
 void
 Thermometer_impl::_add_ref ()
 {
-    ACE_Guard<ACE_Mutex> guard (m_count_mutex);
+    ACE_GUARD_REACTION(ACE_Mutex, guard, m_count_mutex, ACE_UNEXPECTED(;));
     m_ref_count++;
 }
 
@@ -157,7 +157,7 @@
 {
     bool del = false;
     {
-    ACE_Guard<ACE_Mutex> guard (m_count_mutex);
+    ACE_GUARD_REACTION(ACE_Mutex, guard, m_count_mutex, ACE_UNEXPECTED(;));
     if (--m_ref_count == 0)
         del = true;
     }
@@ -379,7 +379,7 @@
 Controller_impl::create_thermometer (CCS::AssetType anum, const char * loc)
 {
     {
-    ACE_Guard<ACE_Mutex> guard (m_assets_mutex);
+    ACE_GUARD_REACTION(ACE_Mutex, guard, m_assets_mutex, ACE_UNEXPECTED(0));
 
     if (anum % 2 == 0)
         throw CORBA::BAD_PARAM ();   // Thermometers have odd numbers
@@ -402,7 +402,7 @@
     CCS::TempType       temp)
 {
     {
-    ACE_Guard<ACE_Mutex> guard (m_assets_mutex);
+    ACE_GUARD_REACTION(ACE_Mutex, guard, m_assets_mutex, ACE_UNEXPECTED(0));
 
     if (anum % 2 != 0)
         throw CORBA::BAD_PARAM ();   // Thermostats have even numbers
@@ -453,7 +453,7 @@
 
     AssetSet tmp_assets;
     {
-        ACE_Guard<ACE_Mutex> guard (m_assets_mutex);
+        ACE_GUARD_REACTION(ACE_Mutex, guard, m_assets_mutex, ACE_UNEXPECTED(0));
         tmp_assets = m_assets;
     }
 
@@ -532,7 +532,7 @@
             bool make = false;
             CCS::AssetType num = 0;
                 {
-                ACE_Guard<ACE_Mutex> guard (m_assets_mutex);
+                ACE_GUARD_REACTION(ACE_Mutex, guard, m_assets_mutex, ACE_UNEXPECTED(;));
                 where = m_assets.find (slist[i].key.asset_num ());
                 if (where != m_assets.end ()) {
                 num = *where;
@@ -612,7 +612,10 @@
     if (istr.fail ())
         throw CORBA::OBJECT_NOT_EXIST ();
 
-    ACE_Guard<ACE_Mutex> guard (m_ctrl->m_assets_mutex);
+    ACE_GUARD_REACTION(ACE_Mutex,
+                       guard,
+                       m_ctrl->m_assets_mutex,
+                       throw CORBA::SystemException());
 
     // Check whether the device is known.
     if (!m_ctrl->exists (anum))
Index: protocols/ace/RMCast/Fragment.cpp
===================================================================
--- protocols/ace/RMCast/Fragment.cpp	(revision 623)
+++ protocols/ace/RMCast/Fragment.cpp	(working copy)
@@ -31,6 +31,10 @@
         u64 sn;
         {
           Lock l (mutex_);
+          if (! l.locked())
+            {
+              ACE_UNEXPECTED(;);
+            }
           sn = sn_++;
         }
 
@@ -61,6 +65,10 @@
         u64 sn;
         {
           Lock l (mutex_);
+          if (! l.locked())
+            {
+              ACE_UNEXPECTED(;);
+            }
           sn = sn_++;
         }
 
Index: protocols/ace/RMCast/Flow.cpp
===================================================================
--- protocols/ace/RMCast/Flow.cpp	(revision 623)
+++ protocols/ace/RMCast/Flow.cpp	(working copy)
@@ -35,6 +35,11 @@
       ACE_Time_Value now_time (ACE_OS::gettimeofday ());
 
       Lock l (mutex_);
+      if (! l.locked())
+        {
+          ACE_UNEXPECTED(;);
+        }
+
       sample_bytes_ += data->size ();
 
       if (sample_start_time_ == ACE_Time_Value (0, 0))
@@ -115,6 +120,10 @@
         ACE_Time_Value nak_time (ACE_OS::gettimeofday ());
 
         Lock l (mutex_);
+        if (! l.locked())
+          {
+            ACE_UNEXPECTED(;);
+          }
 
         nak_time_ = nak_time;
 
Index: protocols/ace/RMCast/Simulator.cpp
===================================================================
--- protocols/ace/RMCast/Simulator.cpp	(revision 623)
+++ protocols/ace/RMCast/Simulator.cpp	(working copy)
@@ -25,6 +25,10 @@
     if ((r % 3) == 0) return;
 
     Lock l (mutex_);
+    if (! l.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     if (hold_.get ())
     {
Index: protocols/ace/RMCast/Acknowledge.cpp
===================================================================
--- protocols/ace/RMCast/Acknowledge.cpp	(revision 623)
+++ protocols/ace/RMCast/Acknowledge.cpp	(working copy)
@@ -46,6 +46,10 @@
   {
     {
       Lock l (mutex_);
+      if (! l.locked())
+        {
+          ACE_UNEXPECTED(;);
+        }
       stop_ = true;
       cond_.signal ();
     }
@@ -89,6 +93,10 @@
 
       {
         Lock l (mutex_);
+        if (! l.locked())
+          {
+            ACE_UNEXPECTED(;);
+          }
 
         if (stop_)
           break;
@@ -152,6 +160,10 @@
         time += params_.tick ();
 
         Lock l (mutex_);
+        if (! l.locked())
+          {
+            ACE_UNEXPECTED(;);
+          }
 
         while (!stop_)
         {
@@ -249,6 +261,10 @@
     // efficient).
     //
     Lock l (mutex_);
+    if (! l.locked())
+      {
+        ACE_UNEXPECTED(;);
+      }
 
     if (NRTM const* nrtm = static_cast<NRTM const*> (m->find (NRTM::id)))
     {
@@ -340,6 +356,10 @@
         if (max_elem > 0)
         {
           Lock l (mutex_);
+          if (! l.locked())
+            {
+              ACE_UNEXPECTED(;);
+            }
 
           Profile_ptr nrtm (create_nrtm (max_elem));
 
Index: protocols/ace/RMCast/Retransmit.cpp
===================================================================
--- protocols/ace/RMCast/Retransmit.cpp	(revision 623)
+++ protocols/ace/RMCast/Retransmit.cpp	(working copy)
@@ -37,6 +37,10 @@
   {
     {
       Lock l (mutex_);
+      if (! l.locked())
+        {
+          ACE_UNEXPECTED(;);
+        }
       stop_ = true;
       cond_.signal ();
     }
@@ -53,6 +57,10 @@
       SN const* sn = static_cast<SN const*> (m->find (SN::id));
 
       Lock l (mutex_);
+      if (! l.locked())
+        {
+          ACE_UNEXPECTED(;);
+        }
       queue_.bind (sn->num (), Descr (m->clone ()));
     }
 
@@ -68,6 +76,10 @@
       if (nak->address () == to)
       {
         Lock l (mutex_);
+        if (! l.locked())
+          {
+            ACE_UNEXPECTED(;);
+          }
 
         for (NAK::iterator j (const_cast<NAK*> (nak)->begin ());
              !j.done ();
@@ -118,6 +130,10 @@
     while (true)
     {
       Lock l (mutex_);
+      if (! l.locked())
+        {
+          ACE_UNEXPECTED(;);
+        }
 
       for (Queue::iterator i (queue_); !i.done ();)
       {
Index: protocols/ace/RMCast/Socket.cpp
===================================================================
--- protocols/ace/RMCast/Socket.cpp	(revision 623)
+++ protocols/ace/RMCast/Socket.cpp	(working copy)
@@ -170,6 +170,10 @@
         abs_time = ACE_OS::gettimeofday () + *timeout;
 
       Lock l (mutex_);
+      if (! l.locked())
+        {
+          ACE_UNEXPECTED(-1);
+        }
 
       while (queue_.is_empty ())
         {
@@ -237,6 +241,10 @@
         abs_time = ACE_OS::gettimeofday () + *timeout;
 
       Lock l (mutex_);
+      if (! l.locked())
+        {
+          ACE_UNEXPECTED(-1);
+        }
 
       while (queue_.is_empty ())
         {
@@ -304,6 +312,10 @@
             }
 
           Lock l (mutex_);
+          if (! l.locked())
+            {
+              ACE_UNEXPECTED(;);
+            }
 
           //if (queue_.size () != 0)
           //  cerr << "recv socket queue size: " << queue_.size () << endl;
Index: protocols/ace/RMCast/Link.cpp
===================================================================
--- protocols/ace/RMCast/Link.cpp	(revision 623)
+++ protocols/ace/RMCast/Link.cpp	(working copy)
@@ -98,6 +98,10 @@
     //
     {
       Lock l (mutex_);
+      if (! l.locked())
+        {
+          ACE_UNEXPECTED(;);
+        }
       stop_ = true;
     }
     recv_mgr_.wait ();
@@ -114,6 +118,10 @@
       if ((ACE_OS::rand () % 17) != 0)
       {
         Lock l (mutex_);
+        if (! l.locked())
+          {
+            ACE_UNEXPECTED(;);
+          }
 
         if (hold_.get ())
         {
@@ -217,6 +225,11 @@
         //
         {
           Lock l (mutex_);
+          if (! l.locked())
+            {
+              ACE_UNEXPECTED(;);
+            }
+
           if (stop_)
             return;
         }
Index: protocols/ace/TMCast/MTQueue.hpp
===================================================================
--- protocols/ace/TMCast/MTQueue.hpp	(revision 623)
+++ protocols/ace/TMCast/MTQueue.hpp	(working copy)
@@ -113,7 +113,10 @@
     void
     lock () const
     {
-      mutex_.acquire ();
+      if (mutex_.acquire () == -1)
+        {
+          ACE_UNEXPECTED(;);
+        }
     }
 
     void
Index: protocols/ace/TMCast/Group.cpp
===================================================================
--- protocols/ace/TMCast/Group.cpp	(revision 623)
+++ protocols/ace/TMCast/Group.cpp	(working copy)
@@ -126,6 +126,10 @@
 
         {
           AutoLock lock (mutex_);
+          if (! lock.locked())
+            {
+              ACE_UNEXPECTED(;);
+            }
 
           // Loop
           //
@@ -365,6 +369,10 @@
 
       MessageQueueAutoLock l1 (out_data_);
       AutoLock l2 (mutex_);
+      if (! l2.locked())
+        {
+          ACE_UNEXPECTED(;);
+        }
 
       throw_if_failed ();
 
@@ -413,6 +421,10 @@
     //FUZZ: enable check_for_lack_ACE_OS
 
       AutoLock lock (mutex_);
+      if (! lock.locked())
+        {
+          throw Group::Failed();
+        }
 
       while (true)
       {
Index: protocols/ace/HTBP/HTBP_Session.cpp
===================================================================
--- protocols/ace/HTBP/HTBP_Session.cpp	(revision 623)
+++ protocols/ace/HTBP/HTBP_Session.cpp	(working copy)
@@ -25,7 +25,7 @@
 ACE_UINT32
 ACE::HTBP::Session::next_session_id ()
 {
-  ACE_Guard<ACE_SYNCH_MUTEX> g(ACE::HTBP::Session::session_id_lock_);
+  ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, ACE::HTBP::Session::session_id_lock_, ACE_UNEXPECTED(0));
   return ++last_session_id_;
 }
 
Index: protocols/ace/HTBP/HTBP_ID_Requestor.cpp
===================================================================
--- protocols/ace/HTBP/HTBP_ID_Requestor.cpp	(revision 623)
+++ protocols/ace/HTBP/HTBP_ID_Requestor.cpp	(working copy)
@@ -101,7 +101,7 @@
   if (ACE::HTBP::ID_Requestor::htid_.length() != 0)
     return ACE::HTBP::ID_Requestor::htid_.rep();
 
-  ACE_Guard<ACE_SYNCH_MUTEX> guard (ACE::HTBP::ID_Requestor::htid_lock_);
+  ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, guard, ACE::HTBP::ID_Requestor::htid_lock_, ACE_UNEXPECTED(0));
 
   if (ACE::HTBP::ID_Requestor::htid_.length() != 0)
     return ACE::HTBP::ID_Requestor::htid_.rep();
Index: ace/UUID.cpp
===================================================================
--- ace/UUID.cpp	(revision 623)
+++ ace/UUID.cpp	(working copy)
@@ -419,7 +419,7 @@
     this->get_timestamp (time_last_);
 
     {
-      ACE_GUARD (ACE_SYNCH_MUTEX, ace_mon, *lock_);
+      ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, ace_mon, *lock_, ACE_UNEXPECTED(;));
       uuid_state_.timestamp = time_last_;
       uuid_state_.node.node_ID (node_id);
     }
@@ -444,9 +444,12 @@
     uuid.time_hi_and_version (tHAV);
 
     u_char cseqHAV;
+    {
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, mon, *lock_, ACE_UNEXPECTED(;));
     uuid.clock_seq_low (static_cast<u_char> (clock_sequence & 0xFF));
     cseqHAV = static_cast<u_char> ((clock_sequence & 0x3f00) >> 8);
     uuid_state_.timestamp = timestamp;
+    }
 
     cseqHAV |= variant;
     uuid.clock_seq_hi_and_reserved (cseqHAV);
@@ -483,7 +486,7 @@
   void
   UUID_Generator::get_timestamp (UUID_Time& timestamp)
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, mon, *lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, mon, *lock_, ACE_UNEXPECTED(;));
 
     this->get_systemtime (timestamp);
 
Index: ace/Sig_Handler.cpp
===================================================================
--- ace/Sig_Handler.cpp	(revision 623)
+++ ace/Sig_Handler.cpp	(working copy)
@@ -71,7 +71,7 @@
   ACE_MT (ACE_Recursive_Thread_Mutex *lock =
           ACE_Managed_Object<ACE_Recursive_Thread_Mutex>::get_preallocated_object
           (ACE_Object_Manager::ACE_SIG_HANDLER_LOCK);
-          ACE_Guard<ACE_Recursive_Thread_Mutex> m (*lock));
+          ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, m, *lock, ACE_UNEXPECTED(0)));
   return ACE_Sig_Handler::sig_pending_ != 0;
 }
 
@@ -83,7 +83,7 @@
   ACE_MT (ACE_Recursive_Thread_Mutex *lock =
           ACE_Managed_Object<ACE_Recursive_Thread_Mutex>::get_preallocated_object
           (ACE_Object_Manager::ACE_SIG_HANDLER_LOCK);
-          ACE_Guard<ACE_Recursive_Thread_Mutex> m (*lock));
+          ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, m, *lock, ACE_UNEXPECTED(;)));
   ACE_Sig_Handler::sig_pending_ = pending;
 }
 
@@ -94,7 +94,7 @@
   ACE_MT (ACE_Recursive_Thread_Mutex *lock =
     ACE_Managed_Object<ACE_Recursive_Thread_Mutex>::get_preallocated_object
       (ACE_Object_Manager::ACE_SIG_HANDLER_LOCK);
-    ACE_Guard<ACE_Recursive_Thread_Mutex> m (*lock));
+    ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, m, *lock, ACE_UNEXPECTED (0)));
 
   if (ACE_Sig_Handler::in_range (signum))
     return ACE_Sig_Handler::signal_handlers_[signum];
@@ -127,7 +127,7 @@
   ACE_MT (ACE_Recursive_Thread_Mutex *lock =
     ACE_Managed_Object<ACE_Recursive_Thread_Mutex>::get_preallocated_object
       (ACE_Object_Manager::ACE_SIG_HANDLER_LOCK);
-    ACE_Guard<ACE_Recursive_Thread_Mutex> m (*lock));
+    ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, m, *lock, ACE_UNEXPECTED(0)));
 
   return ACE_Sig_Handler::handler_i (signum, new_sh);
 }
@@ -186,7 +186,7 @@
   ACE_MT (ACE_Recursive_Thread_Mutex *lock =
     ACE_Managed_Object<ACE_Recursive_Thread_Mutex>::get_preallocated_object
       (ACE_Object_Manager::ACE_SIG_HANDLER_LOCK);
-    ACE_Guard<ACE_Recursive_Thread_Mutex> m (*lock));
+    ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, m, *lock, ACE_UNEXPECTED(-1)));
 
   return ACE_Sig_Handler::register_handler_i (signum,
                                               new_sh,
@@ -207,7 +207,7 @@
   ACE_MT (ACE_Recursive_Thread_Mutex *lock =
     ACE_Managed_Object<ACE_Recursive_Thread_Mutex>::get_preallocated_object
       (ACE_Object_Manager::ACE_SIG_HANDLER_LOCK);
-    ACE_Guard<ACE_Recursive_Thread_Mutex> m (*lock));
+    ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, m, *lock, ACE_UNEXPECTED(-1)));
 
   if (ACE_Sig_Handler::in_range (signum))
     {
@@ -346,7 +346,7 @@
   ACE_MT (ACE_Recursive_Thread_Mutex *lock =
     ACE_Managed_Object<ACE_Recursive_Thread_Mutex>::get_preallocated_object
       (ACE_Object_Manager::ACE_SIG_HANDLER_LOCK);
-    ACE_Guard<ACE_Recursive_Thread_Mutex> m (*lock));
+    ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, m, *lock, ACE_UNEXPECTED(-1)));
 
   if (ACE_Sig_Handler::in_range (signum))
     {
@@ -473,7 +473,7 @@
   ACE_MT (ACE_Recursive_Thread_Mutex *lock =
     ACE_Managed_Object<ACE_Recursive_Thread_Mutex>::get_preallocated_object
       (ACE_Object_Manager::ACE_SIG_HANDLER_LOCK);
-    ACE_Guard<ACE_Recursive_Thread_Mutex> m (*lock));
+	  ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, m, *lock, ACE_UNEXPECTED(-1)));
 
   if (ACE_Sig_Handler::in_range (signum))
     {
Index: ace/Free_List.cpp
===================================================================
--- ace/Free_List.cpp	(revision 623)
+++ ace/Free_List.cpp	(working copy)
@@ -58,7 +58,7 @@
 template <class T, class ACE_LOCK> void
 ACE_Locked_Free_List<T, ACE_LOCK>::add (T *element)
 {
-  ACE_MT (ACE_GUARD (ACE_LOCK, ace_mon, this->mutex_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(;)));
 
   // Check to see that we not at the high water mark.
   if (this->mode_ == ACE_PURE_FREE_LIST
Index: ace/Token_Invariants.cpp
===================================================================
--- ace/Token_Invariants.cpp	(revision 623)
+++ ace/Token_Invariants.cpp	(working copy)
@@ -91,7 +91,7 @@
 ACE_Token_Invariant_Manager::mutex_releasing (const ACE_TCHAR *token_name)
 {
   ACE_TRACE ("ACE_Token_Invariant_Manager::mutex_releasing");
-  ACE_GUARD (ACE_TOKEN_CONST::MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_TOKEN_CONST::MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   ACE_Mutex_Invariants *inv = 0;
   if (this->get_mutex (token_name, inv) == 0)
@@ -130,7 +130,7 @@
 {
   ACE_TRACE ("ACE_Token_Invariant_Manager::rwlock_releasing");
 
-  ACE_GUARD (ACE_TOKEN_CONST::MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_TOKEN_CONST::MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   ACE_RWLock_Invariants *inv = 0;
   if (this->get_rwlock (token_name, inv) == 0)
Index: ace/Select_Reactor_T.cpp
===================================================================
--- ace/Select_Reactor_T.cpp	(revision 623)
+++ ace/Select_Reactor_T.cpp	(working copy)
@@ -104,7 +104,7 @@
 ACE_Select_Reactor_T<ACE_SELECT_REACTOR_TOKEN>::initialized (void)
 {
   ACE_TRACE ("ACE_Select_Reactor_T::initialized");
-  ACE_MT (ACE_GUARD_RETURN (ACE_SELECT_REACTOR_TOKEN, ace_mon, this->token_, false));
+  ACE_MT (ACE_GUARD_REACTION (ACE_SELECT_REACTOR_TOKEN, ace_mon, this->token_, ACE_UNEXPECTED(false)));
   return this->initialized_;
 }
 
@@ -138,15 +138,15 @@
 template <class ACE_SELECT_REACTOR_TOKEN> bool
 ACE_Select_Reactor_T<ACE_SELECT_REACTOR_TOKEN>::restart (void)
 {
-  ACE_MT (ACE_GUARD_RETURN (ACE_SELECT_REACTOR_TOKEN, ace_mon, this->token_, false));
+  ACE_MT (ACE_GUARD_REACTION (ACE_SELECT_REACTOR_TOKEN, ace_mon, this->token_, ACE_UNEXPECTED(-1)));
   return this->restart_;
 }
 
 template <class ACE_SELECT_REACTOR_TOKEN> bool
 ACE_Select_Reactor_T<ACE_SELECT_REACTOR_TOKEN>::restart (bool r)
 {
-  ACE_MT (ACE_GUARD_RETURN (ACE_SELECT_REACTOR_TOKEN, ace_mon, this->token_, false));
-  bool const current_value = this->restart_;
+  ACE_MT (ACE_GUARD_REACTION (ACE_SELECT_REACTOR_TOKEN, ace_mon, this->token_, ACE_UNEXPECTED(-1)));
+  int const current_value = this->restart_;
   this->restart_ = r;
   return current_value;
 }
@@ -155,7 +155,7 @@
 ACE_Select_Reactor_T<ACE_SELECT_REACTOR_TOKEN>::requeue_position (int rp)
 {
   ACE_TRACE ("ACE_Select_Reactor_T::requeue_position");
-  ACE_MT (ACE_GUARD (ACE_SELECT_REACTOR_TOKEN, ace_mon, this->token_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_SELECT_REACTOR_TOKEN, ace_mon, this->token_, ACE_UNEXPECTED(;)));
 #if defined (ACE_WIN32)
   ACE_UNUSED_ARG (rp);
   // Must always requeue ourselves "next" on Win32.
@@ -177,7 +177,7 @@
 ACE_Select_Reactor_T<ACE_SELECT_REACTOR_TOKEN>::max_notify_iterations (int iterations)
 {
   ACE_TRACE ("ACE_Select_Reactor_T::max_notify_iterations");
-  ACE_MT (ACE_GUARD (ACE_SELECT_REACTOR_TOKEN, ace_mon, this->token_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_SELECT_REACTOR_TOKEN, ace_mon, this->token_, ACE_UNEXPECTED(;)));
 
   this->notify_handler_->max_notify_iterations (iterations);
 }
@@ -1014,10 +1014,10 @@
   ACE_Time_Value mwt (max_wait_time);
   ACE_MT (ACE_Countdown_Time countdown (&mwt));
 
-  ACE_MT (ACE_GUARD_RETURN (ACE_SELECT_REACTOR_TOKEN,
+  ACE_MT (ACE_GUARD_REACTION (ACE_SELECT_REACTOR_TOKEN,
                             ace_mon,
                             this->token_,
-                            -1));
+                              ACE_UNEXPECTED(-1)));
 
   if (this->deactivated_)
     return 0;
Index: ace/Message_Block.cpp
===================================================================
--- ace/Message_Block.cpp	(revision 623)
+++ ace/Message_Block.cpp	(working copy)
@@ -202,7 +202,7 @@
   if (this->locking_strategy_)
     {
       // We need to acquire the lock before retrieving the count
-      ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->locking_strategy_, 0);
+      ACE_GUARD_RETURN (ACE_Lock, ace_mon, *this->locking_strategy_, -1);
 
       return this->reference_count_i ();
     }
Index: ace/Reactor.cpp
===================================================================
--- ace/Reactor.cpp	(revision 623)
+++ ace/Reactor.cpp	(working copy)
@@ -175,8 +175,9 @@
 {
   ACE_TRACE ("ACE_Reactor::close_singleton");
 
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                     *ACE_Static_Object_Lock::instance ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Static_Object_Lock::instance (),
+                              ACE_UNEXPECTED(;)));
 
   if (ACE_Reactor::delete_reactor_)
     {
Index: ace/Timer_Hash_T.cpp
===================================================================
--- ace/Timer_Hash_T.cpp	(revision 623)
+++ ace/Timer_Hash_T.cpp	(working copy)
@@ -346,7 +346,7 @@
 ACE_Timer_Hash_T<TYPE, FUNCTOR, ACE_LOCK, BUCKET>::~ACE_Timer_Hash_T (void)
 {
   ACE_TRACE ("ACE_Timer_Hash_T::~ACE_Timer_Hash_T");
-  ACE_MT (ACE_GUARD (ACE_LOCK, ace_mon, this->mutex_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(;)));
 
   delete iterator_;
 
Index: ace/Message_Queue_T.cpp
===================================================================
--- ace/Message_Queue_T.cpp	(revision 623)
+++ ace/Message_Queue_T.cpp	(working copy)
@@ -817,7 +817,7 @@
 ACE_Message_Queue<ACE_SYNCH_USE>::is_empty (void)
 {
   ACE_TRACE ("ACE_Message_Queue<ACE_SYNCH_USE>::is_empty");
-  ACE_GUARD_RETURN (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_, false);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_, ACE_UNEXPECTED(-1));
 
   return this->is_empty_i ();
 }
@@ -828,7 +828,7 @@
 ACE_Message_Queue<ACE_SYNCH_USE>::is_full (void)
 {
   ACE_TRACE ("ACE_Message_Queue<ACE_SYNCH_USE>::is_full");
-  ACE_GUARD_RETURN (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_, false);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_, ACE_UNEXPECTED(-1));
 
   return this->is_full_i ();
 }
@@ -846,7 +846,7 @@
 ACE_Message_Queue<ACE_SYNCH_USE>::high_water_mark (size_t hwm)
 {
   ACE_TRACE ("ACE_Message_Queue<ACE_SYNCH_USE>::high_water_mark");
-  ACE_GUARD (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->high_water_mark_ = hwm;
 }
@@ -864,7 +864,7 @@
 ACE_Message_Queue<ACE_SYNCH_USE>::low_water_mark (size_t lwm)
 {
   ACE_TRACE ("ACE_Message_Queue<ACE_SYNCH_USE>::low_water_mark");
-  ACE_GUARD (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->low_water_mark_ = lwm;
 }
@@ -891,7 +891,7 @@
 ACE_Message_Queue<ACE_SYNCH_USE>::message_count (void)
 {
   ACE_TRACE ("ACE_Message_Queue<ACE_SYNCH_USE>::message_count");
-  ACE_GUARD_RETURN (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_, ACE_UNEXPECTED(0));
 
   return this->cur_count_;
 }
@@ -993,7 +993,7 @@
 ACE_Message_Queue<ACE_SYNCH_USE>::message_bytes (size_t new_value)
 {
   ACE_TRACE ("ACE_Message_Queue<ACE_SYNCH_USE>::message_bytes");
-  ACE_GUARD (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->cur_bytes_ = new_value;
 }
@@ -1002,7 +1002,7 @@
 ACE_Message_Queue<ACE_SYNCH_USE>::message_length (size_t new_value)
 {
   ACE_TRACE ("ACE_Message_Queue<ACE_SYNCH_USE>::message_length");
-  ACE_GUARD (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX_T, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->cur_length_ = new_value;
 }
Index: ace/Process_Manager.cpp
===================================================================
--- ace/Process_Manager.cpp	(revision 623)
+++ ace/Process_Manager.cpp	(working copy)
@@ -183,8 +183,9 @@
 {
   ACE_TRACE ("ACE_Process_Manager::close_singleton");
 
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                     *ACE_Static_Object_Lock::instance ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Static_Object_Lock::instance (),
+                              ACE_UNEXPECTED(;)));
 
   if (ACE_Process_Manager::delete_instance_)
     {
Index: ace/Malloc_T.cpp
===================================================================
--- ace/Malloc_T.cpp	(revision 623)
+++ ace/Malloc_T.cpp	(working copy)
@@ -385,7 +385,7 @@
 ACE_Malloc_T<ACE_MEM_POOL_2, ACE_LOCK, ACE_CB>::free (void *ptr)
 {
   ACE_TRACE ("ACE_Malloc_T<ACE_MEM_POOL_2, ACE_LOCK, ACE_CB>::free");
-  ACE_GUARD (ACE_LOCK, ace_mon, *this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
   this->shared_free (ptr);
 }
Index: ace/Select_Reactor_T.inl
===================================================================
--- ace/Select_Reactor_T.inl	(revision 623)
+++ ace/Select_Reactor_T.inl	(working copy)
@@ -218,9 +218,10 @@
 ACE_Select_Reactor_T<ACE_SELECT_REACTOR_TOKEN>::deactivate (int do_stop)
 {
   {
-    ACE_MT (ACE_GUARD (ACE_SELECT_REACTOR_TOKEN,
+    ACE_MT (ACE_GUARD_REACTION (ACE_SELECT_REACTOR_TOKEN,
                        ace_mon,
-                       this->token_));
+                                this->token_,
+                                ACE_UNEXPECTED(;)));
     this->deactivated_ = do_stop;
   }
 
Index: ace/Dev_Poll_Reactor.cpp
===================================================================
--- ace/Dev_Poll_Reactor.cpp	(revision 623)
+++ ace/Dev_Poll_Reactor.cpp	(working copy)
@@ -2012,7 +2012,7 @@
 {
   ACE_TRACE ("ACE_Dev_Poll_Reactor::max_notify_iterations");
 
-  ACE_MT (ACE_GUARD (ACE_Dev_Poll_Reactor_Token, mon, this->token_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Dev_Poll_Reactor_Token, mon, this->token_, ACE_UNEXPECTED(;)));
 
   this->notify_handler_->max_notify_iterations (iterations);
 }
Index: ace/Cached_Connect_Strategy_T.cpp
===================================================================
--- ace/Cached_Connect_Strategy_T.cpp	(revision 623)
+++ ace/Cached_Connect_Strategy_T.cpp	(working copy)
@@ -527,7 +527,7 @@
 ACE_Cached_Connect_Strategy_Ex<ACE_T2>::cleanup (void)
 {
   // Excluded other threads from changing the cache while we cleanup
-  ACE_GUARD (MUTEX, ace_mon, *this->lock_);
+  ACE_GUARD_REACTION (MUTEX, ace_mon, *this->lock_, ACE_UNEXPECTED(;));
 
   // Close down all cached service handlers.
   typename CONNECTION_CACHE::ITERATOR iter = this->connection_cache_.begin ();
Index: ace/Proactor.cpp
===================================================================
--- ace/Proactor.cpp	(revision 623)
+++ ace/Proactor.cpp	(working copy)
@@ -405,8 +405,9 @@
 {
   ACE_TRACE ("ACE_Proactor::close_singleton");
 
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                     *ACE_Static_Object_Lock::instance ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Static_Object_Lock::instance (),
+                              ACE_UNEXPECTED(;)));
 
   if (ACE_Proactor::delete_proactor_)
     {
@@ -593,7 +594,7 @@
 {
   ACE_TRACE ("ACE_Proactor::proactor_event_loop_done");
 
-  ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, mutex_, -1));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_mon, mutex_, ACE_UNEXPECTED(-1)));
 
   return this->end_event_loop_ != 0 ? 1 : 0 ;
 }
Index: ace/Log_Msg.cpp
===================================================================
--- ace/Log_Msg.cpp	(revision 623)
+++ ace/Log_Msg.cpp	(working copy)
@@ -577,8 +577,9 @@
 {
   ACE_TRACE ("ACE_Log_Msg::flags");
   u_long result;
-  ACE_MT (ACE_GUARD_RETURN (ACE_Recursive_Thread_Mutex, ace_mon,
-                            *ACE_Log_Msg_Manager::get_lock (), 0));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Log_Msg_Manager::get_lock (),
+                              ACE_UNEXPECTED(0)));
 
   result = ACE_Log_Msg::flags_;
   return result;
@@ -588,8 +589,9 @@
 ACE_Log_Msg::set_flags (u_long flgs)
 {
   ACE_TRACE ("ACE_Log_Msg::set_flags");
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                     *ACE_Log_Msg_Manager::get_lock ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Log_Msg_Manager::get_lock (),
+                              ACE_UNEXPECTED(;)));
 
   ACE_SET_BITS (ACE_Log_Msg::flags_, flgs);
 }
@@ -598,8 +600,9 @@
 ACE_Log_Msg::clr_flags (u_long flgs)
 {
   ACE_TRACE ("ACE_Log_Msg::clr_flags");
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                     *ACE_Log_Msg_Manager::get_lock ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Log_Msg_Manager::get_lock (),
+                              ACE_UNEXPECTED(;)));
 
   ACE_CLR_BITS (ACE_Log_Msg::flags_, flgs);
 }
@@ -664,8 +667,9 @@
 {
   // ACE_TRACE ("ACE_Log_Msg::ACE_Log_Msg");
 
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                     *ACE_Log_Msg_Manager::get_lock ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Log_Msg_Manager::get_lock (),
+                              ACE_UNEXPECTED(;)));
   ++instance_count_;
 
   if (this->instance_count_ == 1)
@@ -704,8 +708,9 @@
   // If ACE_Log_Msg_Manager::close () is called, the lock will
   // be deleted.
   {
-    ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                       *ACE_Log_Msg_Manager::get_lock ()));
+    ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                                *ACE_Log_Msg_Manager::get_lock (),
+                                ACE_UNEXPECTED(;)));
     instance_count = --instance_count_;
   }
   // Release the guard.
Index: ace/Test_and_Set.cpp
===================================================================
--- ace/Test_and_Set.cpp	(revision 623)
+++ ace/Test_and_Set.cpp	(working copy)
@@ -22,7 +22,7 @@
 template <class ACE_LOCK, class TYPE> TYPE
 ACE_Test_and_Set<ACE_LOCK, TYPE>::is_set (void) const
 {
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, (ACE_LOCK &) this->lock_, this->is_set_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, (ACE_LOCK &) this->lock_, ACE_UNEXPECTED(this->is_set_));
   return this->is_set_;
 }
 
@@ -30,7 +30,7 @@
 template <class ACE_LOCK, class TYPE> TYPE
 ACE_Test_and_Set<ACE_LOCK, TYPE>::set (TYPE status)
 {
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->lock_, this->is_set_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(this->is_set_));
   TYPE o_status = this->is_set_;
   this->is_set_ = status;
   return o_status;
Index: ace/Thread_Manager.cpp
===================================================================
--- ace/Thread_Manager.cpp	(revision 623)
+++ ace/Thread_Manager.cpp	(working copy)
@@ -417,8 +417,9 @@
 {
   ACE_TRACE ("ACE_Thread_Manager::close_singleton");
 
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                     *ACE_Static_Object_Lock::instance ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Static_Object_Lock::instance (),
+                              ACE_UNEXPECTED(;)));
 
   if (ACE_Thread_Manager::delete_thr_mgr_)
     {
@@ -1119,7 +1120,7 @@
                                  int enable)
 {
   ACE_TRACE ("ACE_Thread_Manager::check_state");
-  ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, this->lock_, -1));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_mon, this->lock_, ACE_UNEXPECTED(-1)));
 
   ACE_UINT32 thr_state;
 
@@ -1190,7 +1191,7 @@
 ACE_Thread_Manager::hthread_within (ACE_hthread_t handle)
 {
   ACE_TRACE ("ACE_Thread_Manager::hthread_within");
-  ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_monx, this->lock_, -1));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_monx, this->lock_, ACE_UNEXPECTED(-1)));
 
   for (ACE_Double_Linked_List_Iterator<ACE_Thread_Descriptor> iter (this->thr_list_);
        !iter.done ();
@@ -1209,7 +1210,7 @@
 ACE_Thread_Manager::thread_within (ACE_thread_t tid)
 {
   ACE_TRACE ("ACE_Thread_Manager::thread_within");
-  ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_monx, this->lock_, -1));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_monx, this->lock_, ACE_UNEXPECTED(-1)));
 
   for (ACE_Double_Linked_List_Iterator<ACE_Thread_Descriptor> iter (this->thr_list_);
        !iter.done ();
Index: ace/Task.inl
===================================================================
--- ace/Task.inl	(revision 623)
+++ ace/Task.inl	(working copy)
@@ -9,7 +9,7 @@
 ACE_Task_Base::grp_id (void) const
 {
   ACE_TRACE ("ACE_Task_Base::grp_id");
-  ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, const_cast <ACE_Thread_Mutex&>(this->lock_), -1));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_mon, const_cast <ACE_Thread_Mutex&>(this->lock_), ACE_UNEXPECTED(-1)));
   return this->grp_id_;
 }
 
@@ -19,7 +19,7 @@
 ACE_Task_Base::grp_id (int identifier)
 {
   ACE_TRACE ("ACE_Task_Base::grp_id");
-  ACE_MT (ACE_GUARD (ACE_Thread_Mutex, ace_mon, this->lock_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_mon, this->lock_, ACE_UNEXPECTED(;)));
 
   // Cache the group id in the task and then set it in the
   // Thread_Manager, if there is one.
@@ -61,7 +61,7 @@
 ACE_Task_Base::thr_count (void) const
 {
   ACE_TRACE ("ACE_Task_Base::thr_count");
-  ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, const_cast <ACE_Thread_Mutex&>(this->lock_), 0));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_mon, const_cast <ACE_Thread_Mutex&>(this->lock_), ACE_UNEXPECTED(0)));
 
   return this->thr_count_;
 }
Index: ace/SSL/SSL_Context.cpp
===================================================================
--- ace/SSL/SSL_Context.cpp	(revision 623)
+++ ace/SSL/SSL_Context.cpp	(working copy)
@@ -138,9 +138,10 @@
 void
 ACE_SSL_Context::ssl_library_init (void)
 {
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex,
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex,
                      ace_ssl_mon,
-                     *ACE_Static_Object_Lock::instance ()));
+                              *ACE_Static_Object_Lock::instance (),
+                              ACE_UNEXPECTED(;)));
 
   if (ssl_library_init_count == 0)
     {
@@ -199,9 +200,10 @@
 void
 ACE_SSL_Context::ssl_library_fini (void)
 {
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex,
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex,
                      ace_ssl_mon,
-                     *ACE_Static_Object_Lock::instance ()));
+                              *ACE_Static_Object_Lock::instance (),
+                              ACE_UNEXPECTED(;)));
 
   --ssl_library_init_count;
   if (ssl_library_init_count == 0)
Index: ace/SSL/SSL_Asynch_Stream.cpp
===================================================================
--- ace/SSL/SSL_Asynch_Stream.cpp	(revision 623)
+++ ace/SSL/SSL_Asynch_Stream.cpp	(working copy)
@@ -986,7 +986,7 @@
 ACE_SSL_Asynch_Stream::handle_write_stream (
   const ACE_Asynch_Write_Stream::Result &result)
 {
-  ACE_MT (ACE_GUARD (ACE_SYNCH_MUTEX, ace_mon, this->mutex_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;)));
 
   this->bio_out_flag_ &= ~BF_AIO;
 
@@ -1031,7 +1031,7 @@
 ACE_SSL_Asynch_Stream::handle_read_stream (
   const ACE_Asynch_Read_Stream::Result &result)
 {
-  ACE_MT (ACE_GUARD (ACE_SYNCH_MUTEX, ace_mon, this->mutex_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;)));
 
   this->bio_inp_flag_ &= ~BF_AIO;
 
@@ -1054,7 +1054,7 @@
   ACE_Handler * user_handler = 0;
 
   {
-    ACE_MT (ACE_GUARD (ACE_SYNCH_MUTEX, ace_mon, this->mutex_));
+    ACE_MT (ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, ace_mon, this->mutex_, ACE_UNEXPECTED(;)));
 
     this->flags_ |= SF_DELETE_ENABLE;
 
Index: ace/Token.inl
===================================================================
--- ace/Token.inl	(revision 623)
+++ ace/Token.inl	(working copy)
@@ -53,7 +53,7 @@
 ACE_Token::current_owner (void)
 {
   ACE_TRACE ("ACE_Token::current_owner");
-  ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, this->lock_, this->owner_);
+  ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_mon, this->lock_, ACE_UNEXPECTED(this->owner_));
 
   return this->owner_;
 }
Index: ace/Connector.cpp
===================================================================
--- ace/Connector.cpp	(revision 623)
+++ ace/Connector.cpp	(working copy)
@@ -75,10 +75,10 @@
 
   {
     // Exclusive access to the Reactor.
-    ACE_GUARD_RETURN (ACE_Lock,
+    ACE_GUARD_REACTION (ACE_Lock,
                       ace_mon,
                       this->reactor ()->lock (),
-                      0);
+                        ACE_UNEXPECTED(0));
 
     // Double check.
     if (!this->svc_handler_)
Index: ace/Bound_Ptr.inl
===================================================================
--- ace/Bound_Ptr.inl	(revision 623)
+++ ace/Bound_Ptr.inl	(working copy)
@@ -98,7 +98,7 @@
 template <class ACE_LOCK> inline void
 ACE_Bound_Ptr_Counter<ACE_LOCK>::attach_weak (ACE_Bound_Ptr_Counter<ACE_LOCK>* counter)
 {
-  ACE_GUARD (ACE_LOCK, guard, counter->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, guard, counter->lock_, ACE_UNEXPECTED(;));
 
   ++counter->self_ref_count_;
 }
@@ -109,7 +109,7 @@
   ACE_Bound_Ptr_Counter<ACE_LOCK> *counter_del = 0;
 
   {
-    ACE_GUARD (ACE_LOCK, guard, counter->lock_);
+    ACE_GUARD_REACTION (ACE_LOCK, guard, counter->lock_, ACE_UNEXPECTED(;));
 
     if (--counter->self_ref_count_ == 0)
       // Since counter contains the lock held by the ACE_Guard, the
@@ -126,7 +126,7 @@
 template <class ACE_LOCK> inline bool
 ACE_Bound_Ptr_Counter<ACE_LOCK>::object_was_deleted (ACE_Bound_Ptr_Counter<ACE_LOCK> *counter)
 {
-  ACE_GUARD_RETURN (ACE_LOCK, guard, counter->lock_, 0);
+  ACE_GUARD_REACTION (ACE_LOCK, guard, counter->lock_, ACE_UNEXPECTED(0));
 
   return counter->obj_ref_count_ == -1;
 }
Index: ace/Service_Repository.cpp
===================================================================
--- ace/Service_Repository.cpp	(revision 623)
+++ ace/Service_Repository.cpp	(working copy)
@@ -83,8 +83,9 @@
 {
   ACE_TRACE ("ACE_Service_Repository::close_singleton");
 
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                     *ACE_Static_Object_Lock::instance ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Static_Object_Lock::instance (),
+                              ACE_UNEXPECTED(;)));
 
   if (ACE_Service_Repository::delete_svc_rep_)
     {
Index: ace/Framework_Component.cpp
===================================================================
--- ace/Framework_Component.cpp	(revision 623)
+++ ace/Framework_Component.cpp	(working copy)
@@ -117,8 +117,9 @@
 {
   ACE_TRACE ("ACE_Framework_Repository::close_singleton");
 
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                     *ACE_Static_Object_Lock::instance ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Static_Object_Lock::instance (),
+                              ACE_UNEXPECTED(;)));
 
   delete ACE_Framework_Repository::repository_;
   ACE_Framework_Repository::repository_ = 0;
Index: ace/Timer_Queue_T.cpp
===================================================================
--- ace/Timer_Queue_T.cpp	(revision 623)
+++ ace/Timer_Queue_T.cpp	(working copy)
@@ -330,7 +330,7 @@
 template <class TYPE, class FUNCTOR, class ACE_LOCK> void
 ACE_Timer_Queue_T<TYPE, FUNCTOR, ACE_LOCK>::return_node (ACE_Timer_Node_T<TYPE> *node)
 {
-  ACE_MT (ACE_GUARD (ACE_LOCK, ace_mon, this->mutex_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(;)));
   this->free_node (node);
 }
 
Index: ace/Atomic_Op_T.inl
===================================================================
--- ace/Atomic_Op_T.inl	(revision 623)
+++ ace/Atomic_Op_T.inl	(working copy)
@@ -17,7 +17,7 @@
 ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator++ (void)
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator++");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, this->value_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(this->value_));
   return ++this->value_;
 }
 
@@ -27,7 +27,7 @@
   typename ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::arg_type rhs)
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator+=");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, this->value_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(this->value_));
   return this->value_ += rhs;
 }
 
@@ -36,7 +36,7 @@
 ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator-- (void)
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator--");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, this->value_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(this->value_));
   return --this->value_;
 }
 
@@ -46,7 +46,7 @@
   typename ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::arg_type rhs)
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator-=");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, this->value_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(this->value_));
   return this->value_ -= rhs;
 }
 
@@ -65,7 +65,7 @@
 ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator++ (int)
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator++");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, this->value_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(this->value_));
   return this->value_++;
 }
 
@@ -74,7 +74,7 @@
 ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator-- (int)
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator--");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, this->value_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(this->value_));
   return this->value_--;
 }
 
@@ -84,7 +84,7 @@
   typename ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::arg_type rhs) const
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator==");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, false);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(false));
   return this->value_ == rhs;
 }
 
@@ -103,7 +103,7 @@
   typename ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::arg_type rhs) const
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator>=");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, false);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(false));
   return this->value_ >= rhs;
 }
 
@@ -113,7 +113,7 @@
   typename ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::arg_type rhs) const
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator>");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, false);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(false));
   return this->value_ > rhs;
 }
 
@@ -123,7 +123,7 @@
   typename ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::arg_type rhs) const
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator<=");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, false);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(false));
   return this->value_ <= rhs;
 }
 
@@ -133,7 +133,7 @@
   typename ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::arg_type rhs) const
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator<");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, false);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(false));
   return this->value_ < rhs;
 }
 
@@ -146,7 +146,7 @@
 
   ACE_Atomic_Op_Ex<ACE_LOCK, TYPE> tmp (rhs);
 
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, *this);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(*this));
   std::swap (this->value_, tmp.value_);
 
   return *this;
@@ -157,7 +157,7 @@
 ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::value (void) const
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::value");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, this->value_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(this->value_));
   return this->value_;
 }
 
@@ -178,7 +178,7 @@
   typename ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::arg_type rhs)
 {
   // ACE_TRACE ("ACE_Atomic_Op_Ex<ACE_LOCK, TYPE>::operator=");
-  ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, *this);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(*this));
   this->value_ = rhs;
   return *this;
 }
Index: ace/DLL_Manager.cpp
===================================================================
--- ace/DLL_Manager.cpp	(revision 623)
+++ ace/DLL_Manager.cpp	(working copy)
@@ -52,7 +52,7 @@
                       ACE_SHLIB_HANDLE handle)
 {
   ACE_TRACE ("ACE_DLL_Handle::open");
-  ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, this->lock_, 0));
+  ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, this->lock_, -1));
 
   if (this->dll_name_)
     {
@@ -348,7 +348,7 @@
 ACE_DLL_Handle::get_handle (int become_owner)
 {
   ACE_TRACE ("ACE_DLL_Handle::get_handle");
-  ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, this->lock_, 0));
+  ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, this->lock_, ACE_SHLIB_INVALID_HANDLE));
 
   if (this->refcount_ == 0 && become_owner != 0)
     {
@@ -520,8 +520,9 @@
 {
   ACE_TRACE ("ACE_DLL_Manager::close_singleton");
 
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon,
-                     *ACE_Static_Object_Lock::instance ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon,
+                              *ACE_Static_Object_Lock::instance (),
+                              ACE_UNEXPECTED(;)));
 
   delete ACE_DLL_Manager::instance_;
   ACE_DLL_Manager::instance_ = 0;
@@ -634,7 +635,7 @@
 ACE_DLL_Manager::unload_policy (u_long unload_policy)
 {
   ACE_TRACE ("ACE_DLL_Manager::unload_policy");
-  ACE_MT (ACE_GUARD (ACE_Thread_Mutex, ace_mon, this->lock_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_mon, this->lock_, ACE_UNEXPECTED(;)));
 
   u_long old_policy = this->unload_policy_;
   this->unload_policy_ = unload_policy;
Index: ace/Timer_Queue_T.inl
===================================================================
--- ace/Timer_Queue_T.inl	(revision 623)
+++ ace/Timer_Queue_T.inl	(working copy)
@@ -158,7 +158,7 @@
                                                            ACE_Timer_Node_Dispatch_Info_T<TYPE> &info)
 {
   ACE_TRACE ("ACE_Timer_Queue_T::dispatch_info");
-  ACE_MT (ACE_GUARD_RETURN (ACE_LOCK, ace_mon, this->mutex_, 0));
+  ACE_MT (ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(0)));
 
   return this->dispatch_info_i (cur_time, info);
 }
Index: ace/Future.cpp
===================================================================
--- ace/Future.cpp	(revision 623)
+++ ace/Future.cpp	(working copy)
@@ -100,7 +100,7 @@
 {
   ACE_ASSERT (rep != 0);
   // Use value_ready_mutex_ for both condition and ref count management
-  ACE_MT (ACE_Guard<ACE_Recursive_Thread_Mutex> r_mon (rep->value_ready_mutex_));
+  ACE_MT (ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, r_mon, rep->value_ready_mutex_, ACE_UNEXPECTED(0)));
   ++rep->ref_count_;
   return rep;
 }
@@ -110,7 +110,7 @@
 {
   ACE_ASSERT (rep != 0);
   // Use value_ready_mutex_ for both condition and ref count management
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, r_mon, rep->value_ready_mutex_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, r_mon, rep->value_ready_mutex_, ACE_UNEXPECTED(;)));
 
   if (rep->ref_count_-- == 0)
     {
@@ -128,7 +128,7 @@
   ACE_ASSERT (rep != 0);
   ACE_ASSERT (new_rep != 0);
   // Use value_ready_mutex_ for both condition and ref count management
-  ACE_MT (ACE_GUARD (ACE_Recursive_Thread_Mutex, r_mon, rep->value_ready_mutex_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, r_mon, rep->value_ready_mutex_, ACE_UNEXPECTED(;)));
 
   ACE_Future_Rep<T>* old = rep;
   rep = new_rep;
@@ -267,7 +267,7 @@
   if (this->value_ == 0)
     {
       // Constructor of ace_mon acquires the mutex.
-      ACE_MT (ACE_GUARD_RETURN (ACE_Recursive_Thread_Mutex, ace_mon, this->value_ready_mutex_, 0));
+      ACE_MT (ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon, this->value_ready_mutex_, ACE_UNEXPECTED(0)));
 
       // If the value is not yet defined we must block until the
       // producer writes to it.
Index: ace/WFMO_Reactor.cpp
===================================================================
--- ace/WFMO_Reactor.cpp	(revision 623)
+++ ace/WFMO_Reactor.cpp	(working copy)
@@ -584,7 +584,7 @@
 ACE_WFMO_Reactor_Handler_Repository::unbind_all (void)
 {
   {
-    ACE_GUARD (ACE_Process_Mutex, ace_mon, this->wfmo_reactor_.lock_);
+    ACE_GUARD_REACTION (ACE_Process_Mutex, ace_mon, this->wfmo_reactor_.lock_, ACE_UNEXPECTED(;));
 
     bool dummy;
     size_t i;
@@ -2689,7 +2689,7 @@
 ACE_WFMO_Reactor::max_notify_iterations (int iterations)
 {
   ACE_TRACE ("ACE_WFMO_Reactor::max_notify_iterations");
-  ACE_GUARD (ACE_Process_Mutex, monitor, this->lock_);
+  ACE_GUARD_REACTION (ACE_Process_Mutex, monitor, this->lock_, ACE_UNEXPECTED(;));
 
   // Must always be > 0 or < 0 to optimize the loop exit condition.
   this->notify_handler_->max_notify_iterations (iterations);
Index: ace/POSIX_Asynch_IO.cpp
===================================================================
--- ace/POSIX_Asynch_IO.cpp	(revision 623)
+++ ace/POSIX_Asynch_IO.cpp	(working copy)
@@ -1053,7 +1053,7 @@
 {
   ACE_TRACE ("ACE_POSIX_Asynch_Accept::handle_close");
 
-  ACE_MT (ACE_GUARD_RETURN (ACE_SYNCH_MUTEX, ace_mon, this->lock_, 0));
+  ACE_MT (ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(0)));
 
   // handle_close is called in two cases:
   //  1. Pseudo task is closing (i.e. proactor destructor)
@@ -1078,7 +1078,7 @@
   ACE_POSIX_Asynch_Accept_Result* result = 0;
 
   {
-    ACE_MT (ACE_GUARD_RETURN (ACE_SYNCH_MUTEX, ace_mon, this->lock_, 0));
+    ACE_MT (ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(0)));
 
     // Deregister this info pertaining to this accept call.
     if (this->result_queue_.dequeue_head (result) != 0)
@@ -1562,7 +1562,7 @@
   ACE_POSIX_Asynch_Connect_Result* result = 0;
 
   {
-    ACE_MT (ACE_GUARD_RETURN (ACE_SYNCH_MUTEX, ace_mon, this->lock_, 0));
+    ACE_MT (ACE_GUARD_RETURN (ACE_SYNCH_MUTEX, ace_mon, this->lock_, -1));
     if (this->result_map_.unbind (fd, result) != 0) // not found
       return -1;
   }
@@ -1603,7 +1603,7 @@
   ACE_POSIX_Asynch_Connect_Result* result = 0;
 
   {
-    ACE_MT (ACE_GUARD_RETURN (ACE_SYNCH_MUTEX, ace_mon, this->lock_, 0));
+    ACE_MT (ACE_GUARD_RETURN (ACE_SYNCH_MUTEX, ace_mon, this->lock_, -1));
     if (this->result_map_.unbind (fd, result) != 0) // not found
       return -1;
   }
Index: ace/Timer_List_T.cpp
===================================================================
--- ace/Timer_List_T.cpp	(revision 623)
+++ ace/Timer_List_T.cpp	(working copy)
@@ -123,7 +123,7 @@
 ACE_Timer_List_T<TYPE, FUNCTOR, ACE_LOCK>::~ACE_Timer_List_T (void)
 {
   ACE_TRACE ("ACE_Timer_List_T::~ACE_Timer_List_T");
-  ACE_MT (ACE_GUARD (ACE_LOCK, ace_mon, this->mutex_));
+  ACE_MT (ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->mutex_, ACE_UNEXPECTED(;)));
 
   delete iterator_;
 
Index: ace/Timer_Queue_Adapters.cpp
===================================================================
--- ace/Timer_Queue_Adapters.cpp	(revision 623)
+++ ace/Timer_Queue_Adapters.cpp	(working copy)
@@ -217,7 +217,7 @@
 template<class TQ> void
 ACE_Thread_Timer_Queue_Adapter<TQ>::deactivate (void)
 {
-  ACE_GUARD (ACE_SYNCH_RECURSIVE_MUTEX, guard, this->mutex_);
+  ACE_GUARD_REACTION (ACE_SYNCH_RECURSIVE_MUTEX, guard, this->mutex_, ACE_UNEXPECTED(;));
 
   this->active_ = false;
   this->condition_.signal ();
Index: ace/Token_Manager.cpp
===================================================================
--- ace/Token_Manager.cpp	(revision 623)
+++ ace/Token_Manager.cpp	(working copy)
@@ -78,7 +78,7 @@
   // (see safe_acquire.)  We have to make sure that only one thread
   // uses the collection at a time.
 
-  ACE_GUARD (ACE_TOKEN_CONST::MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_TOKEN_CONST::MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   TOKEN_NAME name (token_name);
 
@@ -219,7 +219,7 @@
   ACE_TRACE ("ACE_Token_Manager::release_token");
   // again, let's perform our own locking here.
 
-  ACE_GUARD (ACE_TOKEN_CONST::MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_TOKEN_CONST::MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (token->dec_reference () == 0)
     {
Index: ace/TSS_T.cpp
===================================================================
--- ace/TSS_T.cpp	(revision 623)
+++ ace/TSS_T.cpp	(working copy)
@@ -94,7 +94,7 @@
 ACE_TSS<TYPE>::ts_init (void)
 {
   // Ensure that we are serialized!
-  ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, this->keylock_, 0);
+  ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, this->keylock_, -1);
 
   // Use the Double-Check pattern to make sure we only create the key
   // once!
Index: ace/WIN32_Asynch_IO.cpp
===================================================================
--- ace/WIN32_Asynch_IO.cpp	(revision 623)
+++ ace/WIN32_Asynch_IO.cpp	(working copy)
@@ -2728,7 +2728,7 @@
   ACE_WIN32_Asynch_Connect_Result* result = 0;
 
   {
-    ACE_MT (ACE_GUARD_RETURN (ACE_SYNCH_MUTEX, ace_mon, this->lock_, 0));
+    ACE_MT (ACE_GUARD_RETURN (ACE_SYNCH_MUTEX, ace_mon, this->lock_, -1));
     if (this->result_map_.unbind (fd, result) != 0) // not found
       return -1;
   }
@@ -2768,7 +2768,7 @@
   ACE_WIN32_Asynch_Connect_Result* result = 0;
 
   {
-    ACE_MT (ACE_GUARD_RETURN (ACE_SYNCH_MUTEX, ace_mon, this->lock_, 0));
+    ACE_MT (ACE_GUARD_RETURN (ACE_SYNCH_MUTEX, ace_mon, this->lock_, -1));
     if (this->result_map_.unbind (fd, result) != 0) // not found
       return -1;
   }
Index: ace/Timeprobe_T.cpp
===================================================================
--- ace/Timeprobe_T.cpp	(revision 623)
+++ ace/Timeprobe_T.cpp	(working copy)
@@ -85,7 +85,7 @@
 template <class ACE_LOCK, class ALLOCATOR> void
 ACE_Timeprobe_Ex<ACE_LOCK, ALLOCATOR>::timeprobe (u_long event)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->timeprobes_[this->current_size_].event_.event_number_ = event;
   this->timeprobes_[this->current_size_].event_type_ = ACE_timeprobe_t::NUMBER;
@@ -109,7 +109,7 @@
 template <class ACE_LOCK, class ALLOCATOR> void
 ACE_Timeprobe_Ex<ACE_LOCK, ALLOCATOR>::timeprobe (const char *event)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->timeprobes_[this->current_size_].event_.event_description_ = event;
   this->timeprobes_[this->current_size_].event_type_ = ACE_timeprobe_t::STRING;
@@ -133,7 +133,7 @@
 template <class ACE_LOCK, class ALLOCATOR> void
 ACE_Timeprobe_Ex<ACE_LOCK, ALLOCATOR>::reset (void)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   this->current_size_ = 0;
   this->report_buffer_full_ = 0;
@@ -142,7 +142,7 @@
 template <class ACE_LOCK, class ALLOCATOR> void
 ACE_Timeprobe_Ex<ACE_LOCK, ALLOCATOR>::increase_size (u_long size)
 {
-  ACE_GUARD (ACE_LOCK, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_LOCK, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   if (size > this->max_size_)
     {
Index: performance-tests/Synch-Benchmarks/Perf_Test/guard_test.cpp
===================================================================
--- performance-tests/Synch-Benchmarks/Perf_Test/guard_test.cpp	(revision 623)
+++ performance-tests/Synch-Benchmarks/Perf_Test/guard_test.cpp	(working copy)
@@ -92,7 +92,7 @@
   while (!this->done ())
     {
       {
-        ACE_GUARD (ACE_Thread_Mutex, _ace_mon, Guard_Test::mutex_);
+        ACE_GUARD_REACTION (ACE_Thread_Mutex, _ace_mon, Guard_Test::mutex_, ACE_UNEXPECTED(;));
 
         performance_test_options.thr_work_count[ni]++;
         buffer++;
Index: performance-tests/Misc/test_guard.cpp
===================================================================
--- performance-tests/Misc/test_guard.cpp	(revision 623)
+++ performance-tests/Misc/test_guard.cpp	(working copy)
@@ -35,7 +35,7 @@
 
 void guard (void)
 {
-  ACE_GUARD (ACE_Thread_Mutex, _ace_mon, lock_);
+  ACE_GUARD_REACTION (ACE_Thread_Mutex, _ace_mon, lock_, ACE_UNEXPECTED(;));
   dummy++;
 }
 
Index: Kokyu/DSRT_Direct_Dispatcher_Impl_T.cpp
===================================================================
--- Kokyu/DSRT_Direct_Dispatcher_Impl_T.cpp	(revision 623)
+++ Kokyu/DSRT_Direct_Dispatcher_Impl_T.cpp	(working copy)
@@ -107,7 +107,7 @@
       ACE_GUARD_RETURN (cond_lock_t,
                         mon,
                         sched_queue_modified_cond_lock_,
-                        0);
+                        -1);
 
       if (this->shutdown_flagged_)
         break;
@@ -130,7 +130,7 @@
       ACE_GUARD_RETURN (ACE_SYNCH_RECURSIVE_MUTEX,
                         synch_lock_mon,
                         this->synch_lock_,
-                        0);
+                        -1);
 
       if (this->ready_queue_.current_size () <= 0)
         continue;
@@ -246,7 +246,7 @@
   //@@ Perhaps the lock could be moved further down just before
   //setting the condition variable?
   ACE_GUARD_RETURN (cond_lock_t,
-                    mon, this->sched_queue_modified_cond_lock_, 0);
+                    mon, this->sched_queue_modified_cond_lock_, -1);
 
 #ifdef KOKYU_DSRT_LOGGING
   ACE_DEBUG ((LM_DEBUG,
@@ -286,7 +286,7 @@
   //@@ Perhaps the lock could be got rid of. It looks like the state
   //of this object is not getting modified here. It makes calls to
   //other methods, which already are thread-safe.
-  //ACE_Guard<cond_lock_t> mon(sched_queue_modified_cond_lock_);
+  //ACE_GUARD_REACTION(cond_lock_t, mon, sched_queue_modified_cond_lock_, ACE_UNEXPECTED(-1));
 
   int found = this->ready_queue_.find (guid, dispatch_item);
   if (found == 0 && flag == BLOCK)
@@ -349,7 +349,7 @@
   guard.release ();
 
   ACE_GUARD_RETURN (cond_lock_t,
-                    mon, this->sched_queue_modified_cond_lock_, 0);
+                    mon, this->sched_queue_modified_cond_lock_, -1);
   this->sched_queue_modified_ = 1;
   this->sched_queue_modified_cond_.signal ();
   return 0;
@@ -361,7 +361,7 @@
 {
   this->shutdown_flagged_ = 1;
 
-  ACE_Guard<cond_lock_t> mon(this->sched_queue_modified_cond_lock_);
+  ACE_GUARD_REACTION(cond_lock_t, mon, this->sched_queue_modified_cond_lock_, ACE_UNEXPECTED(-1));
   this->sched_queue_modified_ = 1;
   this->sched_queue_modified_cond_.signal ();
   // We have to wait until the scheduler executive thread shuts
Index: tests/MT_Reference_Counted_Event_Handler_Test.cpp
===================================================================
--- tests/MT_Reference_Counted_Event_Handler_Test.cpp	(revision 623)
+++ tests/MT_Reference_Counted_Event_Handler_Test.cpp	(working copy)
@@ -639,7 +639,7 @@
 void
 Connection_Cache::add_connection (Sender *sender)
 {
-  ACE_GUARD (ACE_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   // Make sure that the state of the connection cache is as
   // expected. <sender> should not be already in the cache.
@@ -656,7 +656,7 @@
 void
 Connection_Cache::remove_connection (Sender *sender)
 {
-  ACE_GUARD (ACE_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   // Make sure that the state of the connection cache is as expected.
   // remove_connection() may already have been called.
@@ -700,7 +700,7 @@
 void
 Connection_Cache::release_connection (Sender *sender)
 {
-  ACE_GUARD (ACE_SYNCH_MUTEX, ace_mon, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, ace_mon, this->lock_, ACE_UNEXPECTED(;));
 
   // Make sure that the state of the connection cache is as expected.
   // remove_connection() may have already removed the connection from
Index: tests/Proactor_Test_IPV6.cpp
===================================================================
--- tests/Proactor_Test_IPV6.cpp	(revision 623)
+++ tests/Proactor_Test_IPV6.cpp	(working copy)
@@ -475,7 +475,7 @@
               this->servers_.sessions_up_.value (),
               this->servers_.sessions_down_.value ()));
 
-  ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->list_lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->list_lock_, ACE_UNEXPECTED(;));
   int i;
   for (i = 0; i < MAX_SERVERS; ++i)
     {
@@ -510,7 +510,7 @@
               this->clients_.sessions_up_.value (),
               this->clients_.sessions_down_.value ()));
 
-  ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->list_lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->list_lock_, ACE_UNEXPECTED(;));
   int i;
   for (i = 0; i < MAX_CLIENTS; ++i)
     {
@@ -539,7 +539,7 @@
   // Lock and cancel everything. Then release the lock, possibly allowing
   // cleanups, then grab it again and delete all Servers and Clients.
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->list_lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->list_lock_, ACE_UNEXPECTED(;));
     for (i = 0; i < MAX_CLIENTS; ++i)
       {
         if (this->client_list_[i] != 0)
@@ -553,7 +553,7 @@
       }
   }
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->list_lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->list_lock_, ACE_UNEXPECTED(;));
     for (i = 0; i < MAX_CLIENTS; ++i)
       {
         if (this->client_list_[i] != 0)
@@ -717,7 +717,7 @@
 void
 Server::cancel ()
 {
-  ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
   this->flg_cancel_ = 1;
   this->ws_.cancel ();
@@ -747,7 +747,7 @@
 Server::open (ACE_HANDLE handle, ACE_Message_Block &)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     // Don't buffer serial sends.
     this->handle_ = handle;
@@ -856,7 +856,7 @@
 Server::handle_read_stream (const ACE_Asynch_Read_Stream::Result &result)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_ );
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     ACE_Message_Block & mb = result.message_block ();
 
@@ -956,7 +956,7 @@
 Server::handle_write_stream (const ACE_Asynch_Write_Stream::Result &result)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     ACE_Message_Block & mb = result.message_block ();
 
@@ -1194,7 +1194,7 @@
 void
 Client::cancel ()
 {
-  ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
   this->flg_cancel_ = 1;
   this->ws_.cancel ();
@@ -1275,7 +1275,7 @@
 Client::open (ACE_HANDLE handle, ACE_Message_Block &)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     // Don't buffer serial sends.
     this->handle_ = handle;
@@ -1495,7 +1495,7 @@
 Client::handle_write_stream (const ACE_Asynch_Write_Stream::Result &result)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     ACE_Message_Block & mb = result.message_block ();
 
@@ -1638,7 +1638,7 @@
 Client::handle_read_stream (const ACE_Asynch_Read_Stream::Result &result)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     ACE_Message_Block & mb = result.message_block ();
 
Index: tests/Upgradable_RW_Test.cpp
===================================================================
--- tests/Upgradable_RW_Test.cpp	(revision 623)
+++ tests/Upgradable_RW_Test.cpp	(working copy)
@@ -177,7 +177,13 @@
 #else
         ACE_Guard<ACE_Thread_Mutex> g (mutex);
 #endif /* RW_MUTEX */
+        if (! g.locked())
+          {
+            ACE_UNEXPECTED(-1);
+          }
+
         find_last ();
+
 #if defined (RW_MUTEX)
         if (use_try_upgrade)
           result =
@@ -202,6 +208,10 @@
 #else
           ACE_Guard<ACE_Thread_Mutex> g (mutex);
 #endif /* RW_MUTEX */
+          if (! g.locked())
+            {
+              ACE_UNEXPECTED(-1);
+            }
 
           not_upgraded++;
           find_last ();
@@ -249,6 +259,10 @@
 #else
       ACE_Guard<ACE_Thread_Mutex> g (mutex);
 #endif /* RW_MUTEX */
+      if (! g.locked())
+        {
+          ACE_UNEXPECTED(-1);
+        }
 
       find_last ();
 
@@ -268,6 +282,10 @@
 Time_Calculation::report_time (ACE_Profile_Timer::ACE_Elapsed_Time &elapsed_time)
 {
   ACE_Guard<ACE_Thread_Mutex> g (mutex_);
+  if (! g.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   this->times_.real_time += elapsed_time.real_time;
   this->times_.user_time += elapsed_time.user_time;
Index: tests/TSS_Test_Errno.h
===================================================================
--- tests/TSS_Test_Errno.h	(revision 623)
+++ tests/TSS_Test_Errno.h	(working copy)
@@ -31,12 +31,12 @@
 public:
   Errno()
   {
-    ACE_MT (ACE_GUARD (ACE_Thread_Mutex, ace_Mon, *Errno::lock_));
+    ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_Mon, *Errno::lock_, ACE_UNEXPECTED(;)));
     created_ += 1;
   }
   ~Errno()
   {
-    ACE_MT (ACE_GUARD (ACE_Thread_Mutex, ace_Mon, *Errno::lock_));
+    ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_Mon, *Errno::lock_, ACE_UNEXPECTED(;)));
     deleted_ += 1;
   }
 
@@ -50,7 +50,7 @@
   // with a mutex since it isn't kept in thread-specific storage.
   int flags (void)
   {
-    ACE_MT (ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_Mon, *Errno::lock_, -1));
+    ACE_MT (ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_Mon, *Errno::lock_, ACE_UNEXPECTED(-1)));
 
     return Errno::flags_;
   }
Index: tests/MT_Reactor_Timer_Test.cpp
===================================================================
--- tests/MT_Reactor_Timer_Test.cpp	(revision 623)
+++ tests/MT_Reactor_Timer_Test.cpp	(working copy)
@@ -127,7 +127,7 @@
   long time_tag = static_cast <long> (reinterpret_cast <size_t> (arg));
   ACE_UNUSED_ARG(tv);
 
-  ACE_GUARD_RETURN (ACE_Thread_Mutex, id_lock, this->lock_, 0);
+  ACE_GUARD_REACTION (ACE_Thread_Mutex, id_lock, this->lock_, ACE_UNEXPECTED(0));
 
   ACE_DEBUG ((LM_DEBUG,
               ACE_TEXT ("%T (%t): Timer #%d (id #%d) expired\n"),
Index: tests/Reactors_Test.cpp
===================================================================
--- tests/Reactors_Test.cpp	(revision 623)
+++ tests/Reactors_Test.cpp	(working copy)
@@ -73,7 +73,7 @@
 Test_Task::Test_Task (void)
   : handled_ (0)
 {
-  ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon, recursive_lock);
+  ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon, recursive_lock, ACE_UNEXPECTED(;));
 
   Test_Task::task_count_++;
 
Index: tests/Proactor_Test.cpp
===================================================================
--- tests/Proactor_Test.cpp	(revision 623)
+++ tests/Proactor_Test.cpp	(working copy)
@@ -475,7 +475,7 @@
               this->servers_.sessions_up_.value (),
               this->servers_.sessions_down_.value ()));
 
-  ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->list_lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->list_lock_, ACE_UNEXPECTED(;));
   int i;
   for (i = 0; i < MAX_SERVERS; ++i)
     {
@@ -510,7 +510,7 @@
               this->clients_.sessions_up_.value (),
               this->clients_.sessions_down_.value ()));
 
-  ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->list_lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->list_lock_, ACE_UNEXPECTED(;));
   int i;
   for (i = 0; i < MAX_CLIENTS; ++i)
     {
@@ -539,7 +539,7 @@
   // Lock and cancel everything. Then release the lock, possibly allowing
   // cleanups, then grab it again and delete all Servers and Clients.
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->list_lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->list_lock_, ACE_UNEXPECTED(;));
     for (i = 0; i < MAX_CLIENTS; ++i)
       {
         if (this->client_list_[i] != 0)
@@ -553,7 +553,7 @@
       }
   }
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->list_lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->list_lock_, ACE_UNEXPECTED(;));
     for (i = 0; i < MAX_CLIENTS; ++i)
       {
         if (this->client_list_[i] != 0)
@@ -717,7 +717,7 @@
 void
 Server::cancel ()
 {
-  ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
   this->flg_cancel_ = 1;
   this->ws_.cancel ();
@@ -747,7 +747,7 @@
 Server::open (ACE_HANDLE handle, ACE_Message_Block &)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     // Don't buffer serial sends.
     this->handle_ = handle;
@@ -856,7 +856,7 @@
 Server::handle_read_stream (const ACE_Asynch_Read_Stream::Result &result)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_ );
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_ , ACE_UNEXPECTED(;));
 
     ACE_Message_Block & mb = result.message_block ();
 
@@ -956,7 +956,7 @@
 Server::handle_write_stream (const ACE_Asynch_Write_Stream::Result &result)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     ACE_Message_Block & mb = result.message_block ();
 
@@ -1193,7 +1193,7 @@
 void
 Client::cancel ()
 {
-  ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
   this->flg_cancel_ = 1;
   this->ws_.cancel ();
@@ -1235,7 +1235,7 @@
 Client::open (ACE_HANDLE handle, ACE_Message_Block &)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     // Don't buffer serial sends.
     this->handle_ = handle;
@@ -1455,7 +1455,7 @@
 Client::handle_write_stream (const ACE_Asynch_Write_Stream::Result &result)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     ACE_Message_Block & mb = result.message_block ();
 
@@ -1599,7 +1599,7 @@
 Client::handle_read_stream (const ACE_Asynch_Read_Stream::Result &result)
 {
   {
-    ACE_GUARD (ACE_SYNCH_MUTEX, monitor, this->lock_);
+    ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, monitor, this->lock_, ACE_UNEXPECTED(;));
 
     ACE_Message_Block & mb = result.message_block ();
 
Index: tests/TP_Reactor_Test.cpp
===================================================================
--- tests/TP_Reactor_Test.cpp	(revision 623)
+++ tests/TP_Reactor_Test.cpp	(working copy)
@@ -269,6 +269,10 @@
     total_r_  (0)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (this->mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   for (size_t i = 0; i < MAX_RECEIVERS; ++i)
      this->list_receivers_[i] =0;
@@ -287,6 +291,10 @@
   // in all threads
 
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (this->mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   for (size_t i = 0; i < MAX_RECEIVERS; ++i)
     {
@@ -299,6 +307,11 @@
 Acceptor::on_new_receiver (Receiver &rcvr)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (this->mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   this->sessions_++;
   this->list_receivers_[rcvr.index_] = & rcvr;
   ACE_DEBUG ((LM_DEBUG,
@@ -310,6 +323,10 @@
 Acceptor::on_delete_receiver (Receiver &rcvr)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (this->mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   this->sessions_--;
 
@@ -358,6 +375,10 @@
 Acceptor::make_svc_handler (Receiver *&sh)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (this->mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   if (sessions_ >= MAX_RECEIVERS)
     return -1;
@@ -422,6 +443,10 @@
 Receiver::open (void *)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   ACE_Reactor *TPReactor = ACE_Reactor::instance ();
 
@@ -480,6 +505,10 @@
 Receiver::handle_input (ACE_HANDLE h)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   ACE_Message_Block *mb = 0;
   ACE_NEW_RETURN (mb,
@@ -563,6 +592,10 @@
 Receiver::handle_output (ACE_HANDLE h)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   ACE_Time_Value tv = ACE_Time_Value::zero;
   ACE_Message_Block *mb = 0;
@@ -628,6 +661,10 @@
     total_r_  (0)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (this->mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   for (size_t i = 0; i < MAX_SENDERS; ++i)
      this->list_senders_[i] = 0;
@@ -647,6 +684,10 @@
   // in all threads
 
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (this->mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   for (size_t i = 0; i < MAX_SENDERS; ++i)
     {
@@ -659,6 +700,11 @@
 Connector::on_new_sender (Sender & sndr)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (this->mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
+
   this->sessions_++;
   this->list_senders_[sndr.index_] = &sndr;
   ACE_DEBUG ((LM_DEBUG,
@@ -670,6 +716,10 @@
 Connector::on_delete_sender (Sender & sndr)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (this->mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(;);
+    }
 
   this->sessions_--;
   this->total_snd_ += sndr.get_total_snd();
@@ -734,6 +784,10 @@
 Connector::make_svc_handler (Sender * & sh)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (this->mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   if (sessions_ >= MAX_SENDERS)
     return -1;
@@ -800,6 +854,10 @@
 int Sender::open (void *)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   ACE_Reactor * TPReactor = ACE_Reactor::instance ();
 
@@ -893,6 +951,10 @@
 Sender::handle_input (ACE_HANDLE h)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   ACE_Message_Block *mb = 0;
   ACE_NEW_RETURN (mb,
@@ -960,6 +1022,10 @@
 Sender::handle_output (ACE_HANDLE h)
 {
   ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  if (! locker.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   ACE_Time_Value tv = ACE_Time_Value::zero;
   ACE_Message_Block *mb = 0;
Index: tests/Reader_Writer_Test.cpp
===================================================================
--- tests/Reader_Writer_Test.cpp	(revision 623)
+++ tests/Reader_Writer_Test.cpp	(working copy)
@@ -114,6 +114,11 @@
     {
       ACE_OS::sleep (pause);
       ACE_Read_Guard<ACE_RW_Thread_Mutex> g (rw_mutex);
+      if (! g.locked())
+        {
+          ACE_UNEXPECTED(0);
+        }
+
       // int n = ++current_readers;
       // ACE_DEBUG ((LM_DEBUG, ACE_TEXT (" (%t) I'm reader number %d\n"), n));
 
@@ -214,6 +219,10 @@
       ACE_OS::sleep (pause);
 
       ACE_Write_Guard<ACE_RW_Thread_Mutex> g (rw_mutex);
+      if (! g.locked())
+        {
+          ACE_UNEXPECTED(0);
+        }
 
       ++current_writers;
 
Index: apps/JAWS/clients/Caching/ID_Generator.cpp
===================================================================
--- apps/JAWS/clients/Caching/ID_Generator.cpp	(revision 623)
+++ apps/JAWS/clients/Caching/ID_Generator.cpp	(working copy)
@@ -30,7 +30,7 @@
 void
 ACE_ID_Generator::get_serial_id (time_t &t, size_t &s)
 {
-  ACE_MT (ACE_GUARD (ACE_SYNCH_MUTEX, ace_mon, *ACE_ID_Generator::get_lock ()));
+  ACE_MT (ACE_GUARD_REACTION (ACE_SYNCH_MUTEX, ace_mon, *ACE_ID_Generator::get_lock (), ACE_UNEXPECTED(;)));
   ACE_OS::time (&t);
 
   if (t != ACE_ID_Generator::last_time_)
Index: apps/JAWS/server/HTTP_Helpers.cpp
===================================================================
--- apps/JAWS/server/HTTP_Helpers.cpp	(revision 623)
+++ apps/JAWS/server/HTTP_Helpers.cpp	(working copy)
@@ -103,7 +103,7 @@
   {
 
 #if !defined (ACE_HAS_REENTRANT_LIBC)
-    ACE_MT (ACE_Guard<ACE_SYNCH_MUTEX> g (HTTP_Helper::mutex_));
+    ACE_MT (ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, HTTP_Helper::mutex_, ACE_UNEXPECTED((time_t) -1)));
 #endif /* NOT ACE_HAS_REENTRANT_LIBC */
 
     return ACE_OS::mktime (&tms);
@@ -115,7 +115,7 @@
 {
   if (HTTP_Helper::date_string_ == 0)
     {
-      ACE_MT (ACE_Guard<ACE_SYNCH_MUTEX> m (HTTP_Helper::mutex_));
+      ACE_MT (ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, m, HTTP_Helper::mutex_, ACE_UNEXPECTED(0)));
 
       if (HTTP_Helper::date_string_ == 0)
         {
@@ -387,7 +387,7 @@
 {
   if (HTTP_Status_Code::instance_ == 0)
     {
-      ACE_MT (ACE_Guard<ACE_SYNCH_MUTEX> g (lock_));
+      ACE_MT (ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, lock_, ACE_UNEXPECTED(0)));
 
       if (HTTP_Status_Code::instance_ == 0)
         {
Index: apps/JAWS2/JAWS/Reaper.cpp
===================================================================
--- apps/JAWS2/JAWS/Reaper.cpp	(revision 623)
+++ apps/JAWS2/JAWS/Reaper.cpp	(working copy)
@@ -21,7 +21,7 @@
 {
   if (this->waiting_ == 0)
     {
-      ACE_Guard<ACE_SYNCH_MUTEX> g (this->lock_);
+      ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, this->lock_, ACE_UNEXPECTED(-1));
       if (this->waiting_ == 0)
         {
           if (this->activate () == -1)
Index: apps/JAWS2/JAWS/FILE.cpp
===================================================================
--- apps/JAWS2/JAWS/FILE.cpp	(revision 623)
+++ apps/JAWS2/JAWS/FILE.cpp	(working copy)
@@ -38,7 +38,7 @@
 {
   if (this->map_ == 0)
     {
-      ACE_Guard<ACE_SYNCH_MUTEX> g (this->lock_);
+      ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, this->lock_, ACE_UNEXPECTED(0));
 
       if (this->map_ == 0)
         {
Index: apps/JAWS2/JAWS/Cache_Manager_T.cpp
===================================================================
--- apps/JAWS2/JAWS/Cache_Manager_T.cpp	(revision 623)
+++ apps/JAWS2/JAWS/Cache_Manager_T.cpp	(working copy)
@@ -356,6 +356,10 @@
 ::GET (const KEY &key, JAWS_Cache_Object *&object)
 {
   ACE_Read_Guard<ACE_SYNCH_RW_MUTEX> g (this->lock_);
+  if (! g.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   return this->GET_i (key, object);
 }
@@ -365,6 +369,10 @@
 ::PUT (const KEY &key, const void *data, size_t size, JAWS_Cache_Object *&obj)
 {
   ACE_Write_Guard<ACE_SYNCH_RW_MUTEX> g (this->lock_);
+  if (! g.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   return this->PUT_i (key, data, size, obj);
 }
@@ -456,6 +464,10 @@
   if (obj->size ()/1024 > this->maxobjsize_)
     {
       ACE_Write_Guard<ACE_SYNCH_RW_MUTEX> g (this->lock_);
+      if (! g.locked())
+        {
+          ACE_UNEXPECTED(-1);
+        }
 
       int result = obj->release ();
       if (result == 0)
@@ -486,6 +498,10 @@
 
   {
     ACE_Write_Guard<ACE_SYNCH_RW_MUTEX> g (this->lock_);
+    if (! g.locked())
+      {
+        ACE_UNEXPECTED(-1);
+      }
 
     int result = obj->release ();
 
@@ -514,6 +530,10 @@
 ::FLUSH (void)
 {
   ACE_Write_Guard<ACE_SYNCH_RW_MUTEX> g (this->lock_);
+  if (! g.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
 
   return this->FLUSH_i ();
 }
Index: apps/JAWS2/JAWS/Cache_Object.cpp
===================================================================
--- apps/JAWS2/JAWS/Cache_Object.cpp	(revision 623)
+++ apps/JAWS2/JAWS/Cache_Object.cpp	(working copy)
@@ -173,7 +173,7 @@
 unsigned int
 JAWS_Counted_Cache_Object::count_i (void) const
 {
-  ACE_Guard<ACE_SYNCH_MUTEX> g (this->lock_);
+  ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, this->lock_, ACE_UNEXPECTED(-1));
 
   return this->count_;
 }
@@ -181,7 +181,7 @@
 int
 JAWS_Counted_Cache_Object::acquire_i (void)
 {
-  ACE_Guard<ACE_SYNCH_MUTEX> g (this->lock_);
+  ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, this->lock_, ACE_UNEXPECTED(-1));
 
   this->new_count_++;
   return 0;
@@ -190,7 +190,7 @@
 int
 JAWS_Counted_Cache_Object::release_i (void)
 {
-  ACE_Guard<ACE_SYNCH_MUTEX> g (this->lock_);
+  ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, this->lock_, ACE_UNEXPECTED(-1));
 
   this->new_count_--;
   this->count_ = this->new_count_;
Index: apps/JAWS2/JAWS/Cache_Hash_T.cpp
===================================================================
--- apps/JAWS2/JAWS/Cache_Hash_T.cpp	(revision 623)
+++ apps/JAWS2/JAWS/Cache_Hash_T.cpp	(working copy)
@@ -139,7 +139,7 @@
 
   if (this->hashtable_[hash_idx] == 0)
     {
-      ACE_Guard<ACE_SYNCH_MUTEX> g (this->lock_);
+      ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, this->lock_, ACE_UNEXPECTED(-1));
 
       if (this->new_cachebucket (hash_idx) == -1)
         return -1;
@@ -161,7 +161,7 @@
 
   if (this->hashtable_[hash_idx] == 0)
     {
-      ACE_Guard<ACE_SYNCH_MUTEX> g (this->lock_);
+      ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, this->lock_, ACE_UNEXPECTED(-1));
 
       if (this->new_cachebucket (hash_idx) == -1)
         return -1;
@@ -185,7 +185,7 @@
 
   if (this->hashtable_[hash_idx] == 0)
     {
-      ACE_Guard<ACE_SYNCH_MUTEX> g (this->lock_);
+      ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, this->lock_, ACE_UNEXPECTED(-1));
 
       if (this->new_cachebucket (hash_idx) == -1)
         return -1;
Index: apps/JAWS2/JAWS/Concurrency.cpp
===================================================================
--- apps/JAWS2/JAWS/Concurrency.cpp	(revision 623)
+++ apps/JAWS2/JAWS/Concurrency.cpp	(working copy)
@@ -30,7 +30,7 @@
 {
   if (this->mb_acquired_ == 0)
     {
-      ACE_Guard<ACE_SYNCH_MUTEX> g(this->lock_);
+      ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, this->lock_, ACE_UNEXPECTED(0));
 
       if (this->mb_acquired_ == 0)
         {
Index: apps/JAWS3/jaws3/FILE.cpp
===================================================================
--- apps/JAWS3/jaws3/FILE.cpp	(revision 623)
+++ apps/JAWS3/jaws3/FILE.cpp	(working copy)
@@ -52,7 +52,7 @@
 
   if (this->map_ == 0)
     {
-      ACE_Guard<ACE_SYNCH_MUTEX> g (this->lock_);
+      ACE_GUARD_REACTION(ACE_SYNCH_MUTEX, g, this->lock_, ACE_UNEXPECTED(0));
 
       if (this->map_ == 0)
         {
Index: netsvcs/lib/Log_Message_Receiver.cpp
===================================================================
--- netsvcs/lib/Log_Message_Receiver.cpp	(revision 623)
+++ netsvcs/lib/Log_Message_Receiver.cpp	(working copy)
@@ -92,12 +92,15 @@
 #  if !defined (ACE_LACKS_STATIC_DATA_MEMBER_TEMPLATES)
   Guard guard (copy_lock_);
   if (guard.locked () == 0)
-    return;
+    {
+      ACE_UNEXPECTED(;);
+    }
 #  else
   // Use the "body"s print lock as copy lock.
-  ACE_GUARD (ACE_SYNCH_MUTEX,
+  ACE_GUARD_REACTION (ACE_SYNCH_MUTEX,
              guard,
-             global_copy_lock_);
+                      global_copy_lock_,
+                      ACE_UNEXPECTED(;));
 #  endif /* ACE_LACKS_STATIC_DATA_MEMBER_TEMPLATES */
 #endif /* ACE_HAS_THREADS */
   if (body->count_-- == 0)
Index: contrib/utility/Utility/ReferenceCounting/ExternalLockImpl.ipp
===================================================================
--- contrib/utility/Utility/ReferenceCounting/ExternalLockImpl.ipp	(revision 623)
+++ contrib/utility/Utility/ReferenceCounting/ExternalLockImpl.ipp	(working copy)
@@ -41,6 +41,10 @@
       if (lock_)
       {
         Guard_ guard (*lock_);
+        if (! guard.locked())
+        {
+          ACE_UNEXPECTED(0);
+        }
         _add_ref_i ();
       }
       else
@@ -58,6 +62,10 @@
         if (lock_)
         {
           Guard_ guard (*lock_);
+          if (! guard.locked())
+          {
+            ACE_UNEXPECTED(;);
+          }
           destroy = _remove_ref_i ();
         }
         else
@@ -75,6 +83,10 @@
       if (lock_)
       {
         Guard_ guard (*lock_);
+        if (! guard.locked())
+        {
+          ACE_UNEXPECTED(0);
+        }
         return _refcount_value_i ();
       }
       else
Index: examples/APG/ThreadSafety/Mutex.cpp
===================================================================
--- examples/APG/ThreadSafety/Mutex.cpp	(revision 623)
+++ examples/APG/ThreadSafety/Mutex.cpp	(working copy)
@@ -35,7 +35,7 @@
 public:
   void log (LogMessage *msg)
   {
-    ACE_GUARD (MUTEX, mon, mutex_);
+    ACE_GUARD_REACTION (MUTEX, mon, mutex_, ACE_UNEXPECTED(;));
     if (msg->priority () == LogMessage::CRITICAL)
       logCritical (msg);
   }
@@ -43,7 +43,7 @@
   void logCritical (LogMessage *)
   {
     // Acquires the same mutex as log()!
-    ACE_GUARD(MUTEX, mon, mutex_);
+    ACE_GUARD_REACTION (MUTEX, mon, mutex_, ACE_UNEXPECTED(;));
   }
 
 private:
Index: examples/APG/Threads/Guards.cpp
===================================================================
--- examples/APG/Threads/Guards.cpp	(revision 623)
+++ examples/APG/Threads/Guards.cpp	(working copy)
@@ -52,6 +52,18 @@
   // as a parameter.
   ACE_Guard<ACE_Thread_Mutex> guard (this->mutex_);
 
+  /* 'guard' is successfully constructed even if the acquisition of
+   *   this->mutex_ fails!
+   * Therefore, it is necessary to confirm that the lock was acquired:
+   */
+  if (! guard.locked())
+    {
+      ACE_UNEXPECTED(-1);
+    }
+  /* Another way to accomplish the above check is with the
+   * ACE_GUARD_REACTION() macro.
+   */
+
   // This can throw an exception that is not caught here.
   ACE_NEW_RETURN (object, Object, -1);
   // ..
Index: examples/Threads/reader_writer.cpp
===================================================================
--- examples/Threads/reader_writer.cpp	(revision 623)
+++ examples/Threads/reader_writer.cpp	(working copy)
@@ -90,6 +90,10 @@
        iterations <= n_iterations; iterations++)
     {
       ACE_Read_Guard<ACE_RW_Mutex> g(rw_mutex);
+      if (! g.locked())
+        {
+          ACE_UNEXPECTED(0);
+        }
 
       ++current_readers;
 
@@ -128,6 +132,10 @@
        iterations++)
     {
       ACE_Write_Guard<ACE_RW_Mutex> g(rw_mutex);
+      if (! g.locked())
+        {
+          ACE_UNEXPECTED(0);
+        }
 
       ++current_writers;
 
Index: examples/Threads/thread_specific.h
===================================================================
--- examples/Threads/thread_specific.h	(revision 623)
+++ examples/Threads/thread_specific.h	(working copy)
@@ -25,14 +25,14 @@
   // with a mutex since it isn't kept in thread-specific storage.
   int flags (void)
   {
-    ACE_GUARD_RETURN (ACE_Thread_Mutex, ace_mon, Errno::lock_, -1);
+    ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_mon, Errno::lock_, ACE_UNEXPECTED(-1));
 
     return Errno::flags_;
   }
 
   void flags (int f)
   {
-    ACE_GUARD (ACE_Thread_Mutex, ace_mon, Errno::lock_);
+    ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_mon, Errno::lock_, ACE_UNEXPECTED(;));
 
     Errno::flags_ = f;
   }
Index: examples/Threads/recursive_mutex.cpp
===================================================================
--- examples/Threads/recursive_mutex.cpp	(revision 623)
+++ examples/Threads/recursive_mutex.cpp	(working copy)
@@ -64,7 +64,7 @@
       {
         // This illustrates the use of the ACE_Guard<LOCK> with an
         // ACE_Recursive_Thread_Mutex.
-        ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon, *rm);
+        ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon, *rm, ACE_UNEXPECTED(;));
         ACE_DEBUG ((LM_DEBUG,
                     "(%P|%t) = acquired, nesting = %d, thread id = %u\n",
         rm->get_nesting_level (), rm->get_thread_id ()));
Index: examples/Threads/task_three.cpp
===================================================================
--- examples/Threads/task_three.cpp	(revision 623)
+++ examples/Threads/task_three.cpp	(working copy)
@@ -60,7 +60,7 @@
 
 Test_Task::Test_Task (void)
 {
-  ACE_GUARD (ACE_Thread_Mutex, ace_mon, Lock);
+  ACE_GUARD_REACTION (ACE_Thread_Mutex, ace_mon, Lock, ACE_UNEXPECTED(;));
 
   this->handled_ = 0;
   Test_Task::current_count_++;
Index: examples/Reactor/Proactor/test_proactor2.cpp
===================================================================
--- examples/Reactor/Proactor/test_proactor2.cpp	(revision 623)
+++ examples/Reactor/Proactor/test_proactor2.cpp	(working copy)
@@ -157,14 +157,14 @@
   : handle_ (ACE_INVALID_HANDLE),
     nIOCount ( 0 )
 {
-  ACE_Guard<MyMutex> locker (m_Mtx) ;
+  ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(;));
   nSessions ++ ;
   ACE_DEBUG ((LM_DEBUG, "Receiver Ctor nSessions=%d\n", nSessions ));
 }
 
 Receiver::~Receiver (void)
 {
-  ACE_Guard<MyMutex> locker (m_Mtx) ;
+  ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(;));
   nSessions -- ;
   ACE_OS::closesocket (this->handle_);
   ACE_DEBUG ((LM_DEBUG, "~Receiver Dtor nSessions=%d\n", nSessions ));
@@ -177,7 +177,7 @@
 bool Receiver::check_destroy ()
 {
   {
-    ACE_Guard<MyMutex> locker (m_Mtx) ;
+    ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(true));
 
     if ( nIOCount > 0 )
       {
@@ -225,7 +225,7 @@
 
 int Receiver::initiate_read_stream (void)
 {
-  ACE_Guard<MyMutex> locker (m_Mtx) ;
+  ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(-1));
 
   // Create a new <Message_Block>.  Note that this message block will
   // be used both to <read> data asynchronously from the socket and to
@@ -255,7 +255,7 @@
 
 int Receiver::initiate_write_stream (ACE_Message_Block & mb, int nBytes )
 {
-  ACE_Guard<MyMutex> locker (m_Mtx) ;
+  ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(-1));
   if (this->ws_.write (mb , nBytes ) == -1)
     {
       mb.release ();
@@ -319,7 +319,7 @@
     }
 
   {
-    ACE_Guard<MyMutex> locker (m_Mtx) ;
+    ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(;));
     nIOCount-- ;
   }
   check_destroy () ;
@@ -359,7 +359,7 @@
     }
 
   {
-    ACE_Guard<MyMutex> locker (m_Mtx) ;
+    ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(;));
     nIOCount-- ;
   }
   check_destroy () ;
@@ -490,9 +490,8 @@
 
 int Sender::initiate_write_stream (void)
 {
-  ACE_Guard<MyMutex> locker (m_Mtx) ;
+  ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(-1));
 
-
   welcome_message_.rd_ptr( welcome_message_.base ());
   welcome_message_.wr_ptr( welcome_message_.base ());
   welcome_message_.wr_ptr (ACE_OS::strlen (data));
@@ -513,7 +512,7 @@
 
 int Sender::initiate_read_stream (void)
 {
-  ACE_Guard<MyMutex> locker (m_Mtx) ;
+  ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(-1));
 
   // Create a new <Message_Block>.  Note that this message block will
   // be used both to <read> data asynchronously from the socket and to
@@ -583,7 +582,7 @@
     }
 
   {
-    ACE_Guard<MyMutex> locker (m_Mtx) ;
+    ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(;));
     nIOCount-- ;
   }
 }
@@ -633,7 +632,7 @@
     }
 
   {
-    ACE_Guard<MyMutex> locker (m_Mtx) ;
+    ACE_GUARD_REACTION(MyMutex, locker, m_Mtx, ACE_UNEXPECTED(;));
     nIOCount-- ;
   }
 }
Index: examples/Reactor/Proactor/test_proactor3.cpp
===================================================================
--- examples/Reactor/Proactor/test_proactor3.cpp	(revision 623)
+++ examples/Reactor/Proactor/test_proactor3.cpp	(working copy)
@@ -111,7 +111,7 @@
 void
 MyTask::create_proactor (void)
 {
-  ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(;));
 
   if (threads_ == 0)
     {
@@ -158,7 +158,7 @@
 void
 MyTask::delete_proactor (void)
 {
-  ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(;));
   if (--threads_ == 0)
     {
       ACE_DEBUG ((LM_DEBUG, "(%t) Delete Proactor\n"));
@@ -230,14 +230,14 @@
   : handle_ (ACE_INVALID_HANDLE),
     io_count_ (0)
 {
-  ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(;));
   sessions_++;
   ACE_DEBUG ((LM_DEBUG, "Receiver Ctor sessions_=%d\n", sessions_));
 }
 
 Receiver::~Receiver (void)
 {
-  ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(;));
   sessions_--;
   ACE_OS::closesocket (this->handle_);
   ACE_DEBUG ((LM_DEBUG, "~Receiver Dtor sessions_=%d\n", sessions_));
@@ -248,7 +248,7 @@
 Receiver::check_destroy (void)
 {
   {
-    ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+    ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(1));
 
     if (io_count_ > 0)
       return 1;
@@ -284,7 +284,7 @@
 int
 Receiver::initiate_read_stream (void)
 {
-  ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(-1));
 
   ACE_Message_Block *mb = 0;
   ACE_NEW_RETURN (mb,
@@ -308,7 +308,7 @@
 int
 Receiver::initiate_write_stream (ACE_Message_Block &mb, int nbytes)
 {
-  ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(-1));
   if (nbytes <= 0)
     {
       mb.release ();
@@ -375,7 +375,7 @@
     }
 
   {
-    ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+    ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(;));
     io_count_--;
   }
   check_destroy ();
@@ -412,7 +412,7 @@
     }
 
   {
-    ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+    ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(;));
     io_count_--;
   }
   check_destroy ();
@@ -537,7 +537,7 @@
 int
 Sender::initiate_write_stream (void)
 {
-  ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(-1));
 
   welcome_message_.rd_ptr(welcome_message_.base ());
   welcome_message_.wr_ptr(welcome_message_.base ());
@@ -556,7 +556,7 @@
 int
 Sender::initiate_read_stream (void)
 {
-  ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+  ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(-1));
 
   // Create a new <Message_Block>.  Note that this message block will
   // be used both to <read> data asynchronously from the socket and to
@@ -615,7 +615,7 @@
     }
 
   {
-    ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+    ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(;));
     io_count_--;
   }
 }
@@ -659,7 +659,7 @@
     }
 
   {
-    ACE_Guard<ACE_Recursive_Thread_Mutex> locker (mutex_);
+    ACE_GUARD_REACTION(ACE_Recursive_Thread_Mutex, locker, mutex_, ACE_UNEXPECTED(;));
     io_count_--;
   }
 }
Index: examples/Reactor/Misc/test_reactors.cpp
===================================================================
--- examples/Reactor/Misc/test_reactors.cpp	(revision 623)
+++ examples/Reactor/Misc/test_reactors.cpp	(working copy)
@@ -49,7 +49,7 @@
 Test_Task::Test_Task (void)
   : handled_ (0)
 {
-  ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon, reclock_);
+  ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon, reclock_, ACE_UNEXPECTED(;));
 
   Test_Task::task_count_++;
   ACE_DEBUG ((LM_DEBUG,
@@ -59,7 +59,7 @@
 
 Test_Task::~Test_Task (void)
 {
-  ACE_GUARD (ACE_Recursive_Thread_Mutex, ace_mon, reclock_);
+  ACE_GUARD_REACTION (ACE_Recursive_Thread_Mutex, ace_mon, reclock_, ACE_UNEXPECTED(;));
 
   ACE_DEBUG ((LM_DEBUG,
               "(%t) TT- Test_Task::task_count_ = %d\n",
