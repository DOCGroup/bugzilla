Only in /home/obrunet/work/ACE_wrappers/ace: ace_wchar.cpp
diff -b -d -U 3 ./ace_wchar.h /home/obrunet/work/ACE_wrappers/ace/ace_wchar.h
--- ./ace_wchar.h	2006-01-09 21:08:07.000000000 +0100
+++ /home/obrunet/work/ACE_wrappers/ace/ace_wchar.h	2006-03-16 10:33:32.000000000 +0100
@@ -81,6 +81,11 @@
 # endif /* ACE_HAS_STANDARD_CPP_LIBRARY */
 #endif /* ACE_HAS_WCHAR */
 
+#if defined (ACE_HAS_ICONV)
+#  include /**/ <iconv.h>
+#define ACE_MAX_ICONV_BUFFER	16*1024
+#endif /* ACE_HAS_ICONV */
+
 #if defined (ACE_USES_STD_NAMESPACE_FOR_STDC_LIB) && \
             (ACE_USES_STD_NAMESPACE_FOR_STDC_LIB != 0)
 using std::size_t;
@@ -193,6 +198,10 @@
   /// Internal pointer to the converted string.
   char *s_;
 
+#if defined (ACE_HAS_ICONV)
+  static iconv_t ACE_Wide_To_Ascii_iconv_env;
+#endif /* ACE_HAS_ICONV */
+
   /// Disallow these operation.
   ACE_Wide_To_Ascii (void);
   ACE_Wide_To_Ascii (ACE_Wide_To_Ascii &);
@@ -227,6 +236,10 @@
   /// Internal pointer to the converted string.
   wchar_t *s_;
 
+#if defined (ACE_HAS_ICONV)
+  static iconv_t ACE_Ascii_To_Wide_iconv_env;
+#endif /* ACE_HAS_ICONV */
+
   /// Disallow these operation.
   ACE_Ascii_To_Wide (void);
   ACE_Ascii_To_Wide (ACE_Ascii_To_Wide &);
diff -b -d -U 3 ./ace_wchar.inl /home/obrunet/work/ACE_wrappers/ace/ace_wchar.inl
--- ./ace_wchar.inl	2006-02-11 17:38:26.000000000 +0100
+++ /home/obrunet/work/ACE_wrappers/ace/ace_wchar.inl	2006-03-16 10:33:19.000000000 +0100
@@ -52,13 +52,25 @@
   size_t len = ::wcslen (wstr) + 1;
 # endif /* ACE_WIN32 */
 
+#if !defined (ACE_HAS_ICONV)
   char *str = new char[len];
+#endif
 
 # if defined (ACE_WIN32)
   ::WideCharToMultiByte (cp, 0, wstr, -1, str, len, 0, 0);
 # elif defined (VXWORKS)
   ::wcstombs (str, wstr, len);
-# else /* ACE_WIN32 */
+# elif defined (ACE_HAS_ICONV) /* ACE_VXWORKS */
+  wchar_t *wstri=const_cast<wchar_t*>(wstr); 
+  size_t lensi=ACE_MAX_ICONV_BUFFER,lenwi=len*sizeof(wchar_t);
+  char buf[ACE_MAX_ICONV_BUFFER];
+  char *stri=buf;
+  
+  if(iconv (ACE_Wide_To_Ascii_iconv_env, (char**)&wstri, &lenwi, &stri, &lensi)==-1)
+	return NULL;
+  char *str=new char[ACE_MAX_ICONV_BUFFER-lensi];
+  ::memcpy(str,buf,ACE_MAX_ICONV_BUFFER-lensi);  
+# else /* ACE_HAS_ICONV */
   for (size_t i = 0; i < len; i++)
     {
       wchar_t *t = const_cast <wchar_t *> (wstr);
@@ -70,8 +82,12 @@
 
 inline
 ACE_Wide_To_Ascii::ACE_Wide_To_Ascii (const wchar_t *s)
-  : s_ (ACE_Wide_To_Ascii::convert (s))
 {
+#if defined(ACE_HAS_ICONV)
+	if(ACE_Wide_To_Ascii_iconv_env==NULL)
+		ACE_Wide_To_Ascii_iconv_env=iconv_open("","WCHAR_T");
+#endif
+	s_=ACE_Wide_To_Ascii::convert (s);
 }
 
 inline
@@ -100,13 +116,25 @@
   size_t len = strlen (str) + 1;
 # endif /* ACE_WIN32 */
 
+#if !defined (ACE_HAS_ICONV)
   wchar_t *wstr = new wchar_t[len];
+#endif
 
 # if defined (ACE_WIN32)
   ::MultiByteToWideChar (cp, 0, str, -1, wstr, len);
 # elif defined (VXWORKS)
   ::mbstowcs (wstr, str, len);
-# else /* ACE_WIN32 */
+# elif defined (ACE_HAS_ICONV) /* ACE_VXWORKS */
+  char *stri=const_cast<char*>(str);
+  size_t lensi=len,lenwi=ACE_MAX_ICONV_BUFFER;
+  wchar_t buf[ACE_MAX_ICONV_BUFFER/sizeof(wchar_t)];
+  wchar_t *wstri=buf;
+
+  if(iconv(ACE_Ascii_To_Wide_iconv_env, &stri, &lensi, (char**)&wstri, &lenwi)==-1)
+	return NULL;
+  wchar_t *wstr=new wchar_t[(ACE_MAX_ICONV_BUFFER-lenwi)/sizeof(wchar_t)];
+  ::memcpy(wstr,buf,ACE_MAX_ICONV_BUFFER-lenwi);
+# else /* ACE_HAS_ICONV */
   for (size_t i = 0; i < len; i++)
     {
       char *t = const_cast<char *> (str);
@@ -118,8 +146,12 @@
 
 inline
 ACE_Ascii_To_Wide::ACE_Ascii_To_Wide (const char *s)
-  : s_ (ACE_Ascii_To_Wide::convert (s))
 {
+#if defined(ACE_HAS_ICONV)
+	if(ACE_Ascii_To_Wide_iconv_env==NULL)
+		ACE_Ascii_To_Wide_iconv_env=iconv_open("WCHAR_T","");
+#endif
+	s_=ACE_Ascii_To_Wide::convert (s);
 }
 
 ACE_END_VERSIONED_NAMESPACE_DECL
diff -b -d -U 3 ./GNUmakefile.ACE /home/obrunet/work/ACE_wrappers/ace/GNUmakefile.ACE
--- ./GNUmakefile.ACE	2006-03-05 18:23:27.000000000 +0100
+++ /home/obrunet/work/ACE_wrappers/ace/GNUmakefile.ACE	2006-03-13 11:23:30.000000000 +0100
@@ -320,7 +320,8 @@
   WFMO_Reactor.cpp \
   WIN32_Asynch_IO.cpp \
   WIN32_Proactor.cpp \
-  XTI_ATM_Mcast.cpp
+  XTI_ATM_Mcast.cpp \
+  ace_wchar.cpp
 
 #----------------------------------------------------------------------------
 #       Include macros and targets
