diff -Nru ACE_wrappers/ace/High_Res_Timer.cpp ACE_wrappers-patched-5.4.8/ace/High_Res_Timer.cpp
--- ACE_wrappers/ace/High_Res_Timer.cpp	2005-11-24 11:48:54.000000000 +0200
+++ ACE_wrappers-patched-5.4.8/ace/High_Res_Timer.cpp	2005-12-27 10:02:57.000000000 +0200
@@ -73,8 +73,10 @@
 /* static */
 int ACE_High_Res_Timer::global_scale_factor_status_ = 0;
 
-
 #if defined (linux)
+/* static */
+int ACE_High_Res_Timer::cpu_smp_ = 0;
+
 // Determine the apparent CPU clock speed from /proc/cpuinfo
 ACE_UINT32
 ACE_High_Res_Timer::get_cpuinfo (void)
@@ -257,6 +259,12 @@
 
   this->reset ();
 
+#if defined (linux)
+  CPU_ZERO (&cpu_mask_saved_);
+  thread_bound_ = 0;
+  (ACE_OS::num_processors () > 1) ? cpu_smp_ = 1 :  cpu_smp_ = 0;
+#endif /* defined (linux) */
+
   // Make sure that the global scale factor is set.
   (void) global_scale_factor ();
 }
@@ -277,10 +285,18 @@
       const ACE_Time_Value actual_start =
         ACE_OS::gettimeofday ();
       const ACE_hrtime_t start =
-        ACE_OS::gethrtime ();
+	 ACE_OS::gethrtime (ACE_OS::ACE_HRTIMER_GETTIME
+#if defined (linux)
+		 , ACE_High_Res_Timer::cpu_smp_
+#endif /* defined (linux) */
+		 );
       ACE_OS::sleep (sleep_time);
       const ACE_hrtime_t stop =
-        ACE_OS::gethrtime ();
+        ACE_OS::gethrtime (ACE_OS::ACE_HRTIMER_GETTIME
+#if defined (linux)
+		, ACE_High_Res_Timer::cpu_smp_
+#endif /* defined (linux) */
+		 );
       const ACE_Time_Value actual_delta =
         ACE_OS::gettimeofday () - actual_start;
 
@@ -549,4 +565,73 @@
   return -1;
 }
 
+#if defined (linux)
+int ACE_High_Res_Timer::bind_thread_to_cpu (cpu_set_t cpu_mask)
+{
+  if (ACE_High_Res_Timer::cpu_smp_ == 0)
+  {
+	  // This is useful to have the same code for SMP as well as UP machines.
+	  // On UP CPUs this code will take a doing nothing approach, avoiding
+	  // extra syscalls.
+	  return 0;
+  }
+
+  ACE_OS::thr_self (this->thread_bound_);
+
+  // Works only with linux-threads or NPTL
+  if (ACE_OS::thr_get_affinity (this->thread_bound_, this->cpu_mask_saved_) == -1)
+  {
+	  ACE_DEBUG ((LM_DEBUG, 
+		      ACE_LIB_TEXT ("\nACE_OS::thr_get_affinity failed () to get affinity info.\n")
+		      ACE_LIB_TEXT ("May happen when NPTL thread-id passed to sched_getaffinity ().\n")
+		      ACE_LIB_TEXT ("Trying to get affinity info for the whole process.\n")));
+
+	  this->thread_bound_ = ACE_OS::getpid ();
+
+	  //  Trying to bind the whole process. Works with NPTL as well, 
+	  //  but binds the whole process to a cpu
+	  if (ACE_OS::thr_get_affinity ( this->thread_bound_, this->cpu_mask_saved_) == -1)
+	  {
+		  ACE_ERROR_RETURN ((LM_ERROR, 
+			      ACE_LIB_TEXT ("\nACE_OS::thr_get_affinity failed with errno %d\n")
+			      ACE_LIB_TEXT ("If errno is 0, probably, sched_getaffinity () failed")
+			      ACE_LIB_TEXT (" on kernel 2.4 with the syscall not supported in kernel.\n")
+			      ACE_LIB_TEXT ("Run strace to see ENOSYS - Function not implemented.\n"),
+			      errno), 
+			      -1);
+	  }
+  }
+
+  // Binds either thread or the whole process <thread_bound_>, 
+  // whatever is supported, to the <cpu_mask>. Saves previous mask
+  // in <cpu_mask_saved_>.
+  if (ACE_OS::thr_set_affinity (this->thread_bound_, cpu_mask) == -1)
+  {
+	  ACE_ERROR_RETURN ((LM_ERROR, 
+                    ACE_LIB_TEXT ("\nACE_OS::thr_set_affinity failed with errno %d\n")
+		     ACE_LIB_TEXT ("If errno is 0, probably, sched_setaffinity () failed")
+		     ACE_LIB_TEXT (" on kernel 2.4 with the syscall not supported in kernel.\n")
+		     ACE_LIB_TEXT ("Run strace to see ENOSYS - Function not implemented.\n"),
+		     errno),
+                    -1);
+  }
+
+  return 0;
+}
+
+int ACE_High_Res_Timer::unbind_thread_from_cpu ()
+{
+  // Restores cpu mask to the status prior to bind_thread_to_cpu ()
+  if (ACE_OS::thr_set_affinity (thread_bound_, this->cpu_mask_saved_) == -1)
+  {
+	  ACE_ERROR_RETURN ((LM_ERROR, 
+		     ACE_LIB_TEXT ("ACE_OS::thr_set_affinity failed with errno %d\n"),
+		     errno), 
+                    -1);
+  }
+
+  return 0;
+}
+#endif /* defined (linux) */
+
 ACE_END_VERSIONED_NAMESPACE_DECL
diff -Nru ACE_wrappers/ace/High_Res_Timer.h ACE_wrappers-patched-5.4.8/ace/High_Res_Timer.h
--- ACE_wrappers/ace/High_Res_Timer.h	2005-10-28 18:14:52.000000000 +0200
+++ ACE_wrappers-patched-5.4.8/ace/High_Res_Timer.h	2005-12-27 08:32:13.000000000 +0200
@@ -23,6 +23,7 @@
 #include "ace/Basic_Types.h"
 #include "ace/OS_NS_time.h"
 #include "ace/Time_Value.h"
+#include "ace/OS_NS_Thread.h"
 
 ACE_BEGIN_VERSIONED_NAMESPACE_DECL
 
@@ -89,9 +90,17 @@
  * (laptops for example, but probably also embedded devices),
  * do change the cycle rate while running.
  * Some pentiums can run on (at least) two clock frequency's.
- * Another problem arises with multiprocessor computers, there
- * are reports that the different RDTSC's are not always kept
- * in sync.
+ *
+ * Another problem arises with multiprocessor computers, where
+ * readings of RDTSC counter may occur from different CPUs
+ * or being corrupted in shared cacheline on hyper-threding Intel chipsets.
+ * The problem is addressed on linux by binding thread, containing 
+ * ACE_High_Res_Timer object, to a chosen single CPU, which is enough
+ * for AMD SMP systems, and adding for Intel chipsets with potential 
+ * hyperthreading a one more serializing instruction prior and 
+ * after RDTSC to force memory writing. Use bind_thread_to_cpu () and
+ * unbind_thread_from_cpu () to address the issues.
+ *
  * A windows "timer chip" NOTE: (8254-compatible real-time clock)
  * When ::QueryPerformanceCounter() uses the 8254 it has a
  * frequency off about 1.193 Mhz (or sometimes 3.579 Mhz?) and
@@ -268,6 +277,28 @@
    * (the default).
    */
   static ACE_UINT32 get_cpuinfo (void);
+
+  /**
+  * Binds current thread to a chosen single CPU.
+  * <cpu_mask> is a bitmask of CPUs to bind to, e.g value 1 binds the 
+  * thread to the "CPU 0", 3 to the "CPU 1", etc. make sure, that you
+  * are binding the timer thread to a single CPU.
+  * Use CPU_ZERO and CPU_SET macros from <sched.h> to bind
+  * to a chosen single CPU.
+  *
+  * By itself it is enough for RDTSC-based High_Res_Timer to work 
+  * properly on linux with AMD chipsets. In the case of Intel HW with 
+  * hyperthreading,  the result of rdtsc instruction should be forced 
+  * to be written to memory in order not to be corrupted by being kept 
+  * in shared by the hyper-threads cacheline. 
+  */
+  int bind_thread_to_cpu (cpu_set_t cpu_mask);
+
+  /**
+  * Unbinds current thread from a chosen single CPU, restoring previous
+  * CPU-mask
+  */
+  int unbind_thread_from_cpu ();
 #endif /* defined (linux) */
 
 private:
@@ -299,6 +330,14 @@
   /// Start time of incremental timing.
   ACE_hrtime_t start_incr_;
 
+#if defined (linux)
+  /// Saved cpu-mask to be used by unbind_thread_from_cpu ()
+  cpu_set_t cpu_mask_saved_;
+
+  ///  Kept thread-id or pid-id of whatever is bound to a CPU
+  ACE_hthread_t thread_bound_;
+ #endif /* defined (linux) */
+
   /// Converts ticks to microseconds.  That is, ticks /
   /// global_scale_factor_ == microseconds.
   static ACE_UINT32 global_scale_factor_;
@@ -310,6 +349,12 @@
    * -1 = HR timer not supported
    */
   static int global_scale_factor_status_;
+
+#if defined (linux)
+  /// 'True', when  linux sees the hw as SMP, 'false', when UP.
+  static int cpu_smp_ ;
+#endif /* defined linux */
+
 };
 
 ACE_END_VERSIONED_NAMESPACE_DECL
diff -Nru ACE_wrappers/ace/High_Res_Timer.inl ACE_wrappers-patched-5.4.8/ace/High_Res_Timer.inl
--- ACE_wrappers/ace/High_Res_Timer.inl	2005-10-28 18:14:52.000000000 +0200
+++ ACE_wrappers-patched-5.4.8/ace/High_Res_Timer.inl	2005-12-27 08:32:13.000000000 +0200
@@ -69,7 +69,7 @@
 
   ACE_Time_Value tv;
   ACE_High_Res_Timer::hrtime_to_tv (tv,
-                                    ACE_OS::gethrtime (op));
+                                    ACE_OS::gethrtime (op, ACE_High_Res_Timer::cpu_smp_));
   return tv;
 }
 
@@ -105,7 +105,7 @@
     }
 #endif /* ACE_WIN32 */
 
-  return ACE_OS::gethrtime (op);
+  return ACE_OS::gethrtime (op, ACE_High_Res_Timer::cpu_smp_);
 }
 
 ACE_INLINE ACE_hrtime_t
diff -Nru ACE_wrappers/ace/os_include/os_sched.h ACE_wrappers-patched-5.4.8/ace/os_include/os_sched.h
--- ACE_wrappers/ace/os_include/os_sched.h	2003-07-19 22:04:15.000000000 +0300
+++ ACE_wrappers-patched-5.4.8/ace/os_include/os_sched.h	2005-12-27 10:09:40.000000000 +0200
@@ -36,6 +36,19 @@
 {
 #endif /* __cplusplus */
 
+#if !defined (ACE_HAS_CPU_SET_T)
+
+#if !defined (__cpu_set_t_defined)
+#define __cpu_set_t_defined
+# define ACE_CPU_SETSIZE	1024
+typedef struct
+{
+  ACE_UINT32 bit_array_[ACE_CPU_SETSIZE / (8 * sizeof (ACE_UINT32)];
+} cpu_set_t;
+
+#endif /* !__cpu_set_t_defined */
+#endif /* !ACE_HAS_CPU_SET_T */
+
 #ifdef __cplusplus
 }
 #endif /* __cplusplus */
diff -Nru ACE_wrappers/ace/OS_NS_Thread.cpp ACE_wrappers-patched-5.4.8/ace/OS_NS_Thread.cpp
--- ACE_wrappers/ace/OS_NS_Thread.cpp	2005-11-24 11:48:54.000000000 +0200
+++ ACE_wrappers-patched-5.4.8/ace/OS_NS_Thread.cpp	2005-12-27 08:32:13.000000000 +0200
@@ -24,6 +24,8 @@
 #include "ace/Guard_T.h"
 
 
+
+
 extern "C" void
 ACE_MUTEX_LOCK_CLEANUP_ADAPTER_NAME (void *args)
 {
@@ -4999,6 +5001,85 @@
 }
 
 int
+ACE_OS::thr_get_affinity (ACE_hthread_t thr_id, cpu_set_t& cpu_mask)
+{
+#if defined (linux)
+
+#if defined (ACE_HAS_PTHREAD_GETAFFINITY_NP)
+
+	// To compile with NPTL add to CFLAGS -I/usr/include/nptl.
+	// To link to NPTL libraries one normally needs -L/usr/lib/nptl, but on my Debian unstable distro
+	// it was not working till I passed as LIBS =  -L/lib/tls -ldl-2.3.5 -lpthread-2.3.5 -lrt-2.3.5 -lc-2.3.5,
+	// where 2.3.5 is the version of NPTL libs located in /lib/tls.
+	
+	// Handle of the thread, which is NPTL thread-id, normally a big number
+	if (::pthread_getaffinity_np (thr_id, 
+								 sizeof (cpu_set_t), 
+								 &cpu_mask) != 0)
+	{
+		return -1;
+	}
+	return 0;
+
+#elif defined (ACE_HAS_SCHED_GETAFFINITY)
+	
+	// The process-id is expected as <thr_id>, which can be a thread-id of linux-thread,
+	// thus making binding to cpu of that particular thread only.
+	// If you are using this flag for NPTL-threads, however, please pass as a thr_id
+	// process id obtained by ACE_OS::getpid ()
+	if (::sched_getaffinity(thr_id, 
+						   sizeof (cpu_set_t),
+						   &cpu_mask) == -1)
+	{
+		return -1;
+	}
+	return 0;
+
+#endif /* linux without support for affinity */
+
+#endif /* linux */
+	ACE_UNUSED_ARG (thr_id);
+	ACE_UNUSED_ARG (cpu_mask);
+	ACE_NOTSUP_RETURN (-1);
+}
+
+int
+ACE_OS::thr_set_affinity (ACE_hthread_t thr_id, cpu_set_t cpu_mask)
+{
+#if defined (linux)
+
+#if defined (ACE_HAS_PTHREAD_SETAFFINITY_NP)
+	if (pthread_setaffinity_np (thr_id, 
+		                                           sizeof (cpu_set_t), 
+							     &cpu_mask) != 0)
+	{
+		return -1;
+	}
+	return 0;
+#elif defined (ACE_HAS_SCHED_SETAFFINITY)
+	// The process-id is expected as <thr_id>, which can be a thread-id of linux-thread,
+	// thus making binding to cpu of that particular thread only.
+	// If you are using this flag for NPTL-threads, however, please pass as a thr_id
+	// process id obtained by ACE_OS::getpid (), but whole process will bind your CPUs
+	//
+	if (sched_setaffinity (thr_id, 
+					        sizeof (cpu_set_t),
+						&cpu_mask) == -1)
+	{
+		return -1;
+	}
+	return 0;
+
+#endif /* linux, without CPU affinity support*/
+
+#endif /* linux */
+
+  ACE_UNUSED_ARG (thr_id);
+  ACE_UNUSED_ARG (cpu_mask);
+  ACE_NOTSUP_RETURN (-1);
+}
+
+int
 ACE_OS::thr_key_used (ACE_thread_key_t key)
 {
 #if defined (ACE_WIN32) || defined (ACE_HAS_TSS_EMULATION) || (defined (ACE_PSOS) && defined (ACE_PSOS_HAS_TSS))
diff -Nru ACE_wrappers/ace/OS_NS_Thread.h ACE_wrappers-patched-5.4.8/ace/OS_NS_Thread.h
--- ACE_wrappers/ace/OS_NS_Thread.h	2005-11-24 11:48:54.000000000 +0200
+++ ACE_wrappers-patched-5.4.8/ace/OS_NS_Thread.h	2005-12-27 08:32:13.000000000 +0200
@@ -36,6 +36,7 @@
 # include "ace/OS_NS_signal.h"
 # include "ace/ACE_export.h"
 # include "ace/Object_Manager_Base.h"
+# include "ace/os_include/os_sched.h"
 
 # if defined (ACE_EXPORT_MACRO)
 #   undef ACE_EXPORT_MACRO
@@ -1744,6 +1745,30 @@
                 ACE_THR_FUNC_RETURN *status);
 
   /**
+  * For NPTL-threads, when ACE_HAS_PTHREAD_GETAFFINITY_NP defined,
+  * <thr_id> is the thread-id.
+  * For linux-threads, when ACE_HAS_SCHED_GETAFFINITY defined,
+  * it expects a process-id. Since for linux-threads a thread is seen as a process, 
+  * it does the proper job.
+   */
+   extern ACE_Export
+  int thr_get_affinity (ACE_hthread_t thr_id, 
+					  cpu_set_t& cpu_mask);
+
+  /**
+  * For NPTL-threads, when ACE_HAS_PTHREAD_SETAFFINITY_NP defined,
+  * <thr_id> is the thread-id.
+  * For linux-threads, when ACE_HAS_SCHED_SETAFFINITY defined,
+  * it expects a process-id. Since for linux-threads a thread is seen as a process, 
+  * it does the poper job.
+  * Note, that <cpu_mask> is a bitmask of CPUs to bind to, e.g value 1 binds 
+  * the thread to the "CPU 0", etc
+  */
+  extern ACE_Export
+  int thr_set_affinity (ACE_hthread_t thr_id, 
+					 cpu_set_t cpu_mask);
+
+  /**
    * @note the "inst" arge is deprecated.  It will be ignored.
    */
   extern ACE_Export
diff -Nru ACE_wrappers/ace/OS_NS_time.h ACE_wrappers-patched-5.4.8/ace/OS_NS_time.h
--- ACE_wrappers/ace/OS_NS_time.h	2005-11-24 11:48:54.000000000 +0200
+++ ACE_wrappers-patched-5.4.8/ace/OS_NS_time.h	2005-12-27 08:32:13.000000000 +0200
@@ -285,7 +285,8 @@
                    time_t t0);
 
   ACE_NAMESPACE_INLINE_FUNCTION
-  ACE_hrtime_t gethrtime (const ACE_HRTimer_Op = ACE_HRTIMER_GETTIME);
+  ACE_hrtime_t gethrtime (const ACE_HRTimer_Op = ACE_HRTIMER_GETTIME,
+	  int cpu_smp = 0);
 
   ACE_NAMESPACE_INLINE_FUNCTION
   struct tm *gmtime (const time_t *clock);
diff -Nru ACE_wrappers/ace/OS_NS_time.inl ACE_wrappers-patched-5.4.8/ace/OS_NS_time.inl
--- ACE_wrappers/ace/OS_NS_time.inl	2005-10-28 18:14:54.000000000 +0200
+++ ACE_wrappers-patched-5.4.8/ace/OS_NS_time.inl	2005-12-27 08:32:13.000000000 +0200
@@ -223,11 +223,12 @@
 #endif /* ghs && ACE_HAS_PENTIUM */
 
 ACE_INLINE ACE_hrtime_t
-ACE_OS::gethrtime (const ACE_HRTimer_Op op)
+ACE_OS::gethrtime (const ACE_HRTimer_Op op, int cpu_smp)
 {
   ACE_OS_TRACE ("ACE_OS::gethrtime");
 #if defined (ACE_HAS_HI_RES_TIMER)
   ACE_UNUSED_ARG (op);
+  ACE_UNUSED_ARG (cpu_smp);
   return ::gethrtime ();
 #elif defined (ACE_HAS_AIX_HI_RES_TIMER)
   ACE_UNUSED_ARG (op);
@@ -239,6 +240,7 @@
   return ACE_hrtime_t(tb.tb_high) * ACE_ONE_SECOND_IN_NSECS + tb.tb_low;
 #elif defined (ghs) && defined (ACE_HAS_PENTIUM) && !defined (ACE_WIN32)
   ACE_UNUSED_ARG (op);
+  ACE_UNUSED_ARG (cpu_smp);
   // Use .obj/gethrtime.o, which was compiled with g++.
   return ACE_GETHRTIME_NAME ();
 #elif (defined(__KCC) || defined (__GNUG__) || defined (__INTEL_COMPILER)) && !defined (ACE_WIN32) && !defined(ACE_VXWORKS) && defined (ACE_HAS_PENTIUM)
@@ -254,7 +256,27 @@
   //
   // Read the high-res tick counter directly into memory variable "now".
   // The A constraint signifies a 64-bit int.
+
+#if defined (__amd64__)
   asm volatile ("rdtsc" : "=A" (now) : : "memory");
+#else /* ! __amd64__ - Intel Chipsets */
+
+  // Intel recommends, that on its hyperthreading chipset 
+  // a serializing instruction (e.g. cpuid) 
+  // will be called before and after rdtsc
+  if (cpu_smp)
+  {
+      // could it be alignment issue here?
+      asm volatile ("xor %%eax, %%eax ;" "cpuid ;" "rdtsc ;" 
+			        "mov %%edx, 4(%0) ;" "mov %%eax, (%0) ;" 
+			        "xor %%eax, %%eax ;" "cpuid":: 
+			        "S" (&(now)): "eax", "ebx", "ecx", "edx", "memory");
+  }
+  else
+  {
+      asm volatile ("rdtsc" : "=A" (now) : : "memory");
+  }
+#endif /* __amd64__*/
 
 # if defined (ACE_LACKS_LONGLONG_T)
   ACE_UINT32 least, most;
@@ -285,6 +307,7 @@
   return now;
 #elif defined (ACE_WIN32)
   ACE_UNUSED_ARG(op);
+  ACE_UNUSED_ARG (cpu_smp);
   LARGE_INTEGER freq;
 
   ::QueryPerformanceCounter (&freq);
@@ -298,6 +321,7 @@
 #  endif //ACE_LACKS_LONGLONG_T
 
 #elif defined (CHORUS)
+  ACE_UNUSED_ARG (cpu_smp);
   if (op == ACE_OS::ACE_HRTIMER_GETTIME)
     {
       struct timespec ts;
@@ -333,6 +357,7 @@
   // PowerPC w/ GreenHills or g++.
 
   ACE_UNUSED_ARG (op);
+  ACE_UNUSED_ARG (cpu_smp);
   u_long most;
   u_long least;
 
@@ -358,6 +383,7 @@
 #elif defined (ACE_HAS_CLOCK_GETTIME) || defined (ACE_PSOS)
   // e.g., VxWorks (besides POWERPC && GreenHills) . . .
   ACE_UNUSED_ARG (op);
+  ACE_UNUSED_ARG (cpu_smp);
   struct timespec ts;
 
   ACE_OS::clock_gettime (CLOCK_REALTIME, &ts);
@@ -368,6 +394,7 @@
     ACE_U_ONE_SECOND_IN_NSECS  +  static_cast<ACE_hrtime_t> (ts.tv_nsec);
 #else
   ACE_UNUSED_ARG (op);
+  ACE_UNUSED_ARG (cpu_smp);
   const ACE_Time_Value now = ACE_OS::gettimeofday ();
 
   // Carefully create the return value to avoid arithmetic overflow
