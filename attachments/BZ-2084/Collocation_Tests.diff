diff -urp -N Collocation_Tests.orig/Client_Task.cpp Collocation_Tests/Client_Task.cpp
--- Collocation_Tests.orig/Client_Task.cpp	2003-03-31 20:21:47.000000000 +0200
+++ Collocation_Tests/Client_Task.cpp	2005-04-06 14:14:52.489862876 +0200
@@ -4,6 +4,9 @@
 
 #include "Client_Task.h"
 #include "TestC.h"
+#include "Hello.h"
+#include <tao/ORB_Core.h>
+#include "tao/PortableServer/PortableServer.h"
 
 ACE_RCSID(Muxing, Client_Task, "Client_Task.cpp,v 1.2 2003/03/31 18:21:47 bala Exp")
 
@@ -22,31 +25,66 @@ Client_Task::svc (void)
 {
   ACE_TRY_NEW_ENV
     {
+
+// DF: In the client, we create a hello (callback) object and register it to the server.
+     CORBA::Object_var poa_object =
+       this->corb_->resolve_initial_references("RootPOA"
+                                               ACE_ENV_ARG_PARAMETER);
+     ACE_TRY_CHECK;
+
+     PortableServer::POA_var root_poa =
+       PortableServer::POA::_narrow (poa_object.in ()
+                                     ACE_ENV_ARG_PARAMETER);
+     ACE_TRY_CHECK;
+
+     if (CORBA::is_nil (root_poa.in ()))
+       ACE_ERROR_RETURN ((LM_ERROR,
+                          " (%P|%t) Panic: nil RootPOA\n"),
+                         1);
+
+     PortableServer::POAManager_var poa_manager =
+       root_poa->the_POAManager (ACE_ENV_SINGLE_ARG_PARAMETER);
+     ACE_TRY_CHECK;
+
+     Hello *hello_impl;
+     ACE_NEW_RETURN (hello_impl,
+                     Hello (this->corb_.in (),
+                            ACE_Thread::self ()),
+                     1);
+
+     PortableServer::ServantBase_var owner_transfer(hello_impl);
+
+     Test::Hello_var hello_servant = hello_impl->_this (ACE_ENV_SINGLE_ARG_PARAMETER);
+     ACE_TRY_CHECK;
+     poa_manager->activate (ACE_ENV_SINGLE_ARG_PARAMETER);
+
+	    ACE_DEBUG((LM_DEBUG,"Client (%t) optimize_collocation_objects=%d use_global_collocation=%d\n",
+				    corb_->orb_core()->optimize_collocation_objects(),
+				    corb_->orb_core()->use_global_collocation ()));
+
       CORBA::Object_var tmp =
 	this->corb_->string_to_object (input_
 				       ACE_ENV_ARG_PARAMETER);
       ACE_TRY_CHECK;
 
-      Test::Hello_var hello =
-	Test::Hello::_narrow(tmp.in () ACE_ENV_ARG_PARAMETER);
+      Test::EventNode_var evNode=
+	Test::EventNode::_narrow(tmp.in () ACE_ENV_ARG_PARAMETER);
       ACE_TRY_CHECK;
 
-      if (CORBA::is_nil (hello.in ()))
+      if (CORBA::is_nil (evNode.in ()))
 	{
 	  ACE_ERROR_RETURN ((LM_DEBUG,
-			     "Nil Test::Hello reference <%s>\n",
+			     "Nil Test::EventNode reference <%s>\n",
 			     input_),
 			    1);
 	}
 
-      CORBA::String_var the_string =
-	hello->get_string (ACE_ENV_SINGLE_ARG_PARAMETER);
-      ACE_TRY_CHECK;
+      ACE_DEBUG ((LM_DEBUG, "(%P|%t) - Client starting\n"));
 
-      ACE_DEBUG ((LM_DEBUG, "(%P|%t) - string returned <%s>\n",
-		  the_string.in ()));
+      evNode->registerHello( hello_servant.in() ACE_ENV_ARG_PARAMETER );
+      ACE_TRY_CHECK;
 
-      hello->shutdown (ACE_ENV_SINGLE_ARG_PARAMETER);
+      evNode->shutdown (ACE_ENV_SINGLE_ARG_PARAMETER);
       ACE_TRY_CHECK;
     }
   ACE_CATCHANY
diff -urp -N Collocation_Tests.orig/Collocated_Test.cpp Collocation_Tests/Collocated_Test.cpp
--- Collocation_Tests.orig/Collocated_Test.cpp	2003-12-31 22:47:08.000000000 +0100
+++ Collocation_Tests/Collocated_Test.cpp	2005-04-06 14:15:44.259591054 +0200
@@ -5,6 +5,8 @@
 #include "ace/Argv_Type_Converter.h"
 #include "ace/SString.h"
 #include "ace/Manual_Event.h"
+#include <ace/ARGV.h>
+#include <iostream>
 
 const char *output = "test.ior";
 const char *input = "file://test.ior";
@@ -50,7 +52,24 @@ main (int argc, char *argv[])
   ACE_DECLARE_NEW_CORBA_ENV;
   ACE_TRY
     {
+
+    // Keep a copy of the ORB options args
+    ACE_ARGV orb_args;
+    for (int i = 1; i < argc; ++i)
+    {
+        if (orb_args.add (argv[i]) == -1)
+            return -1;
+    }
+
       ACE_Argv_Type_Converter satc (argc, argv);
+
+#if 1
+                std::cerr << "initialize ORB for server with arguments > ";
+                for(int i=0;i<argc;i++)
+                        std::cerr << argv[i] << " ";
+                std::cerr << "<" << std::endl;
+#endif
+
       CORBA::ORB_var sorb =
         CORBA::ORB_init (satc.get_argc (),
                          satc.get_TCHAR_argv (),
@@ -74,12 +93,26 @@ main (int argc, char *argv[])
       // Wait for the server thread to do some processing
       me.wait ();
 
+#if 1
+      int orb_argc = orb_args.argc();
+                std::cerr << "initialize ORB for client with arguments > ";
+                for(int i=0;i<orb_argc;i++)
+                        std::cerr << orb_args.argv()[i] << " ";
+                std::cerr << "<" << std::endl;
+      CORBA::ORB_var corb =
+        CORBA::ORB_init (orb_argc,
+                         orb_args.argv (),
+                         client_orb.c_str ()
+                         ACE_ENV_ARG_PARAMETER);
+#else
+      // This eats all orb-specific options!
       ACE_Argv_Type_Converter catc (argc, argv);
       CORBA::ORB_var corb =
-        CORBA::ORB_init (catc.get_argc (),
-                         catc.get_TCHAR_argv (),
+        CORBA::ORB_init (catc.get_argc(),
+                         catc.get_TCHAR_argv(),
                          client_orb.c_str ()
                          ACE_ENV_ARG_PARAMETER);
+#endif
       ACE_TRY_CHECK;
 
       Client_Task client_task (input,
diff -urp -N Collocation_Tests.orig/EventNode.cpp Collocation_Tests/EventNode.cpp
--- Collocation_Tests.orig/EventNode.cpp	1970-01-01 01:00:00.000000000 +0100
+++ Collocation_Tests/EventNode.cpp	2005-04-06 14:09:51.870871146 +0200
@@ -0,0 +1,60 @@
+//
+// $Id: EventNode.cpp,v 1.1.1.1 2005/01/03 19:36:13 chad Exp $
+//
+#include "EventNode.h"
+#include "tao/ORB_Core.h"
+#include "tao/ORB_Table.h"
+
+ACE_RCSID(EventNode, EventNode, "$Id: EventNode.cpp,v 1.1.1.1 2005/01/03 19:36:13 chad Exp $")
+
+  EventNode::EventNode (CORBA::ORB_ptr orb,
+              ACE_thread_t thrid)
+  : orb_ (CORBA::ORB::_duplicate (orb))
+    , thr_id_ (thrid)
+{
+}
+
+void EventNode::registerHello ( ::Test::Hello_ptr h ACE_ENV_ARG_DECL )
+  ACE_THROW_SPEC ((CORBA::SystemException))
+{
+  ACE_DEBUG ((LM_DEBUG,
+              "(%P|%t) EventNode: registerHello will call get_string...\n"));
+
+  if (ACE_Thread::self () == this->thr_id_)
+    {
+      if (this->orb_->orb_core ()->optimize_collocation_objects () &&
+          this->orb_->orb_core ()->use_global_collocation ())
+        {
+          ACE_ERROR ((LM_ERROR,
+                      "(%P|%t) ERROR: A remote call has been made "
+                      " exiting ..\n"));
+          ACE_OS::abort ();
+        }
+      else if (this->orb_->orb_core ()->optimize_collocation_objects () &&
+               this->orb_->orb_core ()->use_global_collocation () == 0)
+        {
+          TAO::ORB_Table * const orb_table =
+            TAO::ORB_Table::instance ();
+
+          if (orb_table->find ("server_orb") == 0)
+            {
+              // We are running on a single ORB and this is an error.
+              ACE_ERROR ((LM_ERROR,
+                          "(%P|%t) ERROR: A remote call has been made "
+                          " with a single ORB "
+                          " exiting ..\n"));
+              ACE_OS::abort ();
+            }
+        }
+    }
+
+  CORBA::String_var str = h->get_string( (::CORBA::Long) ACE_Thread::self() );
+  ACE_DEBUG ((LM_DEBUG, "(%P|%t) - EventNode: string returned <%s>\n", str.in ()));
+}
+
+void
+EventNode::shutdown (ACE_ENV_SINGLE_ARG_DECL)
+  ACE_THROW_SPEC ((CORBA::SystemException))
+{
+  this->orb_->shutdown (0 ACE_ENV_ARG_PARAMETER);
+}
diff -urp -N Collocation_Tests.orig/EventNode.h Collocation_Tests/EventNode.h
--- Collocation_Tests.orig/EventNode.h	1970-01-01 01:00:00.000000000 +0100
+++ Collocation_Tests/EventNode.h	2005-04-06 14:09:51.870871146 +0200
@@ -0,0 +1,43 @@
+//
+// $Id: EventNode.h,v 1.1.1.1 2005/01/03 19:36:13 chad Exp $
+//
+
+#ifndef HELLO_H
+#define HELLO_H
+#include /**/ "ace/pre.h"
+
+#include "TestS.h"
+
+#include "ace/OS.h"
+
+/// Implement the Test::EventNode interface
+class EventNode
+  : public virtual POA_Test::EventNode
+  , public virtual PortableServer::RefCountServantBase
+{
+public:
+  /// Constructor
+  EventNode (CORBA::ORB_ptr orb,
+         ACE_thread_t thr_id);
+
+  // = The skeleton methods
+  virtual void registerHello ( ::Test::Hello_ptr h ACE_ENV_ARG_DECL)
+    ACE_THROW_SPEC (( CORBA::SystemException));
+
+  virtual void shutdown (ACE_ENV_SINGLE_ARG_DECL)
+    ACE_THROW_SPEC ((CORBA::SystemException));
+
+private:
+  /// Use an ORB reference to conver strings to objects and shutdown
+  /// the application.
+  CORBA::ORB_var orb_;
+
+  ACE_thread_t thr_id_;
+};
+
+#if defined(_MSC_VER) && (_MSC_VER >= 1200)
+# pragma warning(pop)
+#endif /* _MSC_VER */
+
+#include /**/ "ace/post.h"
+#endif /* HELLO_H */
diff -urp -N Collocation_Tests.orig/GNUmakefile.Collocation_Tests Collocation_Tests/GNUmakefile.Collocation_Tests
--- Collocation_Tests.orig/GNUmakefile.Collocation_Tests	2005-02-10 14:19:05.000000000 +0100
+++ Collocation_Tests/GNUmakefile.Collocation_Tests	2005-04-06 13:44:45.673875731 +0200
@@ -22,6 +22,7 @@ FILES = \
   Client_Task \
   Collocated_Test \
   Hello \
+  EventNode \
   Server_Task
 
 #----------------------------------------------------------------------------
@@ -178,7 +179,7 @@ idl_stubs: $(ADDITIONAL_IDL_TARGETS)
 
 # This assignment forces make to run the idl_stubs
 # target before building any of the source files.
-FORCED_IDL_STUBS = TestC.cpp TestS.cpp Client_Task.cpp Collocated_Test.cpp Hello.cpp Server_Task.cpp
+FORCED_IDL_STUBS = TestC.cpp TestS.cpp Client_Task.cpp Collocated_Test.cpp Hello.cpp EventNode.cpp Server_Task.cpp
 FORCED_IDL_STUBS := $(FORCED_IDL_STUBS:TestC.cpp=)
 FORCED_IDL_STUBS := $(FORCED_IDL_STUBS:TestS.cpp=)
 FORCED_IDL_STUBS := $(FORCED_IDL_STUBS:TestS_T.cpp=)
diff -urp -N Collocation_Tests.orig/Hello.cpp Collocation_Tests/Hello.cpp
--- Collocation_Tests.orig/Hello.cpp	2005-01-20 09:52:36.000000000 +0100
+++ Collocation_Tests/Hello.cpp	2005-04-06 14:16:11.367259317 +0200
@@ -15,13 +15,14 @@ ACE_RCSID(Hello, Hello, "Hello.cpp,v 1.3
 }
 
 char *
-Hello::get_string (ACE_ENV_SINGLE_ARG_DECL_NOT_USED)
+Hello::get_string ( ::CORBA::Long caller_threadid ACE_ENV_ARG_DECL )
+
   ACE_THROW_SPEC ((CORBA::SystemException))
 {
   ACE_DEBUG ((LM_DEBUG,
-              "(%P|%t) Upcall in process ..\n"));
+              "(%P|%t) Hello: upcall in process ..\n"));
 
-  if (ACE_Thread::self () == this->thr_id_)
+  if ((::CORBA::Long)ACE_Thread::self () != caller_threadid) // this means a remote call was made
     {
       if (this->orb_->orb_core ()->optimize_collocation_objects () &&
           this->orb_->orb_core ()->use_global_collocation ())
@@ -48,6 +49,30 @@ Hello::get_string (ACE_ENV_SINGLE_ARG_DE
             }
         }
     }
+  else // a collocated call was made
+    {
+      if (this->orb_->orb_core ()->optimize_collocation_objects () == 0)
+        {
+          ACE_ERROR ((LM_ERROR,
+                      "(%P|%t) ERROR: A collocated call has been made "
+                      " exiting ..\n"));
+          ACE_OS::abort ();
+        }
+      else if (this->orb_->orb_core ()->optimize_collocation_objects () &&
+          this->orb_->orb_core ()->use_global_collocation () == 0)
+        { // per-orb
+          TAO::ORB_Table * const orb_table =
+            TAO::ORB_Table::instance ();
+
+          if (orb_table->find ("server_orb") != 0) // two orbs. No collocated call should have happened.
+            {
+                ACE_ERROR ((LM_ERROR,
+                 "(%P|%t) ERROR: A collocated call has been made "
+                 " exiting ..\n"));
+                ACE_OS::abort ();
+             }
+        }
+     }
 
   return CORBA::string_dup ("Hello there!");
 }
diff -urp -N Collocation_Tests.orig/Hello.h Collocation_Tests/Hello.h
--- Collocation_Tests.orig/Hello.h	2003-07-22 01:51:40.000000000 +0200
+++ Collocation_Tests/Hello.h	2005-04-06 14:16:21.881579076 +0200
@@ -28,7 +28,7 @@ public:
          ACE_thread_t thr_id);
 
   // = The skeleton methods
-  virtual char * get_string (ACE_ENV_SINGLE_ARG_DECL)
+  virtual char * get_string ( ::CORBA::Long caller_threadid ACE_ENV_ARG_DECL )
     ACE_THROW_SPEC ((CORBA::SystemException));
 
   virtual void shutdown (ACE_ENV_SINGLE_ARG_DECL)
diff -urp -N Collocation_Tests.orig/Server_Task.cpp Collocation_Tests/Server_Task.cpp
--- Collocation_Tests.orig/Server_Task.cpp	2003-08-18 18:16:30.000000000 +0200
+++ Collocation_Tests/Server_Task.cpp	2005-04-06 14:16:40.097667949 +0200
@@ -3,8 +3,10 @@
 //
 #include "Server_Task.h"
 #include "TestS.h"
-#include "Hello.h"
+//#include "Hello.h"
+#include "EventNode.h"
 
+#include <tao/ORB_Core.h>
 #include "ace/Manual_Event.h"
 
 ACE_RCSID(Collocated_Test,
@@ -47,6 +49,7 @@ Server_Task::svc (void)
        root_poa->the_POAManager (ACE_ENV_SINGLE_ARG_PARAMETER);
      ACE_TRY_CHECK;
 
+#if 0
      Hello *hello_impl;
      ACE_NEW_RETURN (hello_impl,
                      Hello (this->sorb_.in (),
@@ -58,9 +61,19 @@ Server_Task::svc (void)
      Test::Hello_var hello =
        hello_impl->_this (ACE_ENV_SINGLE_ARG_PARAMETER);
      ACE_TRY_CHECK;
+#endif
+
+     EventNode *evnode_impl = new EventNode(this->sorb_.in(),ACE_Thread::self());
+     PortableServer::ServantBase_var owner_transfer(evnode_impl);
+     Test::EventNode_var evNode = evnode_impl->_this(ACE_ENV_SINGLE_ARG_PARAMETER);
+
+                 ACE_DEBUG((LM_DEBUG,"Server (%t) optimize_collocation_objects=%d use_global_collocation=%d\n",
+		                      sorb_->orb_core()->optimize_collocation_objects(),
+		                     sorb_->orb_core()->use_global_collocation ()));
+
 
      CORBA::String_var ior =
-       this->sorb_->object_to_string (hello.in ()
+       this->sorb_->object_to_string (evNode.in ()
                                       ACE_ENV_ARG_PARAMETER);
      ACE_TRY_CHECK;
 
diff -urp -N Collocation_Tests.orig/Test.idl Collocation_Tests/Test.idl
--- Collocation_Tests.orig/Test.idl	2003-03-23 02:55:50.000000000 +0100
+++ Collocation_Tests/Test.idl	2005-04-06 14:18:36.517059922 +0200
@@ -9,7 +9,7 @@ module Test
   interface Hello
   {
     /// Return a simple string
-    string get_string ();
+    string get_string ( in long caller_threadid );
 
     /// A method to shutdown the ORB
     /**
@@ -17,4 +17,11 @@ module Test
      */
     oneway void shutdown ();
   };
+
+  /// The server side
+  interface EventNode
+  {
+     void registerHello( in Hello h );
+     oneway void shutdown ();
+  };
 };
