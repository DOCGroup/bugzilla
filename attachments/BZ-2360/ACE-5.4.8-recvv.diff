--- ace/SOCK_IO.cpp.orig	Fri Oct 28 18:14:55 2005
+++ ace/SOCK_IO.cpp	Fri Jan 13 15:00:16 2006
@@ -68,11 +68,11 @@
       break;
     }
 
-  u_long inlen;
+  int inlen;
 
   if (ACE_OS::ioctl (this->get_handle (),
                      FIONREAD,
-                     (u_long *) &inlen) == -1)
+                     &inlen) == -1)
     return -1;
   else if (inlen > 0)
     {
