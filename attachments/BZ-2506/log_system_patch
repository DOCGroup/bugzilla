Index: ace/Log_Msg.cpp
===================================================================
RCS file: /cvs/ACE_wrappers-repository/ace/Log_Msg.cpp,v
retrieving revision 4.330
diff -u -r4.330 Log_Msg.cpp
--- ace/Log_Msg.cpp	30 May 2006 12:15:03 -0000	4.330
+++ ace/Log_Msg.cpp	22 Jun 2006 14:45:18 -0000
@@ -1,4 +1,4 @@
-// $Id: Log_Msg.cpp,v 4.330 2006/05/30 12:15:03 jwillemsen Exp $
+// Log_Msg.cpp,v 4.321 2006/02/24 15:36:55 sma Exp
 
 // We need this to get the status of ACE_NTRACE...
 #include "ace/config-all.h"
@@ -41,7 +41,7 @@
 #include "ace/Log_Record.h"
 #include "ace/Recursive_Thread_Mutex.h"
 
-ACE_RCSID(ace, Log_Msg, "$Id: Log_Msg.cpp,v 4.330 2006/05/30 12:15:03 jwillemsen Exp $")
+ACE_RCSID(ace, Log_Msg, "Log_Msg.cpp,v 4.321 2006/02/24 15:36:55 sma Exp")
 
 ACE_BEGIN_VERSIONED_NAMESPACE_DECL
 
@@ -83,9 +83,16 @@
 // the output function. If we could rely on more modern compilers, this
 // would be in an unnamed namespace, but it's a macro instead.
 // count is a size_t, len is an int and assumed to be non-negative.
-#define ACE_UPDATE_COUNT(COUNT, LEN) \
-   do { if (static_cast<size_t> (LEN) > COUNT) COUNT = 0; \
-     else COUNT -= static_cast<size_t> (LEN); \
+#define ACE_UPDATE_BP_AND_COUNT(BP, COUNT, LEN) \
+   do { if( LEN > 0 ) { \
+	  	  if( static_cast<size_t>(LEN) < COUNT) { \
+       		COUNT -= static_cast<size_t> (LEN); \
+			BP += LEN; \
+		  } else { \
+			BP += (COUNT-1); \
+            COUNT = 0; } \
+        } \
+		*BP = static_cast<ACE_TCHAR>('\0'); \
    } while (0)
 
 /// Instance count for Log_Msg - used to know when dynamically
@@ -648,6 +655,7 @@
   : status_ (0),
     errnum_ (0),
     linenum_ (0),
+	msg_size_(ACE_MAXLOGMSGLEN+1),
     msg_ (0),
     restart_ (1),  // Restart by default...
     ostream_ (0),
@@ -689,7 +697,9 @@
         }
     }
 
-  ACE_NEW_NORETURN (this->msg_, ACE_TCHAR[ACE_MAXLOGMSGLEN+1]);
+  ACE_NEW_NORETURN (this->msg_, ACE_TCHAR[this->msg_size_] );
+  ACE_NEW_NORETURN (this->log_record_, ACE_Log_Record );
+
 }
 
 ACE_Log_Msg::~ACE_Log_Msg (void)
@@ -758,8 +768,16 @@
       ostream_ = 0;
     }
 #endif
+    if( this->msg_ )
+    {
+      delete[] this->msg_;
+	  this->msg_ = 0;
+    }
+	if( this->log_record_ ){
+		delete this->log_record_;
+		this->log_record_ = 0;
+	}
 
-  delete[] this->msg_;
 }
 
 // Open the sender-side of the message queue.
@@ -767,12 +785,19 @@
 int
 ACE_Log_Msg::open (const ACE_TCHAR *prog_name,
                    u_long flags,
-                   const ACE_TCHAR *logger_key)
+                   const ACE_TCHAR *logger_key, const size_t max_message_size)
 {
   ACE_TRACE ("ACE_Log_Msg::open");
   ACE_MT (ACE_GUARD_RETURN (ACE_Recursive_Thread_Mutex, ace_mon,
                             *ACE_Log_Msg_Manager::get_lock (), -1));
 
+  this->msg_size_ = max_message_size;
+  if ( this->msg_size_ != max_message_size )
+	{
+		 if ( this->msg_ ) delete[] this->msg_;
+		 ACE_NEW_NORETURN (this->msg_, ACE_TCHAR[this->msg_size_] );
+	}
+
   if (prog_name)
     {
       ACE_OS::free ((void *) ACE_Log_Msg::program_name_);
@@ -925,6 +950,7 @@
  *   'z': print an ACE_OS::WChar character
  *   'Z': print an ACE_OS::WChar character string
  *   '%': format a single percent sign, '%'
+ *	 'B': copy binary data as is, put in params first length of buffer and then pointer to buffer
  */
 ssize_t
 ACE_Log_Msg::log (ACE_Log_Priority log_priority,
@@ -980,17 +1006,17 @@
 
   typedef void (*PTF)(...);
 
+  // Only print the message if <priority_mask_> hasn't been reset to
+  // exclude this logging priority.
+  if (this->log_priority_enabled (log_priority) == 0)
+    return 0;
+
   // Check if there were any conditional values set.
   int conditional_values = this->conditional_values_.is_set_;
 
   // Reset conditional values.
   this->conditional_values_.is_set_ = 0;
 
-  // Only print the message if <priority_mask_> hasn't been reset to
-  // exclude this logging priority.
-  if (this->log_priority_enabled (log_priority) == 0)
-    return 0;
-
   // If conditional values were set and the log priority is correct,
   // then the values are actually set.
   if (conditional_values)
@@ -1007,23 +1033,14 @@
   // errno!
   ACE_Errno_Guard guard (errno);
 
-  ACE_Log_Record log_record (log_priority,
-                             ACE_OS::gettimeofday (),
-                             this->getpid ());
+  this->log_record_->priority ( log_priority );
+  this->log_record_->time_stamp( ACE_OS::gettimeofday () );
+  this->log_record_->pid( this->getpid () );
+
   // bp is pointer to where to put next part of logged message.
   // bspace is the number of characters remaining in msg_.
   ACE_TCHAR *bp = const_cast<ACE_TCHAR *> (this->msg ());
-  size_t bspace = ACE_Log_Record::MAXLOGMSGLEN;  // Leave room for Nul term.
-  if (this->msg_off_ <= ACE_Log_Record::MAXLOGMSGLEN)
-    bspace -= static_cast<size_t> (this->msg_off_);
-
-  // If this platform has snprintf() capability to prevent overrunning the
-  // output buffer, use it. To avoid adding a maintenance-hassle compile-
-  // time couple between here and OS.cpp, don't try to figure this out at
-  // compile time. Instead, do a quick check now; if we get a -1 return,
-  // the platform doesn't support the length-limiting capability.
-  ACE_TCHAR test[2];
-  int can_check = ACE_OS::snprintf (test, 1, ACE_LIB_TEXT ("x")) != -1;
+  size_t bspace = this->msg_size_ - 1;  // Leave room for Nul term.
 
   int abort_prog = 0;
   int exit_value = 0;
@@ -1103,7 +1120,6 @@
           ACE_TCHAR *fp;         // Current format pointer
           int       wp = 0;      // Width/precision extracted from args
           int       done = 0;
-          int       skip_nul_locate = 0;
           int       this_len = 0;    // How many chars s[n]printf wrote
 
           fp = format;
@@ -1161,34 +1177,30 @@
                   {
                     ACE_OS::strcpy (fp, ACE_LIB_TEXT ("f"));
                     double value = va_arg (argp, double);
-                    if (can_check)
-                      this_len = ACE_OS::snprintf (bp, bspace, format, value);
-                    else
-                      this_len = ACE_OS::sprintf (bp, format, value);
-                    ACE_UPDATE_COUNT (bspace, this_len);
+                    this_len = ACE_OS::snprintf (bp, bspace, format, value);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   }
                   break;
 
                 case 'a': // Abort program after handling all of format string.
-                  abort_prog = 1;
-                  exit_value = va_arg (argp, int);
-                  ACE_OS::strsncpy (bp, abort_str, bspace);
-                  if (bspace > ACE_OS::strlen (abort_str))
-                    bspace -= ACE_OS::strlen (abort_str);
-                  else
-                    bspace = 0;
-                  break;
+				  {
+						  size_t abrt_len = ACE_OS::strlen (abort_str);
+						  abort_prog = 1;
+						  exit_value = va_arg (argp, int); // TODO: do we really need it here may be it is bug?
+						  size_t len = bspace>abrt_len?abrt_len+1:bspace; 
+						  ACE_OS::strncpy (bp, abort_str, len);
+						  bp += (len-1);
+						  bspace -= len;
+						  break;
+                  }
 
                 case 'l':             // Source file line number
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("d"));
-                  if (can_check)
-                    this_len = ACE_OS::snprintf (bp,
+                  this_len = ACE_OS::snprintf (bp,
                                                  bspace,
                                                  format,
                                                  this->linenum ());
-                  else
-                    this_len = ACE_OS::sprintf (bp, format, this->linenum ());
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
 
                 case 'N':             // Source file name
@@ -1197,18 +1209,12 @@
 #else
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
 #endif
-                  if (can_check)
-                    this_len = ACE_OS::snprintf (bp, bspace, format,
+                  this_len = ACE_OS::snprintf (bp, bspace, format,
                                                  this->file () ?
                                                  ACE_TEXT_CHAR_TO_TCHAR (this->file ())
                                                  : ACE_LIB_TEXT ("<unknown file>"));
-                  else
-                    this_len = ACE_OS::sprintf (bp, format,
-                                                this->file () ?
-                                                ACE_TEXT_CHAR_TO_TCHAR (this->file ())
-                                                : ACE_LIB_TEXT ("<unknown file>"));
-                  ACE_UPDATE_COUNT (bspace, this_len);
-                  break;
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
+				  break;
 
                 case 'n':             // Program name
 #if !defined (ACE_WIN32) && defined (ACE_USES_WCHAR)
@@ -1216,29 +1222,18 @@
 #else /* ACE_WIN32 && ACE_USES_WCHAR */
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
 #endif
-                  if (can_check)
-                    this_len = ACE_OS::snprintf (bp, bspace, format,
+				  this_len = ACE_OS::snprintf (bp, bspace, format,
                                                  ACE_Log_Msg::program_name_ ?
                                                  ACE_Log_Msg::program_name_ :
                                                  ACE_LIB_TEXT ("<unknown>"));
-                  else
-                    this_len = ACE_OS::sprintf (bp, format,
-                                                ACE_Log_Msg::program_name_ ?
-                                                ACE_Log_Msg::program_name_ :
-                                                ACE_LIB_TEXT ("<unknown>"));
-                  ACE_UPDATE_COUNT (bspace, this_len);
-                  break;
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
+				  break;
 
                 case 'P':             // Process ID
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("d"));
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
-                      (bp, bspace, format,
-                       static_cast<int> (this->getpid ()));
-                  else
-                    this_len = ACE_OS::sprintf
-                      (bp, format, static_cast<int> (this->getpid ()));
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  this_len = ACE_OS::snprintf (bp, bspace, format, 
+												static_cast<int> (this->getpid ()));
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
 
                 case 'p':             // <errno> string, ala perror()
@@ -1258,14 +1253,9 @@
 #else
                         ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s: %s"));
 #endif
-                        if (can_check)
-                          this_len = ACE_OS::snprintf
-                            (bp, bspace, format, va_arg (argp, ACE_TCHAR *),
-                             ACE_TEXT_CHAR_TO_TCHAR (msg));
-                        else
-                          this_len = ACE_OS::sprintf
-                            (bp, format, va_arg (argp, ACE_TCHAR *),
-                             ACE_TEXT_CHAR_TO_TCHAR (msg));
+						this_len = ACE_OS::snprintf (bp, bspace, format,
+														 va_arg (argp, ACE_TCHAR *),
+							                             ACE_TEXT_CHAR_TO_TCHAR (msg));
 #if defined (ACE_WIN32)
                       }
                     else
@@ -1300,36 +1290,24 @@
                             const ACE_TCHAR *message =
                               ACE::sock_error (errno);
                             ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s: %s"));
-                            if (can_check)
-                              this_len = ACE_OS::snprintf
+							this_len = ACE_OS::snprintf
                                 (bp, bspace, format,
                                  va_arg (argp, const ACE_TCHAR *),
                                  message);
-                            else
-                              this_len = ACE_OS::sprintf
-                                (bp, format,
-                                 va_arg (argp, const ACE_TCHAR *),
-                                 message);
                           }
                         else
                           {
                             ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s: %s"));
-                            if (can_check)
-                              this_len = ACE_OS::snprintf
+							this_len = ACE_OS::snprintf
                                 (bp, bspace, format,
                                  va_arg (argp, ACE_TCHAR *),
                                  lpMsgBuf);
-                            else
-                              this_len = ACE_OS::sprintf
-                                (bp, format,
-                                 va_arg (argp, ACE_TCHAR *),
-                                 lpMsgBuf);
-                            // Free the buffer.
+							// Free the buffer.
                             ::LocalFree (lpMsgBuf);
                           }
                       }
 #endif /* ACE_WIN32 */
-                    ACE_UPDATE_COUNT (bspace, this_len);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                     break;
                   }
 
@@ -1339,15 +1317,9 @@
 #else
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
 #endif
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
-                      (bp, bspace, format,
-                       ACE_Log_Record::priority_name (log_priority));
-                  else
-                    this_len = ACE_OS::sprintf
-                      (bp, format,
+				  this_len = ACE_OS::snprintf (bp, bspace, format,
                        ACE_Log_Record::priority_name (log_priority));
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
 
                 case 'm': // Format the string assocated with the errno value.
@@ -1367,12 +1339,7 @@
 #else /* ACE_WIN32 && ACE_USES_WCHAR */
                         ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
 #endif
-                        if (can_check)
-                          this_len = ACE_OS::snprintf
-                            (bp, bspace, format, ACE_TEXT_CHAR_TO_TCHAR (msg));
-                        else
-                          this_len = ACE_OS::sprintf
-                            (bp, format, ACE_TEXT_CHAR_TO_TCHAR (msg));
+						this_len = ACE_OS::snprintf (bp, bspace, format, ACE_TEXT_CHAR_TO_TCHAR (msg));
 #if defined (ACE_WIN32)
                       }
                     else
@@ -1407,56 +1374,43 @@
                             const ACE_TCHAR *message =
                               ACE::sock_error (errno);
                             ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
-                            if (can_check)
-                              this_len = ACE_OS::snprintf
-                                (bp, bspace, format, message);
-                            else
-                              this_len = ACE_OS::sprintf (bp, format, message);
+							this_len = ACE_OS::snprintf (bp, bspace, format, message);
                           }
                         else
                           {
                             ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
-                            if (can_check)
-                              this_len = ACE_OS::snprintf
-                                (bp, bspace, format, lpMsgBuf);
-                            else
-                              this_len = ACE_OS::sprintf
-                                (bp, format, lpMsgBuf);
+							this_len = ACE_OS::snprintf (bp, bspace, format, lpMsgBuf);
                             // Free the buffer.
                             ::LocalFree (lpMsgBuf);
                           }
                       }
 #endif /* ACE_WIN32 */
-                    ACE_UPDATE_COUNT (bspace, this_len);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                     break;
                   }
 
                 case 'R': // Format the return status of the operation.
                   this->op_status (va_arg (argp, int));
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("d"));
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
+				  this_len = ACE_OS::snprintf
                       (bp, bspace, format, this->op_status ());
-                  else
-                    this_len = ACE_OS::sprintf
-                      (bp, format, this->op_status ());
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
 
                 case '{': // Increment the trace_depth, then indent
-                  skip_nul_locate = 1;
                   (void) this->inc ();
                   break;
 
                 case '}': // indent, then decrement trace_depth
-                  skip_nul_locate = 1;
                   (void) this->dec ();
                   break;
 
                 case '$': // insert a newline, then indent the next line
                           // according to %I
-                  *bp++ = '\n';
-                  --bspace;
+				  if ( bspace ){
+						  *bp++ = '\n';
+						  --bspace;
+				  }
                   /* fallthrough */
 
                 case 'I': // Indent with nesting_depth*width spaces
@@ -1479,35 +1433,11 @@
 
                   *bp = '\0';
                   bspace -= static_cast<size_t> (wp);
-                  skip_nul_locate = 1;
                   break;
 
                 case 'r': // Run (invoke) this subroutine.
                   {
-                    long osave = ACE_Log_Msg::msg_off_;
-
-                    if (ACE_BIT_ENABLED (ACE_Log_Msg::flags_,
-                                         ACE_Log_Msg::SILENT) &&
-                        bspace > 1)
-                      {
-                        *bp++ = '{';
-                        --bspace;
-                      }
-                    ACE_Log_Msg::msg_off_ =  bp - this->msg_;
-
                     (*va_arg (argp, PTF))();
-
-                    if (ACE_BIT_ENABLED (ACE_Log_Msg::flags_,
-                                         ACE_Log_Msg::SILENT) &&
-                        bspace > (1 + ACE_OS::strlen (bp)))
-                      {
-                        bspace -= (ACE_OS::strlen (bp) + 1);
-                        bp += ACE_OS::strlen (bp);
-                        *bp++ =  '}';
-                      }
-                    *bp = '\0';
-                    skip_nul_locate = 1;
-                    ACE_Log_Msg::msg_off_ = osave;
                     break;
                   }
 
@@ -1518,32 +1448,20 @@
                     if (sig >= 0 && sig < ACE_NSIG)
                       {
                         ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
-                        if (can_check)
-                          this_len = ACE_OS::snprintf
+						this_len = ACE_OS::snprintf
                             (bp, bspace, format, _sys_siglist[sig]);
-                        else
-                          this_len = ACE_OS::sprintf
-                            (bp, format, _sys_siglist[sig]);
                       }
                     else
                       {
-                        if (can_check)
-                          this_len = ACE_OS::snprintf
+					    this_len = ACE_OS::snprintf
                             (bp, bspace,
                              ACE_LIB_TEXT("<unknown signal> %d"), sig);
-                        else
-                          this_len = ACE_OS::sprintf
-                            (bp, ACE_LIB_TEXT ("<unknown signal> %d"), sig);
                       }
 #else
-                    if (can_check)
-                      this_len = ACE_OS::snprintf
+                    this_len = ACE_OS::snprintf
                         (bp, bspace, ACE_LIB_TEXT ("signal %d"), sig);
-                    else
-                      this_len = ACE_OS::sprintf
-                        (bp, ACE_LIB_TEXT ("signal %d"), sig);
 #endif /* ACE_HAS_SYS_SIGLIST */
-                    ACE_UPDATE_COUNT (bspace, this_len);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                     break;
                   }
 
@@ -1558,12 +1476,9 @@
 #else
                     ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
 #endif
-                    if (can_check)
-                      this_len = ACE_OS::snprintf
+					this_len = ACE_OS::snprintf
                         (bp, bspace, format, day_and_time);
-                    else
-                      this_len = ACE_OS::sprintf (bp, format, day_and_time);
-                    ACE_UPDATE_COUNT (bspace, this_len);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                     break;
                   }
 
@@ -1572,30 +1487,19 @@
                   {
                     ACE_TCHAR day_and_time[35];
                     ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
-                    if (can_check)
-                      this_len = ACE_OS::snprintf
+					this_len = ACE_OS::snprintf
                         (bp, bspace, format,
                          ACE::timestamp (day_and_time, sizeof day_and_time));
-                    else
-                      this_len = ACE_OS::sprintf
-                        (bp, format, ACE::timestamp (day_and_time,
-                                                     sizeof day_and_time));
-                    ACE_UPDATE_COUNT (bspace, this_len);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                     break;
                   }
 
                 case 't': // Format thread id.
 #if defined (ACE_WIN32)
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("u"));
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
+				  this_len = ACE_OS::snprintf
                       (bp, bspace, format,
                        static_cast<unsigned> (ACE_Thread::self ()));
-                  else
-                    this_len =
-                      ACE_OS::sprintf (bp,
-                                       format,
-                                       static_cast <unsigned> (ACE_Thread::self ()));
 #elif defined (ACE_AIX_VERS) && (ACE_AIX_VERS <= 402)
                   // AIX's pthread_t (ACE_hthread_t) is a pointer, and it's
                   // a little ugly to send that through a %u format.  So,
@@ -1608,11 +1512,8 @@
                   //   this would have on that.
                   // -Steve Huston, 19-Aug-97
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("u"));
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
+				  this_len = ACE_OS::snprintf
                       (bp, bspace, format, thread_self());
-                  else
-                    this_len = ACE_OS::sprintf (bp, format, thread_self());
 #elif defined (DIGITAL_UNIX)
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("u"));
                   {
@@ -1623,9 +1524,6 @@
                       ACE_Thread::self ();
 #  endif /* ACE_HAS_THREADS */
 
-                      if (can_check)
-                        this_len = ACE_OS::snprintf (bp, bspace, format, id);
-                      else
                         this_len = ACE_OS::sprintf (bp, format, id);
                   }
 #else
@@ -1636,9 +1534,6 @@
                   // MVS's pthread_t is a struct... yuck. So use the ACE 5.0
                   // code for it.
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("u"));
-                  if (can_check)
-                    this_len = ACE_OS::snprintf (bp, bspace, format, t_id);
-                  else
                     this_len = ACE_OS::sprintf (bp, format, t_id);
 #  else
                   // Yes, this is an ugly C-style cast, but the correct
@@ -1647,16 +1542,12 @@
                   // a pointer type, but doesn't have a _np function to
                   // get an integral type, like the OSes above.
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("lu"));
-                  if (can_check)
                     this_len = ACE_OS::snprintf
                       (bp, bspace, format, (unsigned long)t_id);
-                  else
-                    this_len = ACE_OS::sprintf
-                      (bp, format, (unsigned long)t_id);
 #  endif /* ACE_MWS || ACE_TANDEM_T1248_PTHREADS */
 
 #endif /* ACE_WIN32 */
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
 
                 case 's':                       // String
@@ -1668,13 +1559,9 @@
                     ACE_TCHAR *str = va_arg (argp, ACE_TCHAR *);
                     ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
 #endif /* ACE_WIN32 && ACE_USES_WCHAR */
-                    if (can_check)
-                      this_len = ACE_OS::snprintf
+					this_len = ACE_OS::snprintf
                         (bp, bspace, format, str ? str : ACE_LIB_TEXT ("(null)"));
-                    else
-                      this_len = ACE_OS::sprintf
-                        (bp, format, str ? str : ACE_LIB_TEXT ("(null)"));
-                    ACE_UPDATE_COUNT (bspace, this_len);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   }
                   break;
 
@@ -1686,13 +1573,9 @@
 #else /* ACE_WIN32 && ACE_USES_WCHAR */
                     ACE_OS::strcpy (fp, ACE_LIB_TEXT ("s"));
 #endif /* ACE_WIN32 && ACE_USES_WCHAR */
-                    if (can_check)
-                      this_len = ACE_OS::snprintf
+                    this_len = ACE_OS::snprintf
                         (bp, bspace, format, cstr ? cstr : ACE_LIB_TEXT ("(null)"));
-                    else
-                      this_len = ACE_OS::sprintf
-                        (bp, format, cstr ? cstr : ACE_LIB_TEXT ("(null)"));
-                    ACE_UPDATE_COUNT (bspace, this_len);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   }
                   break;
 
@@ -1705,12 +1588,8 @@
 # else /* ACE_USES_WCHAR */
                     ACE_OS::strcpy (fp, ACE_LIB_TEXT ("S"));
 # endif /* ACE_USES_WCHAR */
-                    if (can_check)
-                      this_len = ACE_OS::snprintf
+                    this_len = ACE_OS::snprintf
                         (bp, bspace, format, wstr ? wstr : ACE_LIB_TEXT ("(null)"));
-                    else
-                      this_len = ACE_OS::sprintf
-                        (bp, format, wstr ? wstr : ACE_LIB_TEXT ("(null)"));
 #elif defined (ACE_HAS_WCHAR)
                     wchar_t *wchar_str = va_arg (argp, wchar_t *);
 # if defined (HPUX)
@@ -1718,14 +1597,10 @@
 # else
                     ACE_OS::strcpy (fp, ACE_LIB_TEXT ("ls"));
 # endif /* HPUX */
-                    if (can_check)
-                      this_len = ACE_OS::snprintf
+                    this_len = ACE_OS::snprintf
                         (bp, bspace, format, wchar_str);
-                    else
-                      this_len = ACE_OS::sprintf
-                        (bp, format, wchar_str);
 #endif /* ACE_WIN32 / ACE_HAS_WCHAR */
-                    ACE_UPDATE_COUNT (bspace, this_len);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   }
                   break;
 
@@ -1736,34 +1611,22 @@
 # else /* ACE_USES_WCHAR */
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("C"));
 # endif /* ACE_USES_WCHAR */
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
+                  this_len = ACE_OS::snprintf
                       (bp, bspace, format, va_arg (argp, int));
-                  else
-                    this_len = ACE_OS::sprintf
-                      (bp, format, va_arg (argp, int));
 #elif defined (ACE_USES_WCHAR)
 # if defined (HPUX)
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("C"));
 # else
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("lc"));
 # endif /* HPUX */
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
+                  this_len = ACE_OS::snprintf
                       (bp, bspace, format, va_arg (argp, wint_t));
-                  else
-                    this_len = ACE_OS::sprintf
-                      (bp, format, va_arg (argp, wint_t));
 #else /* ACE_WIN32 */
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("u"));
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
+                  this_len = ACE_OS::snprintf
                       (bp, bspace, format, va_arg (argp, int));
-                  else
-                    this_len = ACE_OS::sprintf
-                      (bp, format, va_arg (argp, int));
 #endif /* ACE_WIN32 */
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
 
                 case 'z':              // ACE_OS::WChar character
@@ -1787,11 +1650,8 @@
 #else /* ACE_WIN32 */
                     ACE_OS::strcpy (fp, ACE_LIB_TEXT ("u"));
 #endif /* ACE_WIN32 */
-                    if (can_check)
-                      this_len = ACE_OS::snprintf (bp, bspace, format, wtchar);
-                    else
-                      this_len = ACE_OS::sprintf (bp, format, wtchar);
-                    ACE_UPDATE_COUNT (bspace, this_len);
+                    this_len = ACE_OS::snprintf (bp, bspace, format, wtchar);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                     break;
                   }
 
@@ -1832,16 +1692,13 @@
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("ls"));
 # endif /* HPUX */
 #endif /* ACE_WIN32 / ACE_HAS_WCHAR */
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
+                  this_len = ACE_OS::snprintf
                       (bp, bspace, format, wchar_t_str);
-                  else
-                    this_len = ACE_OS::sprintf (bp, format, wchar_t_str);
                   if(sizeof(ACE_OS::WChar) != sizeof(wchar_t))
                     {
                       delete wchar_t_str;
                     }
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
                   }
 
@@ -1851,39 +1708,27 @@
 #else
                   ACE_OS::strcpy (fp, ACE_LIB_TEXT ("c"));
 #endif /* ACE_WIN32 && ACE_USES_WCHAR */
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
+                  this_len = ACE_OS::snprintf
                       (bp, bspace, format, va_arg (argp, int));
-                  else
-                    this_len = ACE_OS::sprintf
-                      (bp, format, va_arg (argp, int));
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
 
                 case 'd': case 'i': case 'o':
                 case 'u': case 'x': case 'X':
                   fp[0] = *format_str;
                   fp[1] = '\0';
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
+                  this_len = ACE_OS::snprintf
                       (bp, bspace, format, va_arg (argp, int));
-                  else
-                    this_len = ACE_OS::sprintf
-                      (bp, format, va_arg (argp, int));
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
 
                 case 'F': case 'f': case 'e': case 'E':
                 case 'g': case 'G':
                   fp[0] = *format_str;
                   fp[1] = '\0';
-                  if (can_check)
-                    this_len = ACE_OS::snprintf
+                  this_len = ACE_OS::snprintf
                       (bp, bspace, format, va_arg (argp, double));
-                  else
-                    this_len = ACE_OS::sprintf
-                      (bp, format, va_arg (argp, double));
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
 
                 case 'Q':
@@ -1893,29 +1738,24 @@
                     ACE_UINT32 hi = va_arg (argp, ACE_UINT32);
                     ACE_UINT32 lo = va_arg (argp, ACE_UINT32);
                     if (hi > 0)
-                      this_len = ACE_OS::sprintf (bp,
+                      this_len = ACE_OS::snprintf (bp, bspace,
                                                   "0x%lx%0*lx",
                                                   hi,
                                                   2 * sizeof lo,
                                                   lo);
                     else
-                      this_len = ACE_OS::sprintf (bp, "0x%lx", lo);
+                      this_len = ACE_OS::snprintf (bp, bspace, "0x%lx", lo);
                   }
 #else  /* ! ACE_LACKS_LONGLONG_T */
                   {
                     const ACE_TCHAR *fmt = ACE_UINT64_FORMAT_SPECIFIER;
                     ACE_OS::strcpy (fp, &fmt[1]);    // Skip leading %
-                    if (can_check)
-                      this_len = ACE_OS::snprintf (bp, bspace,
+                    this_len = ACE_OS::snprintf (bp, bspace,
                                                    format,
                                                    va_arg (argp, ACE_UINT64));
-                    else
-                      this_len = ACE_OS::sprintf (bp,
-                                                  format,
-                                                  va_arg (argp, ACE_UINT64));
                   }
 #endif /* ! ACE_LACKS_LONGLONG_T || ACE_LACKS_UNSIGNEDLONGLONG_T */
-                  ACE_UPDATE_COUNT (bspace, this_len);
+				  ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                   break;
 
                 case 'q':
@@ -1926,30 +1766,33 @@
                    {
                      const ACE_TCHAR *fmt = ACE_INT64_FORMAT_SPECIFIER;
                      ACE_OS::strcpy (fp, &fmt[1]);    // Skip leading %
-                     if (can_check)
-                       this_len = ACE_OS::snprintf (bp, bspace,
+					 this_len = ACE_OS::snprintf (bp, bspace,
                                                     format,
                                                     va_arg (argp, ACE_INT64));
-                     else
-                       this_len = ACE_OS::sprintf (bp,
-                                                   format,
-                                                   va_arg (argp, ACE_INT64));
                    }
  #endif /* ! ACE_LACKS_LONGLONG_T */
-                   ACE_UPDATE_COUNT (bspace, this_len);
+				   ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                    break;
 
                 case '@':
                     ACE_OS::strcpy (fp, ACE_LIB_TEXT ("p"));
-                    if (can_check)
-                      this_len = ACE_OS::snprintf
+					this_len = ACE_OS::snprintf
                         (bp, bspace, format, va_arg (argp, void*));
-                    else
-                      this_len = ACE_OS::sprintf
-                        (bp, format, va_arg (argp, void*));
-                    ACE_UPDATE_COUNT (bspace, this_len);
+					ACE_UPDATE_BP_AND_COUNT(bp,bspace,this_len);
                     break;
 
+				case 'B':
+					{
+						int buf_len  = va_arg (argp, int); 
+						void* tmp_buf = va_arg (argp, void*);
+						if ( buf_len > bspace ) buf_len = bspace; 
+
+						ACE_OS::memcpy(bp,tmp_buf,buf_len);
+						bp -= buf_len;
+						bspace -= buf_len;
+						break;
+					}
+					
                 default:
                   // So, it's not a legit format specifier after all...
                   // Copy from the original % to where we are now, then
@@ -1971,9 +1814,6 @@
               ++format_str;
             }
 
-          if (!skip_nul_locate)
-            while (*bp != '\0') // Locate end of bp.
-              ++bp;
         }
     }
 
@@ -1981,35 +1821,18 @@
 
   ssize_t result = 0;
 
-  // Check that memory was not corrupted, if it corrupted we can't log anything
-  // anymore because all our members could be corrupted.
-  if (bp >= (this->msg_ + ACE_MAXLOGMSGLEN+1))
-    {
-      abort_prog = 1;
-      ACE_OS::fprintf (stderr,
-                       "The following logged message is too long!\n");
-    }
-  else
-    {
-      // Copy the message from thread-specific storage into the transfer
-      // buffer (this can be optimized away by changing other code...).
-      log_record.msg_data (this->msg ());
-
-      // Write the <log_record> to the appropriate location.
-      result = this->log (log_record,
-                          abort_prog);
-    }
+  // set our message in log_record_ without copying it
+  this->log_record_->msg_data (const_cast<ACE_TCHAR*>( this->msg() ), this->msg_size_ - bspace, 0 );
+
+  // Write the <log_record> to the appropriate location.
+  result = this->log (*(this->log_record_), abort_prog);
 
   if (abort_prog)
     {
-      // Since we are now calling abort instead of exit, this value is
-      // not used.
-      ACE_UNUSED_ARG (exit_value);
-
       // *Always* print a message to stderr if we're aborting.  We
       // don't use verbose, however, to avoid recursive aborts if
       // something is hosed.
-      log_record.print (ACE_Log_Msg::local_host_, 0, stderr);
+      this->log_record_->print (ACE_Log_Msg::local_host_, 0, stderr);
       ACE_OS::abort ();
     }
 
@@ -2169,12 +1992,11 @@
   if (this->log_priority_enabled (log_priority) == 0)
     return 0;
 
-  ACE_TCHAR* buf = 0;
-  const size_t buf_sz =
-    ACE_Log_Record::MAXLOGMSGLEN - ACE_Log_Record::VERBOSE_LEN - 58;
+  ACE_TCHAR* buf;
+  size_t buf_sz = this->msg_size_ - ACE_Log_Record::VERBOSE_LEN - 58;
   ACE_NEW_RETURN (buf, ACE_TCHAR[buf_sz], -1);
 
-  ACE_TCHAR *msg_buf = 0;
+  ACE_TCHAR *msg_buf;
   const size_t text_sz = text ? ACE_OS::strlen(text) : 0;
   ACE_NEW_RETURN (msg_buf,
                   ACE_TCHAR[text_sz + 58],
@@ -2183,7 +2005,7 @@
   buf[0] = 0; // in case size = 0
 
   const size_t len = ACE::format_hexdump
-    (buffer, size, buf, buf_sz / sizeof (ACE_TCHAR) - text_sz);
+    (buffer, size, buf, buf_sz - text_sz);
 
   int sz = 0;
 
@@ -2211,8 +2033,8 @@
              msg_buf,
              buf);
 
-  delete [] msg_buf;
   delete [] buf;
+  delete [] msg_buf;
   return 0;
 }
 
@@ -2463,8 +2285,7 @@
 void
 ACE_Log_Msg::msg (const ACE_TCHAR *m)
 {
-  ACE_OS::strsncpy (this->msg_, m,
-                    ((ACE_MAXLOGMSGLEN+1) / sizeof (ACE_TCHAR)));
+  ACE_OS::strsncpy (this->msg_, m, this->msg_size_ );
 }
 
 ACE_Log_Msg_Callback *
Index: ace/Log_Msg.h
===================================================================
RCS file: /cvs/ACE_wrappers-repository/ace/Log_Msg.h,v
retrieving revision 4.127
diff -u -r4.127 Log_Msg.h
--- ace/Log_Msg.h	25 Apr 2006 06:32:10 -0000	4.127
+++ ace/Log_Msg.h	22 Jun 2006 14:45:18 -0000
@@ -223,7 +223,7 @@
    */
   int open (const ACE_TCHAR *prog_name,
             u_long options_flags = ACE_Log_Msg::STDERR,
-            const ACE_TCHAR *logger_key = 0);
+            const ACE_TCHAR *logger_key = 0, const size_t max_message_size = ACE_MAXLOGMSGLEN );
 
   // = Set/get the options flags.
 
@@ -577,6 +577,8 @@
   /// File where the error occurred.
   char file_[MAXPATHLEN + 1];
 
+  ACE_Log_Record* log_record_;
+  size_t     msg_size_;
   /// The log message, which resides in thread-specific storage.  Note
   /// that only the current log message is stored here -- it will be
   /// overwritten by the subsequent call to log().
Index: ace/Log_Record.cpp
===================================================================
RCS file: /cvs/ACE_wrappers-repository/ace/Log_Record.cpp,v
retrieving revision 4.85
diff -u -r4.85 Log_Record.cpp
--- ace/Log_Record.cpp	30 May 2006 11:38:44 -0000	4.85
+++ ace/Log_Record.cpp	22 Jun 2006 14:45:18 -0000
@@ -111,18 +111,130 @@
   ACE_DEBUG ((LM_DEBUG, ACE_LIB_TEXT ("\ntype_ = %u\n"), this->type_));
   ACE_DEBUG ((LM_DEBUG, ACE_LIB_TEXT ("\ntime_stamp_ = (%d, %d)\n"), this->secs_, this->usecs_));
   ACE_DEBUG ((LM_DEBUG, ACE_LIB_TEXT ("\npid_ = %u\n"), this->pid_));
+  ACE_DEBUG ((LM_DEBUG, ACE_LIB_TEXT ("\nbyte_order_ = %u\n"), this->byte_order_));
+  ACE_DEBUG ((LM_DEBUG, ACE_LIB_TEXT ("\nmsg_size_ = %d\n"), this->msg_size_));
   ACE_DEBUG ((LM_DEBUG, ACE_LIB_TEXT ("\nmsg_data_ = %s\n"), this->msg_data_));
   ACE_DEBUG ((LM_DEBUG, ACE_END_DUMP));
 #endif /* ACE_HAS_DUMP */
 }
 
+int
+ACE_Log_Record::encode (char* obuf, const size_t obuf_length)
+{
+  ACE_TRACE ("ACE_Log_Record::encode");
+
+  if ( ( (obuf - (char*)0 ) & (sizeof(ACE_UINT32)-1) ) != 0  )
+        return -1; //  buffer is not aligned on 32 bit border
+
+  register ACE_UINT32* obuf_ = reinterpret_cast<ACE_UINT32*>(obuf);
+
+  if ( ( obuf_length < static_cast<unsigned long>(this->length()) ) || (obuf_ == 0) || (obuf_length == 0) )
+        return this->length(); // no valid buffer or length, return size of expected buffer
+
+
+  obuf_[0] = htonl ( this->length() );
+  obuf_[1] = htonl ( this->type_  );
+  obuf_[2] = htonl ( this->secs_  );
+  obuf_[3] = htonl ( this->usecs_ );
+  obuf_[4] = htonl ( this->pid_   );
+  obuf_[5] = htonl ( this->byte_order_ );
+
+  ACE_OS::memcpy( obuf_+6, this->msg_data_, this->length_*sizeof(ACE_TCHAR) );
+  return 0;
+}
+
+int
+ACE_Log_Record::decode (const char* ibuf, const size_t ibuf_length)
+{
+  ACE_TRACE ("ACE_Log_Record::decode");
+
+  if ( (ibuf-(char*)0) & (sizeof(ACE_UINT32)-1) != 0  ) //  buffer is not aligned on 32 bit border
+        return -1;
+
+  register const ACE_UINT32* ibuf_ = reinterpret_cast<const ACE_UINT32*>(ibuf);
+
+  if ( ibuf_length > (this->msg_size_ * sizeof(ACE_TCHAR)) ){
+        if(this->msg_size_ && this->msg_data_) delete[] this->msg_data_;
+        ACE_NEW_NORETURN ( this->msg_data_, ACE_TCHAR[ ibuf_length ] );
+        if( this->msg_data_ )
+            this->msg_size_ = ibuf_length;
+        else
+          {
+            this->msg_size_ = 0;
+            return -1; // failed to reallocate internal data buffer
+          }
+  }
+
+
+
+  this->type_ = ntohl( ibuf_[1] );
+  this->pid_ = ntohl( ibuf_[4] );
+  this->byte_order_ = ntohl( ibuf_[5] );
+
+  ACE_Time_Value tv (ntohl ( ibuf_[2] ),
+                     ntohl ( ibuf_[3] ));
+
+  this->secs_ = tv.sec ();
+  this->usecs_ = tv.usec ();
+
+  register ACE_UINT32 tmp_len = ntohl( ibuf_[0] ) - sizeof(ACE_UINT32)*6; // length of payload in bytes
+
+  // it's not safe to use mixed implementations of ACE_TCHAR
+  // (i.e. one host has ACE_TCHAR=char and another ACE_TCHAR=wchar_t)
+  this->length_ = static_cast<ACE_INT32>( tmp_len / sizeof(ACE_TCHAR) ); // length of payload in ACE_TCHAR
+  ACE_OS::memcpy( this->msg_data_, ibuf_+6, tmp_len);
+  return 0;
+}
+
 void
-ACE_Log_Record::msg_data (const ACE_TCHAR *data)
+ACE_Log_Record::msg_data (const ACE_TCHAR *data, const ACE_INT32 length, const int copy_data, const int become_owner )
 {
-  // ACE_TRACE ("ACE_Log_Record::msg_data");
-  ACE_OS::strsncpy (this->msg_data_, data,
-                    (MAXLOGMSGLEN / sizeof (ACE_TCHAR)));
-  this->round_up ();
+		if( length ){
+				this->length_ = length;
+		}else{ // length=0 so assume that data is C-string (legacy behaviour)
+				this->length_ = ACE_OS::strlen(data) + 1;
+		}
+
+		if( copy_data ){
+				// realloc msg_data_ buf in case of if it too small
+				if( this->length_ > this->msg_size_ ){
+						// free bufer if we own it (i.e. msg_size_ and msg_data_ is not 0)
+						if(this->msg_size_ && this->msg_data_) delete[] this->msg_data_;
+						this->msg_size_ = this->length_;
+						ACE_NEW_NORETURN (this->msg_data_, ACE_TCHAR[this->length_]);
+				}
+				ACE_OS::memcpy(this->msg_data_, data, sizeof(ACE_TCHAR) * this->length_ );
+		}else{
+				if( become_owner ){
+						// free bufer if we own it (i.e. msg_size_ and msg_data_ is not 0)
+						if(this->msg_size_ && this->msg_data_) delete[] this->msg_data_;
+						this->msg_size_ = this->length_; // this field shouldn't be 0 because of we should own it
+						this->msg_data_ = const_cast<ACE_TCHAR*>(data);
+				}else{
+						// free bufer if we own it (i.e. msg_size_ and msg_data_ is not 0)
+						if(this->msg_size_ && this->msg_data_) delete[] this->msg_data_;
+						this->msg_size_ = 0; // we don't own data so not set this field
+						this->msg_data_ = const_cast<ACE_TCHAR*>(data);
+				}
+		}
+
+}
+
+ACE_Log_Record::ACE_Log_Record (const ACE_Log_Record& src)
+{
+	this->length_ = src.length_;
+	this->type_ = src.type_;
+	this->secs_ = src.secs_;
+	this->usecs_ = src.usecs_;
+	this->pid_ = src.pid_;
+	this->byte_order_ = src.byte_order_;
+    this->msg_size_ = src.msg_size_;
+	ACE_NEW_NORETURN (this->msg_data_, ACE_TCHAR[this->msg_size_]);
+	if ( this->msg_data_ ){
+		ACE_OS::memcpy(this->msg_data_, src.msg_data_, sizeof(ACE_TCHAR) * src.msg_size_ );
+	}else{
+		this->msg_size_ = 0;
+	}
 }
 
 ACE_Log_Record::ACE_Log_Record (ACE_Log_Priority lp,
@@ -133,10 +245,13 @@
     secs_ (ts_sec),
     usecs_ (0),
     pid_ (ACE_UINT32 (p)),
-    msg_data_ (0)
+	byte_order_ ( ACE_CDR_BYTE_ORDER ),
+	msg_size_(MAXLOGMSGLEN)
 {
   // ACE_TRACE ("ACE_Log_Record::ACE_Log_Record");
-  ACE_NEW_NORETURN (this->msg_data_, ACE_TCHAR[MAXLOGMSGLEN]);
+  ACE_NEW_NORETURN (this->msg_data_, ACE_TCHAR[this->msg_size_]);
+  if ( !this->msg_data_ )
+	this->msg_size_ = 0;
 }
 
 ACE_Log_Record::ACE_Log_Record (ACE_Log_Priority lp,
@@ -147,23 +262,13 @@
     secs_ ((ACE_UINT32) ts.sec ()),
     usecs_ ((ACE_UINT32) ts.usec ()),
     pid_ (ACE_UINT32 (p)),
-    msg_data_ (0)
+	byte_order_ ( ACE_CDR_BYTE_ORDER ),
+    msg_size_(MAXLOGMSGLEN)
 {
   // ACE_TRACE ("ACE_Log_Record::ACE_Log_Record");
-  ACE_NEW_NORETURN (this->msg_data_, ACE_TCHAR[MAXLOGMSGLEN]);
-}
-
-void
-ACE_Log_Record::round_up (void)
-{
-  // ACE_TRACE ("ACE_Log_Record::round_up");
-  // Determine the length of the payload.
-  size_t len = sizeof (*this) + (sizeof (ACE_TCHAR) * ((ACE_OS::strlen (this->msg_data_) + 1)));
-
-  // Round up to the alignment.
-  len = ((len + ACE_Log_Record::ALIGN_WORDB - 1)
-         & ~(ACE_Log_Record::ALIGN_WORDB - 1));
-  this->length_ = static_cast<ACE_UINT32> (len);
+  ACE_NEW_NORETURN (this->msg_data_,ACE_TCHAR[this->msg_size_]);
+  if ( !this->msg_data_ )
+    this->msg_size_ = 0;
 }
 
 ACE_Log_Record::ACE_Log_Record (void)
@@ -172,16 +277,19 @@
     secs_ (0),
     usecs_ (0),
     pid_ (0),
-    msg_data_ (0)
+	byte_order_ (ACE_CDR_BYTE_ORDER),
+    msg_size_(MAXLOGMSGLEN)
 {
   // ACE_TRACE ("ACE_Log_Record::ACE_Log_Record");
-  ACE_NEW_NORETURN (this->msg_data_, ACE_TCHAR[MAXLOGMSGLEN]);
+  ACE_NEW_NORETURN (this->msg_data_,ACE_TCHAR[this->msg_size_]);
+  if ( ! this->msg_data_ )
+		this->msg_size_ = 0;
 }
 
 int
 ACE_Log_Record::format_msg (const ACE_TCHAR host_name[],
                             u_long verbose_flag,
-                            ACE_TCHAR *verbose_msg)
+                            ACE_TCHAR *verbose_msg, const ACE_UINT32 verbose_msg_size)
 {
   /* 0123456789012345678901234     */
   /* Oct 18 14:25:36.000 1989<nul> */
@@ -229,7 +337,7 @@
       const ACE_TCHAR *lhost_name = ((host_name == 0)
                                       ? ACE_LIB_TEXT ("<local_host>")
                                       : host_name);
-      ACE_OS::sprintf (verbose_msg,
+      ACE_OS::snprintf (verbose_msg, verbose_msg_size,
                        verbose_fmt,
                        timestamp,
                        lhost_name,
@@ -238,13 +346,13 @@
                        this->msg_data_);
     }
   else if (ACE_BIT_ENABLED (verbose_flag, ACE_Log_Msg::VERBOSE_LITE))
-    ACE_OS::sprintf (verbose_msg,
+    ACE_OS::snprintf (verbose_msg, verbose_msg_size,
                      verbose_lite_fmt,
                      timestamp,
                      ACE_Log_Record::priority_name (ACE_Log_Priority (this->type_)),
                      this->msg_data_);
   else
-    ACE_OS::strcpy (verbose_msg, this->msg_data_);
+    ACE_OS::strncpy (verbose_msg, this->msg_data_, verbose_msg_size);
   return 0;
 }
 
@@ -255,29 +363,31 @@
 {
   if (ACE_LOG_MSG->log_priority_enabled (ACE_Log_Priority (this->type_)))
     {
-      ACE_TCHAR *verbose_msg = 0;
-      ACE_NEW_RETURN (verbose_msg, ACE_TCHAR[MAXVERBOSELOGMSGLEN], -1);
-
-      int result = this->format_msg (host_name,
-                                     verbose_flag,
-                                     verbose_msg);
+  ACE_TCHAR* verbose_msg;
+  ACE_UINT32 verbose_msg_size = this->length_ - sizeof(*this) + VERBOSE_LEN;
+  ACE_NEW_RETURN (verbose_msg,ACE_TCHAR[verbose_msg_size+1], -1);
+
+  int result = this->format_msg (host_name,
+                                 verbose_flag,
+                                 verbose_msg,
+								 verbose_msg_size);
 
       if (result == 0)
         {
-          if (fp != 0)
-            {
-              int verbose_msg_len =
-                static_cast<int> (ACE_OS::strlen (verbose_msg));
-              int fwrite_result = ACE_OS::fprintf (fp,
-                                                   ACE_LIB_TEXT ("%s"),
-                                                   verbose_msg);
-              // We should have written everything
-              if (fwrite_result != verbose_msg_len)
-                result = -1;
-              else
-                ACE_OS::fflush (fp);
-            }
-        }
+		if (fp != 0)
+		{
+			verbose_msg[verbose_msg_size]=0;
+			int verbose_msg_len =
+				static_cast<int> (ACE_OS::strlen (verbose_msg));
+			int fwrite_result = ACE_OS::fprintf (fp, ACE_LIB_TEXT ("%s"), verbose_msg);
+
+			// We should have written everything
+			if (fwrite_result != verbose_msg_len)
+				result = -1;
+			else
+				ACE_OS::fflush (fp);
+		}
+	}
 
       delete [] verbose_msg;
 
@@ -334,7 +444,7 @@
     cdr.read_char_array (log_msg, buffer_len);
 #endif /* ACE_USES_WCHAR */
     log_msg[buffer_len] = '\0';
-    log_record.set_msg_data_ptr (log_msg);
+    log_record.msg_data (log_msg, buffer_len, 0, 1);
   }
   return cdr.good_bit ();
 }
@@ -346,27 +456,52 @@
                        u_long verbose_flag,
                        ACE_OSTREAM_TYPE &s)
 {
-  if (ACE_LOG_MSG->log_priority_enabled (ACE_Log_Priority (this->type_)))
-    {
-      ACE_TCHAR* verbose_msg = 0;
-      ACE_NEW_RETURN (verbose_msg, ACE_TCHAR[MAXVERBOSELOGMSGLEN], -1);
-
-      int const result = this->format_msg (host_name, verbose_flag, verbose_msg);
+	if (ACE_LOG_MSG->log_priority_enabled (ACE_Log_Priority (this->type_)))
+	{
 
-      if (result == 0)
-        {
-          // Since ostream expects only chars, we cannot pass wchar_t's
-          s << ACE_TEXT_ALWAYS_CHAR (verbose_msg);
-          s.flush ();
-        }
+		ACE_TCHAR* verbose_msg;
+		ACE_UINT32 verbose_msg_size = this->length_ - sizeof(*this) + VERBOSE_LEN;
+		ACE_NEW_RETURN (verbose_msg,ACE_TCHAR[verbose_msg_size+1], -1);
+		int result = this->format_msg (host_name, verbose_flag, verbose_msg, verbose_msg_size);
+
+		if (result == 0)
+		{
+			verbose_msg[verbose_msg_size]=0;
+			// Since ostream expects only chars, we cannot pass wchar_t's
+			s << ACE_TEXT_ALWAYS_CHAR (verbose_msg);
+			s.flush ();
+		}
+
+		delete [] verbose_msg;
+
+		return result;
+	}
+	return 0;
+}
 
-      delete [] verbose_msg;
+#endif /* ! ACE_LACKS_IOSTREAM_TOTALLY */
 
-      return result;
-    }
-  return 0;
+int
+ACE_Log_Record::msg_data_len ( const ACE_INT32 new_len ) 
+{
+  ACE_TRACE ("ACE_Log_Record::msg_data_len");
+  if ( this->msg_size_ == 0 ) {
+	ACE_NEW_NORETURN ( this->msg_data_, ACE_TCHAR[ new_len ] );
+	this->msg_size_ = new_len;
+  } else {
+	if ( this->msg_size_  < new_len ) {
+		ACE_TCHAR* l_tmp_ptr;
+		ACE_NEW_NORETURN ( l_tmp_ptr, ACE_TCHAR[ new_len ] );
+		if ( l_tmp_ptr ) {
+			ACE_OS::memcpy( l_tmp_ptr, this->msg_data_, this->length_);
+			delete [] this->msg_data_;
+			this->msg_data_ = l_tmp_ptr;
+			this->msg_size_ = new_len;
+		}
+	}
+  }
+  return this->msg_size_;
 }
 
-#endif /* ! ACE_LACKS_IOSTREAM_TOTALLY */
 
 ACE_END_VERSIONED_NAMESPACE_DECL
Index: ace/Log_Record.h
===================================================================
RCS file: /cvs/ACE_wrappers-repository/ace/Log_Record.h,v
retrieving revision 4.56
diff -u -r4.56 Log_Record.h
--- ace/Log_Record.h	27 May 2006 15:52:26 -0000	4.56
+++ ace/Log_Record.h	22 Jun 2006 14:45:18 -0000
@@ -67,6 +67,8 @@
                   const ACE_Time_Value &time_stamp,
                   long pid);
 
+  ACE_Log_Record (const ACE_Log_Record& src);
+
   /// Default dtor.
   ~ACE_Log_Record (void);
 
@@ -91,7 +93,7 @@
 
   int format_msg (const ACE_TCHAR host_name[],
                   u_long verbose_flag,
-                  ACE_TCHAR *verbose_msg);
+                  ACE_TCHAR *verbose_msg, const ACE_UINT32 verbose_msg_size);
 
   /**
    * Returns a character array with the string form of the
@@ -105,11 +107,24 @@
                              const ACE_TCHAR *name);
 
   // = Marshall/demarshall
-  /// Encode the <Log_Record> for transmission on the network.
-  void encode (void);
+  /** Encode the <Log_Record> for transmission on the network.
+   *  \param obuf output buffer where to put encoded message, should be aligned on 32 bit border
+   *  \param obuf_length size of obuf
+   *  \return operation status code
+   *			- 0 -  success
+   *			- -1 - buffer is not aligned on 32 bit border
+   *			- >0 - size of encoded encoded log to be written to buffer. It's returned if obuf is NULL or it hasn't enough space
+   */
+  int encode (char* obuf, const size_t obuf_length);
 
-  /// Decode the <Log_Record> received from the network.
-  void decode (void);
+  /** Decode the <Log_Record> received from the network.
+   *  \param ibuf output buffer where to put encoded message, should be aligned on 32 bit border
+   *  \param ibuf_length size of ibuf
+   *  \return operation status code
+   *			- 0 -  success
+   *			- -1 - buffer is not aligned on 32 bit border
+   */
+  int decode (const char* ibuf, const size_t ibuf_length);
 
   // = Set/get methods
 
@@ -130,11 +145,11 @@
   /// power of 2, as defined by the enums in <ACE_Log_Priority>).
   void priority (u_long num);
 
-  /// Get the length of the <Log_Record>.
+  /// Get the length of the encoded <Log_Record> in bytes.
   long length (void) const;
 
-  /// Set the length of the <Log_Record>.
-  void length (long);
+  /// Set the length of the <Log_Record>. Obsolete, do nothing.
+  void length (long){}; // obsolete and senceless, setting length in old implementation may result in access to memory beyond object borders.
 
   /// Get the time stamp of the <Log_Record>.
   ACE_Time_Value time_stamp (void) const;
@@ -148,19 +163,36 @@
   /// Set the process id of the <Log_Record>.
   void pid (long);
 
-  /// Get the message data of the <Log_Record>.
-  const ACE_TCHAR *msg_data (void) const;
+  /** Return byte order of <Log_Record> payload i.e. ACE_CDR_BYTE_ORDER for origin <Log_Record> host 
+	* \return
+	*		- 1 - payload byteorder is LITTLE_ENDIAN
+	*		- 0 - payload byteorder is BIG_ENDIAN	
+  */
+  int byte_order(){ return this->byte_order_; }; 
 
-  /// Set the message data pointer 
-  void set_msg_data_ptr (ACE_TCHAR *data);
+  /// Get the payload of the <Log_Record>.
+  const ACE_TCHAR *msg_data (void) const;
 
-  /// Set the message data of the <Log_Record>.
-  void msg_data (const ACE_TCHAR *data);
+  /** Set the message data of the <Log_Record>.
+   *    For compatibility reason if specified only data then it's assumed that <data> is NUL-terminated string.
+   *	\param data - buffer to payload data
+   *	\param length - length of payload data in ACE_TCHAR, (default) it's 0 and it's assumed that <data> is NULL-terminated string.
+   *	\param copy_data - 1 - (default) copy payload to internal buffer, 0 - assign value of <data> to internal buffer
+   *	\param become_owner - 0 - (default) do not assume ownership of data buffer, 1 - assume ownership of data buffer
+   */ 
+  void msg_data (const ACE_TCHAR *data, const ACE_INT32 length=0, const int copy_data=1, const int become_owner=0);
 
-  /// Get the size of the message data of the <Log_Record>, including
-  /// a byte for the NUL.
+  /// Get the size of the message data of the <Log_Record> in ACE_TCHAR, including
+  /// NUL at the end of string.
   size_t msg_data_len (void) const;
 
+  /** Set size of internal payload buffer
+   * \return size of internal payload buffer
+   * causes reallocation of payload buffer if new_len > current size of buffer, all data copied to new buffer
+   * if ACE_Log_Record don't own buffer then new buffer of new_len size will be allocated and assigned to this object
+   */
+  int msg_data_len ( const ACE_INT32 new_len);
+
   /// Dump the state of an object.
   void dump (void) const;
 
@@ -168,15 +200,8 @@
   ACE_ALLOC_HOOK_DECLARE;
 
 private:
-  /// Round up to the alignment restrictions.
-  void round_up (void);
 
-  /**
-   * Total length of the logging record in bytes.  This field *must*
-   * come first in order for various IPC framing mechanisms to work
-   * correctly.  In addition, the field must be an ACE_INT32 in order
-   * to be passed portably across platforms.
-   */
+  /// length of the message in msg_data_ (i.e. amount of used ACE_TCHARs).  
   ACE_INT32 length_;
 
   /// Type of logging record.
@@ -189,8 +214,14 @@
   /// Id of process that generated the logging record.
   ACE_UINT32 pid_;
 
+  /// Byte order of msg_data_ part
+  ACE_UINT32 byte_order_;
+
+  /// Current capacity of msg_data_ buffer
+  ACE_INT32 msg_size_; 
+
   /// Logging record data
-  ACE_TCHAR *msg_data_; // Add one for NUL-terminator.
+  ACE_TCHAR* msg_data_; // Add one for NUL-terminator.
 
 };
 
Index: ace/Log_Record.inl
===================================================================
RCS file: /cvs/ACE_wrappers-repository/ace/Log_Record.inl,v
retrieving revision 4.5
diff -u -r4.5 Log_Record.inl
--- ace/Log_Record.inl	16 May 2006 22:27:33 -0000	4.5
+++ ace/Log_Record.inl	22 Jun 2006 14:45:18 -0000
@@ -12,35 +12,12 @@
 ACE_INLINE
 ACE_Log_Record::~ACE_Log_Record (void)
 {
-  if (this->msg_data_)
-    delete[] this->msg_data_;
-}
-
-ACE_INLINE void
-ACE_Log_Record::encode (void)
-{
-  ACE_TRACE ("ACE_Log_Record::encode");
-  this->length_ = htonl (this->length_);
-  this->type_ = htonl (this->type_);
-  // Make sure we don't enclose the sec() and usec() fields until
-  // they've been normalized.
-  this->secs_ = htonl (this->secs_);
-  this->usecs_ = htonl (this->usecs_);
-  this->pid_ = htonl (this->pid_);
-}
-
-ACE_INLINE void
-ACE_Log_Record::decode (void)
-{
-  ACE_TRACE ("ACE_Log_Record::decode");
-  ACE_Time_Value tv (ntohl (this->secs_),
-                     ntohl (this->usecs_));
-
-  this->secs_ = tv.sec ();
-  this->usecs_ = tv.usec ();
-  this->type_ = ntohl (this->type_);
-  this->pid_ = ntohl (this->pid_);
-  this->length_ = ntohl (this->length_);
+  // if we own msg_data_ and it not 0 then delete it
+  if(this->msg_size_ && this->msg_data_){
+	 delete[] this->msg_data_;
+	 this->msg_data_ = 0;
+	 this->msg_size_ = 0;
+  }
 }
 
 ACE_INLINE ACE_UINT32
@@ -61,14 +38,7 @@
 ACE_Log_Record::length (void) const
 {
   ACE_TRACE ("ACE_Log_Record::length");
-  return (long) this->length_;
-}
-
-ACE_INLINE void
-ACE_Log_Record::length (long l)
-{
-  ACE_TRACE ("ACE_Log_Record::length");
-  this->length_ = static_cast<ACE_UINT32> (l);
+  return (long) this->length_*sizeof(ACE_TCHAR) + sizeof(ACE_UINT32)*6;
 }
 
 ACE_INLINE ACE_Time_Value
@@ -107,17 +77,12 @@
   return this->msg_data_;
 }
 
-ACE_INLINE void
-ACE_Log_Record::set_msg_data_ptr (ACE_TCHAR *data)
-{
-  this->msg_data_ = data;
-}
-
 ACE_INLINE size_t
 ACE_Log_Record::msg_data_len (void) const
 {
   ACE_TRACE ("ACE_Log_Record::msg_data_len");
-  return ACE_OS::strlen (this->msg_data_) + 1;
+  return this->length_;
 }
 
+
 ACE_END_VERSIONED_NAMESPACE_DECL
Index: examples/APG/Logging/Callback-3.h
===================================================================
RCS file: /cvs/ACE_wrappers-repository/examples/APG/Logging/Callback-3.h,v
retrieving revision 1.2
diff -u -r1.2 Callback-3.h
--- examples/APG/Logging/Callback-3.h	23 Feb 2006 23:23:39 -0000	1.2
+++ examples/APG/Logging/Callback-3.h	22 Jun 2006 14:45:19 -0000
@@ -53,13 +53,16 @@
 	}
 
       size_t len = log_record.length();
-      log_record.encode ();
-
-      if (this->logger_->send_n ((char *) &log_record, len) == -1)
-	{
-	  delete this->logger_;
-	  this->logger_ = 0;
-	}
+	  char* tmp_buf;
+	  ACE_NEW_NORETURN( tmp_buf, char[len] );
+	  if ( log_record.encode (tmp_buf, len) == 0 ){
+		  if (this->logger_->send_n ((char *) tmp_buf, len) == -1)
+		  {
+			  delete this->logger_;
+			  this->logger_ = 0;
+		  }
+	  }
+	  delete[] tmp_buf;
     }
 
 private:
Index: examples/Log_Msg/test_callback.cpp
===================================================================
RCS file: /cvs/ACE_wrappers-repository/examples/Log_Msg/test_callback.cpp,v
retrieving revision 4.11
diff -u -r4.11 test_callback.cpp
--- examples/Log_Msg/test_callback.cpp	20 Jun 2006 07:42:07 -0000	4.11
+++ examples/Log_Msg/test_callback.cpp	22 Jun 2006 14:45:21 -0000
@@ -85,7 +85,7 @@
       ACE_TCHAR verbose_msg[ACE_Log_Record::MAXVERBOSELOGMSGLEN];
       int result = log_record.format_msg (ACE_LOG_MSG->local_host (),
                                           ACE_LOG_MSG->flags (),
-                                          verbose_msg);
+                                          verbose_msg, sizeof(verbose_msg) );
       if (result == 0)
         {
           if (use_log_msg)
Index: examples/Logger/Acceptor-server/server_loggerd.cpp
===================================================================
RCS file: /cvs/ACE_wrappers-repository/examples/Logger/Acceptor-server/server_loggerd.cpp,v
retrieving revision 4.34
diff -u -r4.34 server_loggerd.cpp
--- examples/Logger/Acceptor-server/server_loggerd.cpp	20 Apr 2006 11:05:57 -0000	4.34
+++ examples/Logger/Acceptor-server/server_loggerd.cpp	22 Jun 2006 14:45:21 -0000
@@ -106,7 +106,7 @@
   // receiver side.  To correctly handle this is painful, and we leave
   // it as an exercise for the reader ;-).
 
-  ssize_t len;
+  int32_t len;
   ssize_t n = this->peer ().recv ((void *) &len, sizeof len);
 
   switch (n)
@@ -123,18 +123,20 @@
     case sizeof (size_t):
       {
         ACE_Log_Record lp;
-
+		char* tmp_buf;
+		ACE_NEW_NORETURN( tmp_buf, char[ntohl (len)] );
+		*((ACE_INT32*)tmp_buf) = len;
         len = ntohl (len);
-        n = this->peer ().recv_n ((void *) &lp, len);
+		
+        n = this->peer ().recv_n ( tmp_buf+sizeof(ACE_INT32), len);
 
-        if (n != len)
+        if (n != (len-sizeof(ACE_INT32)) )
           ACE_ERROR_RETURN ((LM_ERROR, ACE_TEXT ("(%P|%t) %p at host %C\n"),
                              ACE_TEXT ("client logger"), this->peer_name_),-1);
         /* NOTREACHED */
 
-        lp.decode ();
 
-        if (lp.length () == n)
+        if ( lp.decode (tmp_buf,len) == 0 )
           {
             ACE_DEBUG ((LM_DEBUG, ACE_TEXT ("(%P|%t) ")));
 #if !defined (ACE_LACKS_IOSTREAM_TOTALLY)
@@ -147,6 +149,8 @@
           ACE_ERROR ((LM_ERROR,
                       ACE_TEXT ("(%P|%t) error, lp.length = %d, n = %d\n"),
                       lp.length (), n));
+
+		delete[] tmp_buf;
         break;
       }
     default:
Index: examples/Logger/client/logging_app.cpp
===================================================================
RCS file: /cvs/ACE_wrappers-repository/examples/Logger/client/logging_app.cpp,v
retrieving revision 4.9
diff -u -r4.9 logging_app.cpp
--- examples/Logger/client/logging_app.cpp	27 Oct 2004 21:06:58 -0000	4.9
+++ examples/Logger/client/logging_app.cpp	22 Jun 2006 14:45:21 -0000
@@ -38,15 +38,14 @@
                                  ACE_OS::getpid ());
 
       ACE_TCHAR buf[BUFSIZ];
+      ACE_TCHAR log_buf[BUFSIZ+sizeof(log_record)];
       ACE_OS::sprintf (buf, ACE_TEXT ("message = %d\n"), i + 1);
       log_record.msg_data (buf);
-      size_t len = log_record.length ();
-      size_t encoded_len = htonl (len);
 
-      log_record.encode ();
+      log_record.encode (log_buf, sizeof(log_buf) );
+      size_t len = log_record.length ();
 
-      if (logger.send (4, &encoded_len, sizeof encoded_len,
-		       (char *) &log_record, len) == -1)
+      if (logger.send (2, (char *) log_buf, len) == -1)
 	ACE_ERROR_RETURN ((LM_ERROR, ACE_TEXT ("%p\n"), ACE_TEXT ("send")),-1);
       else
 	ACE_OS::sleep (1);
Index: examples/Logger/simple-server/Logging_Handler.cpp
===================================================================
RCS file: /cvs/ACE_wrappers-repository/examples/Logger/simple-server/Logging_Handler.cpp,v
retrieving revision 4.18
diff -u -r4.18 Logging_Handler.cpp
--- examples/Logger/simple-server/Logging_Handler.cpp	27 Oct 2004 21:06:58 -0000	4.18
+++ examples/Logger/simple-server/Logging_Handler.cpp	22 Jun 2006 14:45:21 -0000
@@ -52,7 +52,7 @@
 Logging_Handler::handle_input (ACE_HANDLE)
 {
   ssize_t n;
-  size_t len;
+  int32_t len;
 
   // Perform two recv's to emulate record-oriented semantics.  Note
   // that this code is not entirely portable since it relies on the
@@ -71,20 +71,22 @@
                          "(%P|%t) closing log daemon at host %s (fd = %d)\n",
                          this->host_name_, this->get_handle ()), -1);
       /* NOTREACHED */
-    case sizeof (size_t):
+    case sizeof (len):
       {
         ACE_Log_Record lp;
 
+		char* tmp_buf;
+		ACE_NEW_NORETURN( tmp_buf, char[ ntohl (len) ] );
+		*((int32_t*)tmp_buf) = len;
+
         len = ntohl (len);
-        n = this->cli_stream_.recv_n ((void *) &lp, len);
-        if (n != (ssize_t) len)
+        n = this->cli_stream_.recv_n ( tmp_buf+sizeof(len), len);
+        if (n != (ssize_t) (len-sizeof(len)))
           ACE_ERROR_RETURN ((LM_ERROR, ACE_TEXT ("(%P|%t) %p at host %C\n"),
                              ACE_TEXT ("client logger"), this->host_name_), -1);
         /* NOTREACHED */
 
-        lp.decode ();
-
-        if (lp.length () == n)
+        if (lp.decode (tmp_buf,len) == 0)
           {
             ACE_DEBUG ((LM_DEBUG, ACE_TEXT ("(%P|%t) ")));
             lp.print (ACE_TEXT_CHAR_TO_TCHAR (this->host_name_), 1);
@@ -93,6 +95,8 @@
           ACE_ERROR ((LM_ERROR,
                       ACE_TEXT ("(%P|%t) error, lp.length = %d, n = %d\n"),
                       lp.length (), n));
+
+		delete[] tmp_buf;
         break;
       }
     default:
Index: tests/Log_Msg_Test.cpp
===================================================================
RCS file: /cvs/ACE_wrappers-repository/tests/Log_Msg_Test.cpp,v
retrieving revision 4.43
diff -u -r4.43 Log_Msg_Test.cpp
--- tests/Log_Msg_Test.cpp	20 Apr 2006 07:41:47 -0000	4.43
+++ tests/Log_Msg_Test.cpp	22 Jun 2006 14:45:22 -0000
@@ -111,7 +111,7 @@
       ACE_TCHAR verbose_msg[ACE_Log_Record::MAXVERBOSELOGMSGLEN];
       int result = log_record.format_msg (ACE_LOG_MSG->local_host (),
                                           ACE_LOG_MSG->flags (),
-                                          verbose_msg);
+                                          verbose_msg, sizeof(verbose_msg) );
       if (result == 0)
         {
           if (use_log_msg)
Index: netsvcs/clients/Logger/direct_logging.cpp
===================================================================
RCS file: /cvs/ACE_wrappers-repository/netsvcs/clients/Logger/direct_logging.cpp,v
retrieving revision 4.11
diff -u -r4.11 direct_logging.cpp
--- netsvcs/clients/Logger/direct_logging.cpp	23 May 2006 15:51:48 -0000	4.11
+++ netsvcs/clients/Logger/direct_logging.cpp	22 Jun 2006 14:45:22 -0000
@@ -34,42 +34,24 @@
     ACE_ERROR_RETURN ((LM_ERROR, "%p\n", "open"), -1);
 
   log_record.msg_data (DATA);
-  const size_t max_payload_size =
-    4 // type()
-    + 8 // timestamp
-    + 4 // process id
-    + 4 // data length
-    + ACE_Log_Record::MAXLOGMSGLEN // data
-    + ACE_CDR::MAX_ALIGNMENT; // padding;
 
-  // Insert contents of <log_record> into payload stream.
-  ACE_OutputCDR payload (max_payload_size);
-  payload << log_record;
-
-  // Get the number of bytes used by the CDR stream.
-  ACE_CDR::ULong length = payload.total_length ();
-
-  // Send a header so the receiver can determine the byte order and
-  // size of the incoming CDR stream.
-  ACE_OutputCDR header (ACE_CDR::MAX_ALIGNMENT + 8);
-  header << ACE_OutputCDR::from_boolean (ACE_CDR_BYTE_ORDER);
-
-  // Store the size of the payload that follows
-  header << ACE_CDR::ULong (length);
-
-  // Use an iovec to send both buffer and payload simultaneously.
-  iovec iov[2];  
-  iov[0].iov_base = header.begin ()->rd_ptr ();
-  iov[0].iov_len  = 8;
-  iov[1].iov_base = payload.begin ()->rd_ptr ();
-  iov[1].iov_len  = length;
-
-  if (logger.sendv_n (iov, 2) == -1)
-    ACE_ERROR_RETURN ((LM_ERROR, "%p\n", "send"), -1);
-  else if (logger.close () == -1)
-    ACE_ERROR_RETURN ((LM_ERROR, "%p\n", "close"), -1);
-
-#if defined (ACE_WIN32)
+  size_t len = log_record.length ();
+  char* tmp_buf;
+  ACE_NEW_NORETURN(tmp_buf, char[len] );
+  if ( log_record.encode (tmp_buf, len) == 0 )
+  {
+	  if (logger.send (tmp_buf, len) == -1)
+	  {
+		  delete[] tmp_buf; 
+		  ACE_ERROR_RETURN ((LM_ERROR, "%p\n", "send"), -1);
+	  } else if (logger.close () == -1)
+	         {
+			 delete[] tmp_buf; 
+			 ACE_ERROR_RETURN ((LM_ERROR, "%p\n", "close"), -1);
+		 }
+  }
+  
+ #if defined (ACE_WIN32)
   // !!Important, Winsock is broken in that if you don't close
   // down the connection before exiting main, you'll lose data.
   // More over, your server might get "Access Violation" from
Index: netsvcs/lib/Client_Logging_Handler.cpp
===================================================================
RCS file: /cvs/ACE_wrappers-repository/netsvcs/lib/Client_Logging_Handler.cpp,v
retrieving revision 4.70
diff -u -r4.70 Client_Logging_Handler.cpp
--- netsvcs/lib/Client_Logging_Handler.cpp	19 Jun 2006 12:51:17 -0000	4.70
+++ netsvcs/lib/Client_Logging_Handler.cpp	22 Jun 2006 14:45:23 -0000
@@ -105,12 +105,12 @@
                        ACE_TEXT ("received data from server!\n")),
                       -1);
   ACE_Log_Record log_record;
+  ACE_INT32 length;
 
   // We need to use the old two-read trick here since TCP sockets
   // don't support framing natively.  Allocate a message block for the
   // payload; initially at least large enough to hold the header, but
   // needs some room for alignment.
-  ACE_Message_Block *payload_p = 0;
   ACE_Message_Block *header_p = 0;
   ACE_NEW_RETURN (header_p,
                   ACE_Message_Block (ACE_DEFAULT_CDR_BUFSIZE),
@@ -118,11 +118,8 @@
 
   auto_ptr <ACE_Message_Block> header (header_p);
 
-  // Align the Message Block for a CDR stream
-  ACE_CDR::mb_align (header.get ());
-
-  ACE_CDR::Boolean byte_order;
-  ACE_CDR::ULong length;
+  // Align the Message Block buffer
+  header_p->reset();
 
 #if defined (ACE_HAS_STREAM_PIPES)
   // We're getting a logging message from a local application using
@@ -130,7 +127,7 @@
 
   ACE_Str_Buf header_msg (header->wr_ptr (),
                           0,
-                          8);
+                          sizeof(length) );
 
   ACE_SPIPE_Stream spipe;
   spipe.set_handle (handle);
@@ -164,7 +161,7 @@
 
   ssize_t count = ACE::recv_n (handle,
 			       header->wr_ptr (),
-			       8);
+			       sizeof(length));
   switch (count)
     {
       // Handle shutdown and error cases.
@@ -191,40 +188,25 @@
       return 0;
       /* NOTREACHED */
 
-    case 8:
+    case sizeof(length):
       // Just fall through in this case..
       break;
     }
 #endif /* ACE_HAS_STREAM_PIPES */
 
   // Reflect addition of 8 bytes for the header.
-  header->wr_ptr (8); 
-
-  // Create a CDR stream to parse the 8-byte header.
-  ACE_InputCDR header_cdr (header.get ());
-
-  // Extract the byte-order and use helper methods to disambiguate
-  // octet, booleans, and chars.
-  header_cdr >> ACE_InputCDR::to_boolean (byte_order);
-
-  // Set the byte-order on the stream...
-  header_cdr.reset_byte_order (byte_order);
+  header->wr_ptr (sizeof(length)); 
 
   // Extract the length
-  header_cdr >> length;
-
-  ACE_NEW_RETURN (payload_p,
-                  ACE_Message_Block (length),
-                  -1);
-  auto_ptr <ACE_Message_Block> payload (payload_p);
+  length = ACE_NTOHL ( *reinterpret_cast<ACE_INT32*>(header->base()) );
 
-  // Ensure there's sufficient room for log record payload.
-  ACE_CDR::grow (payload.get (), 8 + ACE_CDR::MAX_ALIGNMENT + length);
+  // enlarge buffer to hold whole message
+  header->size(length);
 
 #if defined (ACE_HAS_STREAM_PIPES)
-  ACE_Str_Buf payload_msg (payload->wr_ptr (),
+  ACE_Str_Buf payload_msg (header->wr_ptr (),
                            0,
-                           length);
+                           length - sizeof(length) );
 
   // We've got a framed IPC mechanism, so we can just do a <recv>.
   result = spipe.recv ((ACE_Str_Buf *) 0,
@@ -251,7 +233,7 @@
     }
 #else
   // Use <recv_n> to obtain the contents.
-  if (ACE::recv_n (handle, payload->wr_ptr (), length) <= 0)
+  if (ACE::recv_n (handle, header->wr_ptr (), length - sizeof(length) ) <= 0)
     {
       ACE_ERROR ((LM_ERROR,
                   ACE_TEXT ("%p\n"),
@@ -272,13 +254,14 @@
 #endif /* ACE_HAS_STREAM_PIPES */
 
   // Reflect additional bytes for the message.
-  payload->wr_ptr (length);   
+  header->wr_ptr (length - sizeof(length));   
 
-  ACE_InputCDR payload_cdr (payload.get ());
-  payload_cdr.reset_byte_order (byte_order);
-  payload_cdr >> log_record;  // Finally extract the <ACE_log_record>.
 
-  log_record.length (length);
+  if ( log_record.decode ( header->base(), header->length() ) != 0 )
+  {
+      ACE_ERROR ((LM_ERROR, ACE_TEXT ("log_record.decode is failed")));
+      return -1;
+  }
 
   // Forward the logging record to the server.
   if (this->send (log_record) == -1)
@@ -337,59 +320,35 @@
                       ACE_Log_Msg::instance ()->flags (),
                       stderr);
   else
-    {
-      // Serialize the log record using a CDR stream, allocate enough
-      // space for the complete <ACE_Log_Record>.
-      const size_t max_payload_size =
-	4 // type()
-	+ 8 // timestamp
-	+ 4 // process id
-	+ 4 // data length
-	+ ACE_Log_Record::MAXLOGMSGLEN // data
-	+ ACE_CDR::MAX_ALIGNMENT; // padding;
-
-      // Insert contents of <log_record> into payload stream.
-      ACE_OutputCDR payload (max_payload_size);
-      payload << log_record;
-
-      // Get the number of bytes used by the CDR stream.
-      ACE_CDR::ULong length = payload.total_length ();
-
-      // Send a header so the receiver can determine the byte order and
-      // size of the incoming CDR stream.
-      ACE_OutputCDR header (ACE_CDR::MAX_ALIGNMENT + 8);
-      header << ACE_OutputCDR::from_boolean (ACE_CDR_BYTE_ORDER);
-
-      // Store the size of the payload that follows
-      header << ACE_CDR::ULong (length);
-
-      // Use an iovec to send both buffer and payload simultaneously.
-      iovec iov[2];
-      iov[0].iov_base = header.begin ()->rd_ptr ();
-      iov[0].iov_len  = 8;
-      iov[1].iov_base = payload.begin ()->rd_ptr ();
-      iov[1].iov_len  = length;
-
-      // We're running over sockets, so send header and payload
-      // efficiently using "gather-write".
-      if (ACE::sendv_n (this->logging_output_,iov, 2) == -1)
-	{
-	  ACE_DEBUG ((LM_DEBUG, 
-                      "Something about the sendv_n() failed, so switch to stderr\n"));
-	  if (ACE_Log_Msg::instance ()->msg_ostream () == 0)
-	    // Switch over to logging to stderr for now.  At some
-	    // point, we'll improve the implementation to queue up the
-	    // message, try to reestablish a connection, and then send
-	    // the queued data once we've reconnect to the logging
-	    // server.  If you'd like to implement this functionality
-	    // and contribute it back to ACE that would be great!
-	    this->logging_output_ = ACE_STDERR;
-	}
-      else
-        ACE_DEBUG ((LM_DEBUG,
-                    "Sent logging message %s successfully to Server Logging Daemon!\n",
-                    log_record.priority_name (ACE_Log_Priority (log_record.type ()))));
-    }
+  {
+
+	  long len = log_record.length ();
+	  char* tmp_buf; 
+	  ACE_NEW_NORETURN (tmp_buf, char[len] );
+	  if (log_record.encode (tmp_buf,len) == 0){
+
+		  // We're running over sockets, so send header and payload
+		  // efficiently using "gather-write".
+		  if (ACE::send (this->logging_output_, tmp_buf, len) == -1)
+		  {
+			  ACE_DEBUG ((LM_DEBUG, 
+						  "Something about the sendv_n() failed, so switch to stderr\n"));
+			  if (ACE_Log_Msg::instance ()->msg_ostream () == 0)
+				  // Switch over to logging to stderr for now.  At some
+				  // point, we'll improve the implementation to queue up the
+				  // message, try to reestablish a connection, and then send
+				  // the queued data once we've reconnect to the logging
+				  // server.  If you'd like to implement this functionality
+				  // and contribute it back to ACE that would be great!
+				  this->logging_output_ = ACE_STDERR;
+		  }
+		  else
+			  ACE_DEBUG ((LM_DEBUG,
+						  "Sent logging message %s successfully to Server Logging Daemon!\n",
+						  log_record.priority_name (ACE_Log_Priority (log_record.type ()))));
+	  }
+	  delete[] tmp_buf;
+  }
 
   return 0;
 }
Index: netsvcs/lib/Server_Logging_Handler_T.cpp
===================================================================
RCS file: /cvs/ACE_wrappers-repository/netsvcs/lib/Server_Logging_Handler_T.cpp,v
retrieving revision 4.44
diff -u -r4.44 Server_Logging_Handler_T.cpp
--- netsvcs/lib/Server_Logging_Handler_T.cpp	26 May 2006 21:49:34 -0000	4.44
+++ netsvcs/lib/Server_Logging_Handler_T.cpp	22 Jun 2006 14:45:23 -0000
@@ -64,7 +64,6 @@
   // don't support framing natively.  Allocate a message block for the
   // payload; initially at least large enough to hold the header, but
   // needs some room for alignment.
-  ACE_Message_Block *payload_p = 0;
   ACE_Message_Block *header_p = 0;
   ACE_NEW_RETURN (header_p,
                   ACE_Message_Block (ACE_DEFAULT_CDR_BUFSIZE),
@@ -72,15 +71,14 @@
 
   auto_ptr <ACE_Message_Block> header (header_p);
 
-  // Align the Message Block for a CDR stream
-  ACE_CDR::mb_align (header.get ());
-
-  ACE_CDR::Boolean byte_order;
-  ACE_CDR::ULong length;
+  // Align the Message Block buffer
+  header_p->reset();
+  
+  ACE_INT32 length;
 
   ssize_t count = ACE::recv_n (this->peer ().get_handle (), 
 			       header->wr_ptr (),
-			       8);
+			       sizeof(ACE_INT32) );
   switch (count)
     {
       // Handle shutdown and error cases.
@@ -95,38 +93,20 @@
       return -1;
       /* NOTREACHED */
 
-    case 8:
+    case sizeof(ACE_INT32):
       // Just fall through in this case..
       break;
     }
 
-  header->wr_ptr (8); // Reflect addition of 8 bytes.
+  header->wr_ptr (sizeof(ACE_INT32)); // Reflect addition of 8 bytes.
 
-  // Create a CDR stream to parse the 8-byte header.
-  ACE_InputCDR header_cdr (header.get ());
-
-  // Extract the byte-order and use helper methods to disambiguate
-  // octet, booleans, and chars.
-  header_cdr >> ACE_InputCDR::to_boolean (byte_order);
-
-  // Set the byte-order on the stream...
-  header_cdr.reset_byte_order (byte_order);
-
-  // Extract the length
-  header_cdr >> length;
-  
-  ACE_NEW_RETURN (payload_p,
-                  ACE_Message_Block (length),
-                  -1);
-  auto_ptr <ACE_Message_Block> payload (payload_p);
-
-  // Ensure there's sufficient room for log record payload.
-  ACE_CDR::grow (payload.get (), 8 + ACE_CDR::MAX_ALIGNMENT + length);
+  length = ACE_NTOHL( *reinterpret_cast<ACE_INT32*>(header->base()) );
+  header->size(length);
 
   // Use <recv_n> to obtain the contents.
   if (ACE::recv_n (this->peer ().get_handle (),
-                   payload->wr_ptr (),
-                   length) <= 0) 
+                   header->wr_ptr (),
+                   length - sizeof(ACE_INT32) ) <= 0) 
     {
       ACE_ERROR ((LM_ERROR,
                   ACE_TEXT ("%p\n"),
@@ -134,14 +114,13 @@
       return -1;
     }
 
-  payload->wr_ptr (length);   // Reflect additional bytes
-
-  ACE_InputCDR payload_cdr (payload.get ());
-  payload_cdr.reset_byte_order (byte_order);
-  payload_cdr >> log_record;  // Finally extract the <ACE_log_record>.
-
-  log_record.length (length);
+  header->wr_ptr (length - sizeof(ACE_INT32));   // Reflect additional bytes
 
+  if ( log_record.decode( header->base(), length) != 0 )
+  {
+	  ACE_ERROR ((LM_ERROR, ACE_TEXT ("log_record.decode is failed\n")));
+	  return -1;
+   }
   // Send the log record to the log message receiver for processing.
   if (ACE_BIT_ENABLED (ACE_Log_Msg::instance ()->flags (),
 		       ACE_Log_Msg::STDERR))
